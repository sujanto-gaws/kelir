{
  "name": "kelir-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Browser-driving end-to-end harness for a deployed Kelir stack (decision D-14).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "@types/node": "^26.2.0",
    "typescript": "^5.7.0"
  }
}
