# Kelir Product Backlog

**Status:** Living · **Last updated:** 2026-09-01

The [Sprint Plan](01.%20Sprint%20Plan.md) says *when* work happens; this document says *what the work is* and proves that every requirement has a home. It is the scope inventory: each of the 164 functional requirements in the [SRS](../../docs/requirements/srs.md) §4 is assigned to an epic, a backlog item, and a target sprint — or listed explicitly as unscheduled.

Scope authority remains the SRS. Where this backlog and the SRS disagree, the SRS wins and this document is corrected; where this backlog and the sprint plan disagree on sequence, the sprint plan wins.

---

## 1. How This Backlog Is Organized

- **Epic** — one per SRS §4 requirement area, keyed by the area's FR prefix (`AUTH`, `IDM`, …). Epics are stable; items inside them churn.
- **Item** — a demoable slice, sized to fit inside one sprint, carrying the FR IDs it satisfies. Items are the unit that enters a sprint and the unit a status report reports on.
- **Target** — the sprint from the [sprint map](01.%20Sprint%20Plan.md#3-sprint-map). `Stretch` means the item is planned into that phase but is the first thing cut when velocity demands it. `Unscheduled` means no phase claims it yet.
- **Priority** — from the SRS (`Must` / `Should` / `Could`). Priorities are not re-assigned here; they are copied.

Every FR ID appears in exactly one item row or in the unscheduled list (§7). §8 reconciles the counts.

## 2. Definition of Ready

The sprint plan defines *Done*; an item is **Ready** to be pulled into a sprint when:

1. It names its FR IDs, and its acceptance criteria are stated in SRS terms.
2. Its data model exists in the [Database Schema](../../docs/design/02.%20Database%20Schema.md), or the migration it needs is identified by file name.
3. Its API surface is expressible under the [naming convention](../../docs/standards/02.%20Naming%20Convention.md) §5, and its permission strings (`module:resource:action`) are decided.
4. Its upstream dependencies are Done or in the same sprint.
5. It is demoable on its own — an item that cannot be shown is a task, not a backlog item.

## 3. Epic Map

| Epic | SRS § | FRs | Phase | Sprints | Release |
|---|---|---|---|---|---|
| AUTH — Authentication and session | 4.1 | 8 | 2 | 3–4 | `v0.2.0` |
| IDM — Users, roles, permissions | 4.2 | 9 | 2 | 3–4 | `v0.2.0` |
| ORG — Organization | 4.3 | 5 | 2 | 3–4 | `v0.2.0` |
| MDM — Master data (Party model) | 4.4 | 11 | 3 | 5–6 | `v0.3.0` |
| RAD — Metadata and dynamic rendering | 4.5 | 12 | 4 + 7 | 7–9, 14–16 | `v0.4.0`, `v0.7.0` |
| DTYPE — Document types | 4.6 | 8 | 4 | 7–9 | `v0.4.0` |
| DOC — Document management | 4.7 | 14 | 4 | 7–9 | `v0.4.0` |
| WF — Workflow engine | 4.8 | 16 | 5 | 10–11 | `v0.5.0` |
| TASK — Task inbox | 4.9 | 9 | 5 | 10–11 | `v0.5.0` |
| ATT — Attachments | 4.10 | 10 | 6 | 12–13 | `v0.6.0` |
| CMT — Comments | 4.11 | 7 | 6 | 12–13 | `v0.6.0` |
| ACT — Activity events | 4.12 | 5 | 6 | 12–13 | `v0.6.0` |
| AUD — Audit trail | 4.12 | 5 | 6 | 12–13 | `v0.6.0` |
| NTF — Notifications | 4.13 | 7 | 6 | 12–13 | `v0.6.0` |
| RPT — Reporting and dashboard | 4.14 | 8 | 8 | 17 | `v0.8.0` |
| SRH — Search | 4.15 | 4 | 3–4, 8 | 5–9, 17 | `v0.3.0`+ |
| INT — Integration | 4.16 | 10 | 9 | 18–19 | `v0.9.0` |
| PLG — Plugins and extensions | 4.17 | 8 | 9 | 18–19 | `v0.9.0` |
| API — API surface | 4.18 | 8 | 1 (continuous) | 1–21 | every |

RAD spans two phases deliberately (decision **D-2**, §6.1). The MVP is tagged at the end of the Phase 6 epics (decision **D-1**) — every epic above ATT/CMT/ACT/AUD/NTF in this table is MVP-gating.

---

## 4. Backlog Items by Epic

> **A migration that has not been written yet is named by what it creates, not by a number.** Naming convention §4.3 gives a migration the next free number *when it is written*, so any number written down in advance is a guess that the next out-of-plan migration invalidates — and this document guessed wrong for four consecutive shifts, carrying `0009_rad.sql` through `0017_plugin.sql` long after the sequence had moved past them. The [Database Schema](../../docs/design/02.%20Database%20Schema.md) mapping table is the sequence and is the only place that states it. Migrations that already exist keep their numbers below, because those are fixed.


### Sprint 0 — Foundation Enablers (no FR coverage)

| Item | Target | Notes |
|---|---|---|
| Repository, branch protection, CI pipeline | Sprint 0 | [Git workflow](../../docs/standards/05.%20Git%20Workflow.md); commit-header lint per [convention 03](../../docs/standards/03.%20Commit%20Message%20Convention.md) |
| Docker Compose stack (postgres, minio, mailpit, backend, frontend) | Sprint 0 | Satisfies MVP criterion 15 and NFR-AVA-001 |
| Backlog seeded into the issue tracker | Sprint 0 | This document is the source; the tracker is the working copy |

### AUTH — Authentication and Session (SRS §4.1)

| Item | FRs | Priority | Target |
|---|---|---|---|
| Password login/logout with Argon2 hashing and token issuance | FR-AUTH-001, 002, 003, 004 | Must | Sprint 3 |
| Login activity written to the audit log | FR-AUTH-008 | Must | Sprint 3 |
| Change password | FR-AUTH-005 | Should | Sprint 5 — carry-over; built in Sprint 4, held open by its test debt (#60) |
| Forgot/reset password over email (mailpit locally) | FR-AUTH-006 | Should | Sprint 7 — carried twice, then scheduled (**D-13**), then built. `password_reset_tokens` had existed unread since `0006`: the table shipped in Sprint 4, the flow in Sprint 7 |
| External SSO / OIDC | FR-AUTH-007 | Could | Unscheduled — SRS §4.1 defers it explicitly |

FR-AUTH-008 depends on the audit write path; in Phase 2 it writes rows, and the tamper-evidence work lands with AUD in Phase 6.

### IDM — Users, Roles, Permissions (SRS §4.2)

| Item | FRs | Priority | Target |
|---|---|---|---|
| User CRUD with active/inactive status | FR-IDM-001, 007 | Must | Sprint 3 |
| Role CRUD, permission catalogue, role–permission mapping, role assignment | FR-IDM-002, 003, 004, 005 | Must | Sprint 3 |
| Permission middleware enforcing `module:resource:action` server-side | FR-IDM-005, FR-API-008 | Must | Sprint 3 |
| Admin screens for users, roles, permissions | FR-IDM-001..005 | Must | Sprint 4 |
| User delegation | FR-IDM-006 | Should | **Sprint 11 — built** ([#184](https://github.com/sujanto-gaws/kelir/issues/184)), with FR-WF-009 and FR-TASK-008 as one item per **D-17**. Returned from §7 by that decision, its D-13 condition met; #176's named seam was there, so this was an addition rather than the redesign the entry criterion was guarding against. A window is opened in the caller's own name and read by `workflow::service::assignment::redirect` |
| Users assignable to a department | FR-IDM-008 | Should | Sprint 7 — re-scoped by **D-8**, and delivered with FR-ORG-002 rather than beside it, so it follows FR-ORG-002's target (**D-13**) |
| Tenant-scoped queries on every identity read | FR-IDM-009 | Should | Sprint 3–4 for the query scoping; completed with FR-ORG-001 by **D-18**, which supersedes **D-7** |

FR-IDM-009 is mostly structural: `tenant_id` is in the BASE column set ([Database Schema](../../docs/design/02.%20Database%20Schema.md) §1), so isolation is enforced by query scoping from the first identity endpoint onward. **D-7** stopped there and the mode stayed off; **D-18** finishes it. A tenant is named at sign-in, resolved once, and carried in the token claim every downstream query filters by; `GET /deployment` tells the client which mode it is talking to; and the two joins that had been reading across the boundary — `roles_of_user` and `permissions_for_user` — are scoped like their siblings ([#65](https://github.com/sujanto-gaws/kelir/issues/65)).

### ORG — Organization (SRS §4.3)

| Item | FRs | Priority | Target |
|---|---|---|---|
| Tenant management, with per-request tenancy | FR-ORG-001, FR-IDM-009 | Should | Sprint 8 — returned from §7 by **D-18**, its D-13 condition met. One item with FR-IDM-009 and with [#65](https://github.com/sujanto-gaws/kelir/issues/65): a surface that creates tenants nobody can sign in to is not tenant management |
| Department management (the sole department surface, per **D-8**) | FR-ORG-002 | Should | Sprint 7 — carried twice, then scheduled (**D-13**). Master data already reads `departments` to validate a party role's department id, so a consumer depends on rows nothing can create |
| Positions, workgroups, organizational hierarchy | FR-ORG-003, 004, 005 | Could | Unscheduled |

### MDM — Master Data (SRS §4.4)

| Item | FRs | Priority | Target |
|---|---|---|---|
| `0008_master_data.sql` + Party CRUD (persons, party groups) | FR-MDM-001 | Must | Sprint 5 |
| Party roles with role-specific profiles (supplier, customer, employee, contact) | FR-MDM-002 | Must | Sprint 5 |
| Identifications, relationships, statuses, contact mechanisms | FR-MDM-003 | Must | Sprint 5 |
| Role-filtered endpoints (`/master-data/suppliers`, `/customers`, `/employees`) | FR-MDM-002, 008 | Must | Sprint 6 — moved at Sprint 5 planning to land with the list UI that consumes them; first to pull forward if Sprint 5 runs ahead |
| Facility master data | FR-MDM-004 | Must | Sprint 6 |
| Product and service master data | FR-MDM-005, 006 | Should | Sprint 7 — moved at Sprint 6 planning. `Should`, tables already created by `0008`, and no consumer until reporting. First to pull forward if Sprint 6 runs ahead |
| Lifecycle status (active/inactive) across master-data entities | FR-MDM-007 | Must | Sprint 6 |
| List UI with search, filter, pagination | FR-MDM-008 | Must | Sprint 6 |
| Master-data change audit records | FR-MDM-009 | Must | Sprint 6 |
| External source references (`master_data_source_references`) | FR-MDM-011 | Should | Sprint 7 — moved at Sprint 6 planning. The table exists; its foreign key to `external_systems` arrives with the integration migration in Phase 9, so building the surface before that link exists would mean building it twice |
| Master-data changes routed through document workflow | FR-MDM-010 | Should | **Sprint 13** — cut from Sprint 11 and re-targeted by **D-43**. It needed the whole workflow phase to exist first, and after Sprint 11 it does; the tail of Phase 6 is where the sprint plan already said it belonged |

Model authority: [architectures/05. Core - Master Data - Party.md](../../docs/architectures/05.%20Core%20-%20Master%20Data%20-%20Party.md) and Database Schema §4.

### RAD — Metadata and Dynamic Rendering (SRS §4.5)

| Item | FRs | Priority | Target |
|---|---|---|---|
| The RAD metadata migration — entity, form, list, field, menu metadata tables | FR-RAD-001, 002, 003, 005 | Must | Sprint 7 — **blocks the document migration** |
| Form/list definition storage APIs (no builder UI) | FR-RAD-002, 003 | Must | Sprint 7 |
| Lookup fields bound to master data | FR-RAD-007 | Must | Sprint 8 — the binding lives in JFSS `settings` rather than on the component, because the specification is frozen (**D-23**) |
| JFSS form renderer — read and render a published form definition | FR-RAD-010 | Must | Sprint 8 |
| The rendered form evaluates its own rules — validation and calculation in the browser | FR-RAD-010, FR-RAD-006 | Must | Sprint 8 — the *evaluation*, not the engine around it; see the note below |
| Submit, and the server re-evaluation that makes the client's arithmetic untrusted (JFSS S8.1, S10.2) | FR-RAD-010, FR-RAD-006 | Must | Sprint 8 — the Tamper-Proof Pattern; **the security-critical half of the renderer** |
| Document types created through configuration | FR-RAD-008 | Must | Sprint 8 |
| Workflow assigned to document type | FR-RAD-009 | Must | **Sprint 10 — built** ([#187](https://github.com/sujanto-gaws/kelir/issues/187)); moved from Sprint 11 at Sprint 11 planning. Sprint 10's exit says a document type carries a workflow, and #165 configures a type's form binding and numbering rule but not its workflow; a `Must` cannot sit after the sprint whose exit depends on it |
| JSON Logic evaluator adopted on both sides (`datalogic-rs`, **D-10**) — pinned to one version, `sum` registered as a custom operator, the spike corpus promoted to a CI parity gate | FR-RAD-006 | Must | Sprint 7 — **Sprint 8's renderer depends on it**, per the note above |
| Validation and calculation rule engines around that evaluator — rule catalogue, dependency graph, cycle detection, error mapping (Tamper-Proof Pattern) | FR-RAD-006 | Must | Sprints 14–16 |
| Dynamic list renderer | FR-RAD-003, 010 | Must | Sprints 14–16 |
| Conditional field visibility | FR-RAD-011 | Should | Sprints 14–16 |
| Document type builder and menu builder UI | FR-RAD-004, 008 | Should | Sprints 14–16 |
| Metadata versioning | FR-RAD-012 | Could | Unscheduled |

Specification authority: [JSON Form Schema](../../docs/schema/JSON%20Form%20Schema.md) 2.0.1 with the [validation](../../docs/schema/JFSS%20Validation%20Rule%20Registry.md) and [calculation](../../docs/schema/JFSS%20Calculation%20Rule%20Registry.md) registries.

> **The evaluator was scheduled later than the renderer needs it, and that is now fixed.** FR-RAD-010 lands in Sprint 8 and includes *submit*; JFSS S8.1 requires the backend to re-evaluate every `calculate` expression and overwrite the submitted value before persistence, and S10.2 requires the same for every `conditional`. A working evaluator is therefore a **Sprint 8 dependency, not a Sprint 14 one**. **D-10** (2026-08-24) settles it: `datalogic-rs` is adopted on both sides in **Sprint 7**, as its own item, so Sprint 8 builds the renderer against an evaluator that is already there. What stays in Sprints 14–16 is the surrounding engine — rule catalogue, dependency graph, cycle detection, error mapping — rather than the evaluation itself, which is what the two rows below now say. Surfaced by the [operator-parity spike](../spikes/01.%20JFSS%20Operator%20Parity.md), whose remaining findings are **D-15**.

### DTYPE — Document Types (SRS §4.6)

| Item | FRs | Priority | Target |
|---|---|---|---|
| The document migration + document type definitions | FR-DTYPE-001 | Must | Sprint 7 |
| Form and workflow bindings on the document type | FR-DTYPE-002, 003 | Must | Sprint 7 |
| Numbering rules with scoped sequences | FR-DTYPE-004 | Must | Sprint 7 |
| Attachment rules and metadata rules | FR-DTYPE-005, 006 | Should | Sprint 9 tail — **D-16**, and the first of the cut order |
| Document security level | FR-DTYPE-008 | Should | Sprint 9 tail — **D-16**, and among the first cut |
| Retention policy reference stored on the type | FR-DTYPE-007 | Could | Sprint 9 tail (storage only) — **D-16**, last of the cut order; purge behaviour unscheduled |

`document_types.form_id` references `rad_forms` (Database Schema §6.2); `document_type_workflows.workflow_definition_id` is added by the workflow migration. Aggregate shape per the [Document Type Definition Schema](../../docs/schema/Document%20Type%20Definition%20Schema.md).

### DOC — Document Management (SRS §4.7)

| Item | FRs | Priority | Target |
|---|---|---|---|
| Create document from type, store form data and metadata, edit the draft | FR-DOC-001, 002, 005, 006 | Must | Sprint 9 — moved by **D-16**; needs the document type of Sprint 7 to exist. Create and edit are one item ([#167](https://github.com/sujanto-gaws/kelir/issues/167)): a draft that can be created and not corrected is not a draft |
| Submit with number assignment inside the submit transaction | FR-DOC-003, 004 | Must | Sprint 9 — **D-16**; the numbering rule it consumes lands in Sprint 7 as #158 |
| Document status field and transitions owned by the document module | FR-DOC-007 | Must | Sprint 9 — **D-16** |
| Link document to master-data entity | FR-DOC-011 | Must | Sprint 9 — **D-16**; it links a document, and documents land in Sprint 9 |
| Document list with search and filter | FR-DOC-013 | Must | Sprint 9 — **D-16** |
| Document detail workspace (tabs shell) | FR-DOC-014 | Must | Sprint 9 — **D-16** |
| Version history | FR-DOC-008 | Should | Sprint 9 tail — **D-16**, and among the first cut |
| Cancel before completion; archive after completion | FR-DOC-009, 010 | Should | Sprint 9 tail — **D-16**, and among the first cut |
| Link document to workflow process instance | FR-DOC-012 | Must | **Sprint 10 — built** ([#178](https://github.com/sujanto-gaws/kelir/issues/178)) |

### WF — Workflow Engine (SRS §4.8)

| Item | FRs | Priority | Target |
|---|---|---|---|
| The workflow migration + workflow definitions, states, transitions | FR-WF-001, 002, 003 | Must | **Sprint 10 — built** ([#174](https://github.com/sujanto-gaws/kelir/issues/174)) |
| Process instances with workflow variables | FR-WF-014 | Must | **Sprint 10 — built** ([#175](https://github.com/sujanto-gaws/kelir/issues/175)) |
| User tasks generated on transition | FR-WF-004 | Must | **Sprint 10 — built** ([#176](https://github.com/sujanto-gaws/kelir/issues/176)) |
| Approve and reject actions | FR-WF-006, 007 | Must | **Sprint 10 — built** ([#177](https://github.com/sujanto-gaws/kelir/issues/177)) |
| Document status synchronized from workflow transitions | FR-WF-013 | Must | **Sprint 10 — built** ([#178](https://github.com/sujanto-gaws/kelir/issues/178)), and **D-36** is what it cost the manual route |
| Workflow history record per transition | FR-WF-012 | Must | **Sprint 11 — built** ([#181](https://github.com/sujanto-gaws/kelir/issues/181)) |
| Return action | FR-WF-008 | Should | **Sprint 11 — built** ([#183](https://github.com/sujanto-gaws/kelir/issues/183)). The return target is the definition's `to`, not an inferred step back (**D-42**); the resubmission comes back through `POST /documents/{id}/submission` keeping its number |
| Delegate action | FR-WF-009 | Should | **Sprint 11 — built** ([#184](https://github.com/sujanto-gaws/kelir/issues/184)) as `POST /workflow/tasks/{id}/delegation`, beside FR-IDM-006 and FR-TASK-008. **It fires no transition**: handing a task on changes who holds it and does not move the process, so `DELEGATE` stays in JWSS §4's vocabulary driven by nothing |
| Conditional routing | FR-WF-015 | Should | **Sprint 11 — built** ([#186](https://github.com/sujanto-gaws/kelir/issues/186)). Cheaper than it looked and for the stated reason: **D-10**'s evaluator (#154) and the save-time operator bound (#174) were already there, so AC1 and AC2 are regression assertions. What the item changed is that **an evaluation failure now stops the transition** instead of reading as `false` and taking the fallback — recorded as JWSS **R-4** §6.4, and a reversal of Kelir's own earlier answer — that a state whose conditions were all false with no fallback says so, and that the evaluation is recorded in the history (`workflow_history.routing_json`) so *why this branch* is answerable |
| Task due dates | FR-WF-011 | Should | **Sprint 11 — built** ([#185](https://github.com/sujanto-gaws/kelir/issues/185)), with FR-TASK-007 as one item: a due date nobody is shown is a column. JWSS §3.1's `dueInHours` was already declared, validated and parsed; what arrived is the writer. Stamped by the database at generation, so the deadline and every later overdue comparison share one clock |
| System tasks | FR-WF-005 | Should | **Sprint 14** — cut from Sprint 11 as its declared first stretch, re-targeted by **D-43** to the head of Phase 7. It is the driver an `AUTO` transition has waited for since Sprint 10, and `workflow::service::engine` names the paragraph the invocation goes in |
| Escalation rules | FR-WF-010 | Could | Unscheduled |
| Parallel approval | FR-WF-016 | Could | Unscheduled — SRS §4.8 defers it explicitly |

Definition shape per the [JSON Workflow Schema](../../docs/schema/JSON%20Workflow%20Schema.md). Phase 5 scope is sequential approval only — and since Sprint 10 that is a **partial unique index** rather than an intention (`uq_workflow_tasks_open_per_instance`, Database Schema §7.6), so FR-WF-016 has something to argue with when it is scheduled.

**Two things the engine stores and does not run**, both stated where a reader will find them rather than left to be discovered: `guards` and `actions` are validated for shape and never invoked, because there is no hook chain to merge them into (architectures/01 §12.4.2); and `AUTO` transitions are accepted and never fired, because nothing drives one until FR-WF-005. **D-37** records the third narrowing — the two `assigneeType` values Kelir refuses at save.

### TASK — Task Inbox (SRS §4.9)

| Item | FRs | Priority | Target |
|---|---|---|---|
| Inbox listing tasks assigned to the user and to the user's roles | FR-TASK-001, 002 | Must | **Sprint 10 — built** ([#179](https://github.com/sujanto-gaws/kelir/issues/179)) |
| Task detail view | FR-TASK-003 | Must | **Sprint 10 — built** ([#179](https://github.com/sujanto-gaws/kelir/issues/179)) |
| Approve and reject from the task (surfaces FR-WF-006/007) | FR-TASK-004, 005 | Must | **Sprint 11 — built** ([#182](https://github.com/sujanto-gaws/kelir/issues/182)) |
| Comment captured with the approval decision | FR-TASK-006 | Must | **Sprint 11 — built** ([#182](https://github.com/sujanto-gaws/kelir/issues/182)). Written to `workflow_tasks.comment`, `approval_decisions.comment` and `workflow_history.comment` in one transaction (Database Schema §7.6, §7.8, §7.11), so it did not wait for the comment module in Phase 6. Whether a reason is *required* is the definition's, per transition — JWSS §4.1's `requiresComment` (**D-41**) |
| Overdue task indicator | FR-TASK-007 | Should | **Sprint 11 — built** ([#185](https://github.com/sujanto-gaws/kelir/issues/185)), beside FR-WF-011. `isOverdue` is the server's answer, computed in the statement that reads the row; the inbox narrows to it through `scope=overdue`. **Nothing acts on lateness** — FR-WF-010 (escalation) is unscheduled and FR-NTF-006/007 depend on it |
| Task delegation | FR-TASK-008 | Should | **Sprint 11 — built** ([#184](https://github.com/sujanto-gaws/kelir/issues/184)). The task screen's hand-over, and the complement to the window: a task already assigned when a window opens does not move, so it is handed over one at a time by the person holding it |
| Completed task view | FR-TASK-009 | Should | **Sprint 13** — cut from Sprint 11, re-targeted by **D-43** beside FR-SRH-003: both refine the inbox [#179](https://github.com/sujanto-gaws/kelir/issues/179) built, and splitting them would build the query twice |
| Task search | FR-SRH-003 | Should | **Sprint 13** — cut from Sprint 11, re-targeted by **D-43** beside FR-TASK-009 |

### ATT — Attachments (SRS §4.10)

| Item | FRs | Priority | Target |
|---|---|---|---|
| The attachment migration + upload to object storage with metadata | FR-ATT-001, 003 | Must | Sprint 12 |
| Download with permission check | FR-ATT-002 | Must | Sprint 12 |
| File size and file type validation | FR-ATT-004, 005 | Must | Sprint 12 |
| Async virus-scan gating before an attachment becomes downloadable | FR-ATT-001, 002 | Must | Sprint 12 — ClamAV compose service (**D-4**); upload sets `virus_scan_status = PENDING`, the scan worker updates it via the outbox, download blocked until `CLEAN` |
| Attachment activity events | FR-ATT-008 | Must | Sprint 12 |
| Attachment categories | FR-ATT-006 | Should | Sprint 13 |
| Soft-delete | FR-ATT-009 | Should | Sprint 13 |
| External document references | FR-ATT-010 | Should | Sprint 13 |
| Attachment versioning | FR-ATT-007 | Could | Sprint 13 (stretch) — tables exist in `0011` |

FR-ATT-004's 25 MB default was baselined on 2026-08-11 (**D-5**); it stays configurable, and the limit is enforced server-side before the file reaches storage. The scanner is a ClamAV service in the compose stack (**D-4**) — it joins `deploy/docker/docker-compose.yml` in Sprint 12, when something consumes it.

### CMT — Comments (SRS §4.11)

| Item | FRs | Priority | Target |
|---|---|---|---|
| The comment migration + add comment to document | FR-CMT-001 | Must | Sprint 12 |
| Comment activity events | FR-CMT-007 | Must | Sprint 12 |
| Threaded replies | FR-CMT-002 | Should | Sprint 13 |
| Edit and delete when permitted | FR-CMT-003, 004 | Should | Sprint 13 |
| Resolve status and user mentions | FR-CMT-005, 006 | Could | Sprint 13 (stretch) — mention tables exist in `0012` |

### ACT / AUD — Activity and Audit (SRS §4.12)

| Item | FRs | Priority | Target |
|---|---|---|---|
| The activity and audit migration + activity events for document lifecycle and workflow actions | FR-ACT-001, 004 | Must | Sprint 12 |
| Activity events for attachments and comments | FR-ACT-002, 003 | Must | Sprint 12 |
| Activity timeline per document | FR-ACT-005 | Must | Sprint 13 |
| Formal audit events with actor, action, object | FR-AUD-001 | Must | **Sprint 3 — built**, corrected by **D-44**. `audit_events` is `0003_audit.sql`; `modules::audit` has written actor, action, object type and object id since the identity module's first write |
| Append-only enforcement (hash chain, no update/delete path) | FR-AUD-003 | Must | **Sprint 3 — built**, corrected by **D-44**; `tests/audit_hash_chain.rs` holds it. `0019` made the hash cover the payload and `0022` made it tell an absent value from an empty one — both defects in what was hashed, not in whether it was |
| Audit search for authorized users | FR-AUD-004 | Must | Sprint 13 |
| Old/new value snapshots | FR-AUD-002 | Should | **Sprint 6 — built**, corrected by **D-44**. Both halves are read off the row rather than off the request ([#135](https://github.com/sujanto-gaws/kelir/issues/135)), and **D-12** narrowed who may read them back |
| IP address and actor capture | FR-AUD-005 | Should | **Sprint 12** — moved by **D-44**, and it is a *gap* rather than unbuilt work. The column exists, `middleware::client_address` resolves the address and the rate limiter uses it; all 53 audit call sites pass `None`. An audit column that is always null reads as evidence the address was unavailable |

The audit write path is called from Phase 2 onward (FR-AUTH-008, FR-MDM-009); Phase 6 adds tamper evidence, search, and snapshots.

### NTF — Notifications (SRS §4.13)

| Item | FRs | Priority | Target |
|---|---|---|---|
| The notification migration + in-app notification centre | FR-NTF-001 | Must | Sprint 13 |
| Notify on task assignment | FR-NTF-002 | Must | Sprint 13 |
| Notify on approval/rejection | FR-NTF-003 | Must | Sprint 13 |
| Email channel | FR-NTF-004 | Should | Sprint 13 |
| Notification templates | FR-NTF-005 | Should | Sprint 13 |
| Due-task reminders and escalation notifications | FR-NTF-006, 007 | Could | Unscheduled — depends on FR-WF-010 |

### RPT — Reporting and Dashboard (SRS §4.14)

| Item | FRs | Priority | Target |
|---|---|---|---|
| Dashboard summary shell | FR-RPT-001 | Must | Sprint 17 |
| Pending tasks widget | FR-RPT-002 | Must | Sprint 17 |
| Recent documents widget | FR-RPT-003 | Must | Sprint 17 |
| Document status summary chart | FR-RPT-004 | Should | Sprint 17 |
| Overdue tasks widget | FR-RPT-005 | Should | Sprint 17 |
| Approval time report | FR-RPT-006 | Could | Sprint 17 |
| Workload by department report; CSV/Excel export | FR-RPT-007, 008 | Could | Unscheduled |

### SRH — Search (SRS §4.15)

| Item | FRs | Priority | Target |
|---|---|---|---|
| Master data search (delivered by FR-MDM-008) | FR-SRH-002 | Must | Sprint 6 |
| Document search (delivered by FR-DOC-013) | FR-SRH-001 | Must | Sprint 9 — follows FR-DOC-013, which **D-16** moved |
| Task search | FR-SRH-003 | Should | **Sprint 13** — cut from Sprint 11, re-targeted by **D-43** beside FR-TASK-009 |
| Global cross-entity search | FR-SRH-004 | Could | Unscheduled |

### INT — Integration (SRS §4.16)

| Item | FRs | Priority | Target |
|---|---|---|---|
| The integration migration + integration core and external system registry | FR-INT-001 | Should | Sprint 18 |
| Outbound REST connector | FR-INT-002 | Should | Sprint 18 |
| Inbound REST endpoints | FR-INT-003 | Should | Sprint 18 |
| Integration logs | FR-INT-006 | Should | Sprint 18 |
| Outbox dispatcher emitting the event envelope | FR-INT-005 | Could | Sprint 19 |
| External document reference resolution | FR-INT-010 | Should | Sprint 19 |
| Webhook receiver | FR-INT-004 | Could | Sprint 19 (stretch) |
| Retry with backoff for failed deliveries | FR-INT-007 | Could | Unscheduled |
| Master data synchronization; file exchange | FR-INT-008, 009 | Could | Unscheduled |

Envelope shape per the [Event Envelope Schema](../../docs/schema/Event%20Envelope%20Schema.md); layer design in [architectures/03](../../docs/architectures/03.%20Kelir%20Modules%20for%20Interfacing%20with%20External%20Systems.md).

### PLG — Plugins and Extensions (SRS §4.17)

| Item | FRs | Priority | Target |
|---|---|---|---|
| The plugin migration + plugin registry and manifest ingestion | FR-PLG-001, 002 | Could | Sprint 19 |
| Enable/disable lifecycle | FR-PLG-003 | Could | Sprint 19 |
| Plugin settings and permissions | FR-PLG-004, 005 | Could | Sprint 19 |
| Plugin install/config auditing | FR-PLG-008 | Could | Sprint 19 |
| UI and backend extension points | FR-PLG-006, 007 | Could | Sprint 19 (stretch) |

Scope is config-based plugins only ([architectures/04](../../docs/architectures/04.%20Kelir%20Plugin%20and%20Extension%20Management%20Concept.md) Phase 1); manifest per the [Plugin Manifest Schema](../../docs/schema/Plugin%20Manifest%20Schema.md), hooks per the [Lifecycle Hook Contract](../../docs/schema/Lifecycle%20Hook%20Contract.md).

### API — API Surface (SRS §4.18)

| Item | FRs | Priority | Target |
|---|---|---|---|
| REST/JSON under `/api/v1` with the standard response envelope and `AppError` | FR-API-001, 002, 003, 005 | Must | Sprint 1 |
| Pagination on every list endpoint | FR-API-006 | Must | Sprint 1 |
| Authentication and authorization on every route | FR-API-008 | Must | Sprint 3 |
| Sorting and filtering conventions | FR-API-007 | Should | Sprint 5 |
| OpenAPI document published and kept current | FR-API-004 | Must | Sprint 1, maintained every sprint |

FR-API-004 was raised from `Should` to `Must` in SRS v0.5 (**D-3**) because MVP criterion 14 requires documented APIs. It is not a one-sprint item: the OpenAPI document is regenerated or updated in every sprint that changes the API surface, and that is part of the Definition of Done.

### NFR — Non-Functional Requirements (SRS §5)

Non-functional requirements are constraints on every item above, not items of their own; they are verified continuously and audited in the hardening sprints.

| Group | IDs | Enforced from | Verified |
|---|---|---|---|
| Performance | NFR-PERF-001..004 | Sprint 1 (pagination, streaming) | Sprint 20 load test |
| Scalability | NFR-SCA-001..004 | Sprint 1 (stateless handlers, external storage) | Sprint 20 |
| Availability | NFR-AVA-001..004 | Sprint 0–1 (compose, health, graceful shutdown) | Sprint 21 backup/restore drill |
| Security | NFR-SEC-001..010 | Sprint 3 (hashing, tokens, authz); Sprint 4 (rate limit) | Sprint 20 security pass |
| Auditability | NFR-AUD-001..004 | Sprint 12 (audit module) | Sprint 21 |
| Maintainability | NFR-MNT-001..005 | Sprint 0 (standards adopted) | Every PR review |
| Usability | NFR-USE-001..005 | Sprint 1 (shell, error surfaces) | Sprint 21 |
| Compatibility | NFR-CMP-001..004 | Sprint 1 (PostgreSQL, modern browsers) | Sprint 21 |

NFR-PERF-001, NFR-AVA-004 and NFR-SEC-008/009/010 were baselined on 2026-08-11 (**D-5**) and are now firm verification targets: P95 1 s list / 2 s detail at 10,000 documents and 50 concurrent users; 99.5% monthly uptime with RPO ≤ 24 h, RTO ≤ 4 h and daily automated backups; 5 failed logins → 15-minute lockout; encryption at rest for attachments and backups; TLS 1.2+ in transit.

Two of these need work scheduled outside the hardening sprints: NFR-SEC-008 is implemented in Sprint 4 — the account lockout half landed in Sprint 3, the rate limit in Sprint 4, and neither yet satisfies the baselined "5 failed logins → 15-minute lockout" (issue #55, open decision) — and NFR-AVA-004's daily backups plus the restore drill that proves RPO/RTO are Sprint 21 items that need a staging environment holding realistic data volume.

---

## 5. MVP Coverage Check (SRS §9)

Each MVP acceptance criterion mapped to the items that satisfy it and the sprint they land in. The MVP is tagged `v0.6.0` at the end of Phase 6 (decision **D-1**); "Covered" below means covered *at that tag*.

| # | SRS §9 criterion | Satisfied by | Lands | Status |
|---|---|---|---|---|
| 1 | Users can login and logout | FR-AUTH-001, 003, 004 | Sprint 3 | Covered |
| 2 | Administrators can manage users and roles | FR-IDM-001..005, 007 | Sprints 3–4 | Covered |
| 3 | Administrators can configure master data | FR-MDM-001..004, 007, 008 | Sprints 5–6 | Covered |
| 4 | Administrators can configure document types | FR-DTYPE-001..004, FR-RAD-008 | Sprints 7–8 | Covered — RAD storage moved into Phase 4 (**D-2**) |
| 5 | Users can create documents from a configured type | FR-DOC-001, 004, 005, FR-RAD-010 | Sprints 8–9 | Covered — read-only renderer moved into Phase 4 (**D-2**); the document half moved to Sprint 9 with **D-16**, still inside the phase |
| 6 | Users can upload attachments | FR-ATT-001..005 | Sprints 12–13 | Covered at the tag |
| 7 | Users can submit documents | FR-DOC-003 | Sprint 7 | Covered |
| 8 | Workflow engine creates tasks | FR-WF-004, FR-TASK-001..003 | Sprint 10 | Covered |
| 9 | Approvers can approve or reject tasks | FR-WF-006, 007, FR-TASK-004, 005 | Sprints 10–11 | Covered |
| 10 | Document status changes automatically | FR-WF-013, FR-DOC-007 | Sprint 10 | Covered |
| 11 | Comments can be added | FR-CMT-001, FR-TASK-006 | Sprints 12–13 | **FR-TASK-006 is covered** ([#182](https://github.com/sujanto-gaws/kelir/issues/182)) — a decision carries its reason. FR-CMT-001, the document conversation, is covered at the tag; the two are different things and §7.8.1 says which is which |
| 12 | Activity log records actions | FR-ACT-001..005 | Sprints 12–13 | Covered at the tag |
| 13 | Audit log records approvals and status changes | FR-AUD-001, 003, FR-WF-012 | Sprints 3–11 | **Covered.** FR-AUD-001 and 003 shipped in Sprint 3 (**D-44** corrects the row that said Sprint 12); FR-WF-012 shipped in Sprint 11 ([#181](https://github.com/sujanto-gaws/kelir/issues/181)) and is the *workflow's* history rather than the audit trail, which is the distinction that issue existed to draw |
| 14 | REST APIs available and documented | FR-API-001..006, 008 | Sprint 1 onward | Covered — FR-API-004 must not be cut |
| 15 | Application runs using Docker Compose | NFR-AVA-001 | Sprint 0 | Covered |

**Result: all 15 criteria are met at `v0.6.0`**, the end of Phase 6. This holds because of two decisions taken on 2026-08-11: **D-1** moved the MVP tag from `v0.5.0` (where criteria 6, 11, 12 and 13 were still unbuilt) to `v0.6.0`, and **D-2** moved RAD's storage layer and form renderer into Phase 4 (where criteria 4 and 5 need them). At `v0.5.0` the count would have been 10 of 15.

Four `Must`-priority FRs still land after the MVP tag — FR-RAD-006 (rule engines, Phase 7) and FR-RPT-001, 002, 003 (dashboard, Phase 8) — as does the dynamic-list half of FR-RAD-003/010. That is legitimate, not a gap: SRS v0.5 defines `Must` as "required for the 1.0 release" and names §9 as the sole MVP gate (decision **D-3**), so a `Must` requirement outside §9 is post-MVP by design. The converse no longer occurs — FR-API-004 was raised to `Must` so that nothing §9 depends on can be cut as a `Should`.

---

## 6. Decisions

### 6.1 Resolved

Resolved 2026-08-11 unless the row says otherwise.

| ID | Decision | Resolution |
|---|---|---|
| **D-1** | The MVP milestone was tagged at the end of Phase 5 (`v0.5.0`), but SRS §9 criteria 6, 11, 12 and 13 are Phase 6 work. Either the tag moved or the scope did. | **The MVP tag moves to the end of Phase 6 (`v0.6.0`).** Pulling upload/download, flat comments, the activity timeline and the audit write path into Sprints 10–11 would have loaded the workflow engine's critical path with a second module; moving the tag costs calendar and nothing else. `v0.5.0` remains a real release — an approval-workflow milestone, not the MVP. Applied to sprint plan §3/§4/§6 and SDD §14. |
| **D-2** | RAD was Phase 7, but `document_types.form_id` references `rad_forms`, so the RAD migration must apply before the document migration in Phase 4 — and MVP criteria 4 and 5 need a form renderer, not just tables. (The two files were numbered `0004_rad.sql` and `0005_document.sql` when this decision was taken on 2026-08-11; renumbered 2026-08-13 when Sprint 4 consumed versions `0005` and `0006`, again on 2026-08-20 when the NFR-SEC-008 lockout expiry took `0007`, and four more times during Sprint 6 — which is the argument for naming them by content here and letting the [Database Schema](../../docs/design/02.%20Database%20Schema.md) mapping table hold the numbers. The ordering constraint the decision rests on is unchanged, and is the only part of D-2 that a renumbering can never disturb.) | **RAD splits across two phases.** Phase 4 takes the metadata tables, form/list definition storage APIs, lookup fields, and a read-only JFSS renderer (FR-RAD-001, 002, 003, 005, 007, 008, 010). Phase 7 keeps the rule engines, dynamic list renderer, conditional visibility and builder UIs. Applied to sprint plan §3/§6; §4 of this document was already written against the split. Consequence: Phase 4 is now the widest phase, and the JFSS operator-parity spike moves from Sprints 12–13 to Sprints 5–6. |
| **D-3** | FR-API-004 (OpenAPI) was `Should` but is required by MVP criterion 14. More broadly, four `Must` FRs (FR-RAD-006, FR-RPT-001..003) are absent from SRS §9, while §4 defined `Must` as "required for MVP". | **Priority and MVP scope are now separate concepts.** SRS v0.5 redefines `Must` as "required for the 1.0 release" and states that §9 is the sole MVP gate; FR-API-004 is raised to `Must`. This removes both mismatches rather than papering over one — a `Must` item outside §9 is legitimately post-MVP, and nothing named by §9 can be cut as a `Should`. Applied to SRS §4 preamble and §4.18. |
| **D-4** | Attachment virus scanning (Phase 6) had no chosen scanner. | **ClamAV as a service in the compose stack.** Matches the self-hosted stack already in `deploy/docker/docker-compose.yml`, adds no external dependency or per-file cost, and keeps file content on-site. The container joins the compose file in Sprint 12, not now — a scanner with no consumer would just slow every local stack. Scan is asynchronous: upload sets `virus_scan_status = PENDING`, the worker updates it via the outbox, download is blocked until `CLEAN` (Database Schema §8.1). |
| **D-5** | Six values carried "proposed target — confirm before baseline": FR-ATT-004 (25 MB), NFR-PERF-001, NFR-AVA-004, NFR-SEC-008, NFR-SEC-009, NFR-SEC-010. | **All six confirmed as written and baselined in SRS v0.5**; the markers are removed and the values are now verification targets. See the note in §4's NFR table for what this obliges and when. |
| **D-6** | FR-IDM-004 read "the system shall manage permissions", which was taken to promise administrator CRUD over permission rows. Nothing in the stack implements it — the catalogue is migration seed data with a read endpoint — and it was recorded delivered twice in Sprint 3, by #21 and again by #22, without anyone opening the backend to look for a write path (#69). | **(2026-08-20) Amend the requirement; build nothing.** FR-IDM-004 now reads "maintain the permission catalogue that authorization checks resolve against" ([SRS](../../docs/requirements/srs.md) v0.6 §4.2). A permission is an identifier the code checks, so the meaningful set is fixed by the code: a row an administrator invents is inert, and a check whose row an administrator deletes becomes ungrantable — runtime CRUD adds two failure modes and no capability. The catalogue is system-defined, seeded by migration and extended at plugin-installation time from the manifest (FR-PLG-005, Database Schema §13.4); the administrative surface is role–permission mapping, which is FR-IDM-005 and already ships with the #61 screens. **Rejected:** implementing permission CRUD as written, which would have needed an answer for what an unchecked permission row means and how a code check survives its row being deleted. Consequence: FR-IDM-004 is met by what exists — the `permissions` table, `GET /api/v1/identity/permissions`, and the role-permission editor — and #23 closes on all five of its FRs rather than four. |
| **D-7** (superseded by **D-18**, 2026-08-25) | FR-IDM-009 obliges tenant isolation "if multi-tenant mode is enabled", and nothing resolves a tenant per request — so the condition has no state in which it is true. Identity queries are tenant-scoped (94 references, every one from the caller's own claims), but turning `KELIR_MULTI_TENANT` on makes sign-in impossible from the UI (#67) and the bootstrap grants a role across the boundary (#65). Master data is the first module where a second tenant would show. | **(2026-08-20) One tenant per deployment; the mode is not exercised before 1.0.** The requirement stands and its structural half is delivered — `tenant_id` is in the BASE column set and every identity read is scoped by it, which is the half that would be expensive to retrofit. What is deferred is the half that has no consumer: per-request resolution, its transport (subdomain, header, or login field), and the sub-decision #67 raises about how the frontend learns the deployment's mode. #67 becomes a **boot guard** instead of a login field — the backend refuses to start with the mode on, rather than serving a sign-in page nobody can complete — and #65 stays open, correctly labelled `Should`, non-urgent while the default tenant holds. **Rejected:** building resolution in Sprint 5, which would spend roughly two items on a mode no deployment runs and would push Phase 3 for it. Applied to [SRS](../../docs/requirements/srs.md) v0.7 §4.2 and §10. MVP scope unchanged — §9 names no tenant criterion. **Superseded on 2026-08-25 by D-18**, which took the deferred half rather than extending it: the mode runs, the guard is gone, and #27, #65 and #67 all close against it. Read this row as history — nothing in it still describes the system. |
| **D-8** | FR-IDM-008 ("department and position management") and FR-ORG-002 ("manage departments") point at the same `departments` table, and issues #25 and #28 both claim it. FR-ORG-003 separately owns positions, which have no table. Two `Should` items were carrying one surface. | **(2026-08-20) FR-ORG-002 owns departments; FR-IDM-008 re-scopes to "assigning users to a department".** The organization epic owns the entity, identity owns the edge — `users.department_id` — which is the only part that is genuinely an identity concern. Positions stay with FR-ORG-003, `Could` and unscheduled, and no requirement claims them twice. **Issue #25 closes into #28**; the merged item is the single department surface. **Rejected:** building both as written, which would have shipped two administrative screens over one table and left Phase 3 to guess which one a party's department reference meant. Applied to [SRS](../../docs/requirements/srs.md) v0.7 §4.2/§4.3 and §4 of this document. No FR added or removed; §8 counts unchanged. |
| **D-9** | The staging environment has been the outstanding item since Sprint 2: `kelir-staging-01` does not exist, and no infrastructure is available to host it. It blocked the `v0.1.0` tag, blocked `v0.2.0`, and the Definition of Done clause that names it — "demoable from a staging deploy" — was waived four sprints running, twice with an expiry that was then extended rather than met. A gate nobody can pass does not gate anything; it teaches readers that the Definition of Done is aspirational. | **(2026-08-21) There is no staging environment, and the Definition of Done stops naming one.** Staging is **unscheduled rather than abandoned**: `deploy/staging/` keeps its verified definition — compose file, Caddy single-origin config, provisioning script — so that if a host ever appears the work is a deploy rather than a redesign, and a README there says plainly that nothing runs it. **The clause is replaced, not deleted.** "Demoable from a staging deploy" becomes "demoable from the Docker Compose stack built from release images", and the release checklist's deploy and smoke-pass steps say the same. That keeps the property the staging clause was actually protecting — that what is demonstrated is built the way a release is built, not run from a working tree — while naming an environment that exists and is already required by SRS §9 criterion 15. **Consequence:** #12 closes as not planned, Phase 1 and Phase 2 exit on the replacement check, and the release records for `v0.1.0` and `v0.2.0` are settled against it. **Rejected:** striking staging from the release process as a concept, which would mean rewriting the process to reintroduce a step it used to have; and dropping the clause without a replacement, which would lose the build-provenance property and leave "demoable" meaning a developer's laptop. |

| **D-10** | **Which JSON Logic evaluator does Kelir take?** The [operator-parity spike](../spikes/01.%20JFSS%20Operator%20Parity.md) (#31) found the Calculation Rule Registry's Rust column naming `json-logic-rs`, which is not a crates.io package. The crate it meant, `jsonlogic-rs` 0.5.0, **returns unknown operators instead of rejecting them**, which inverts the Tamper-Proof Pattern: the registry's own §6.1 invoice pattern persists a grand total of 0 in place of 42, with nothing logged and nothing returning 400. It also has no custom-operator API, so `sum` — which §3.2 requires in all three environments — cannot be registered at all. Raised 2026-08-21 as Sprint 5 item 5; Phase 4's entry criterion. | **(2026-08-24) `datalogic-rs` 5.2.0 on the backend, and its official WebAssembly build in place of `json-logic-js` on the frontend.** The spike measured 51 expressions across four evaluators and the two agree on **51/51 including the error cases**, so Polyglot Parity holds *by construction* rather than by a test suite chasing it. Both packages are pinned to the same exact version, and any bump is a parity-affecting change that re-runs the corpus. **Cost, accepted deliberately:** 588 KB gzipped against 4.1 KB for `json-logic-js`. It is lazy-loaded — only form pages need an evaluator — and NFR-PERF-001 baselines view latency rather than bundle weight, so nothing is formally breached; the size is a choice, not a side effect. Licence moves from MIT to Apache-2.0. **Rejected:** keeping `json-logic-js` on the client with `datalogic-rs` on the server, which the spike calls strictly weaker — it makes parity a property tests chase rather than one that holds, leaves the five known divergences in place, and needs a wrapper per divergence. **A re-sequencing follows and is the part most easily missed.** FR-RAD-010 lands in Sprint 8 and includes *submit*; JFSS S8.1 requires the backend to re-evaluate every `calculate` expression and overwrite the submitted value before persistence, and S10.2 requires the same for `conditional`. **A working evaluator is therefore a Sprint 8 dependency, not a Sprint 14 one.** Adoption moves to Sprint 7 as its own item so that Sprint 8 builds the renderer against a settled evaluator; what stays in Sprints 14–16 is the surrounding engine — rule catalogue, dependency graph, cycle detection, error mapping — rather than the evaluation itself. **Still open and not resolved by this:** the `conditional.logic` tier that no registry governs (spike §2.6) and the `regex` params constraint no Rust backend can honour as specified (§2.7). Both are recorded as **D-15**. Applied to §4's RAD rows, the [sprint plan](01.%20Sprint%20Plan.md) §5 Sprint 7 and §6, and the spike corpus, which becomes a CI parity gate with adoption. **Built 2026-08-25 (#154), and two things the build corrected in this record.** First, "51/51 including the error cases" is agreement between the two *adopted* sides, not with `json-logic-js`, against which the engine agrees on 46/51 after the mandated wrapper — the gate in [`parity/`](../../parity/README.md) therefore holds Kelir's two sides to each other, which is what the decision actually bought. Second, the corpus is 55 cases, not 51: flipping the engine's NaN handling left the original set green, because every non-numeric operand in it was `null`, `""` or a bool, all of which the coercion settings handle first. The four added cases put an array, an object and a non-numeric string in front of `+` and `*`. (**60 since #163**, which added the modulo by zero **D-24** names and the corpus had no case for.) The 588 KB is confirmed off the first-load path: the engine builds into its own chunk and a separate `.wasm` asset, and the entry chunk is byte-for-byte the same size with and without an importer. |
| **D-11** | The JFSS family was written for a two-language backend — "Golang / Rust" in the JFSS target stack, a Go column in both Calculation Rule Registry matrices, Go implementations of `sum` and `map`, and `Go/Rust` implementation notes throughout the Validation Rule Registry. Kelir's backend is Rust; no Go service exists, none is planned, and nothing in the repository compiles Go. The [operator-parity spike](../spikes/01.%20JFSS%20Operator%20Parity.md) §2.8 raised it as the mechanism behind a worse problem: three columns of support ticks, none of them checked, and no build that would have failed — which is how the Rust column came to name a crate that does not exist. | **(2026-08-21) The JFSS documents describe one backend, and it is Rust.** Every Go reference is converted or removed across the three affected documents. **Calculation Rule Registry v1.4.0:** Go column dropped from both matrices, the Go `sum` and `map` sections deleted, the Rust `sum` section renumbered 4.3 → **4.1**, and two sections added — **4.2** for the Vue `sum` it has to agree with (previously only implied, which is precisely how two implementations drift) and **4.3** forbidding local reimplementation of standard operators. **Validation Rule Registry v1.3.0:** every `Go/Rust` note converted to Rust and made concrete rather than generic — `unique`/`exists` now state why the allow-list is the only shape that compiles under `sqlx::query!` (§2.5 requires compile-time-verified queries, and `query!` cannot take a runtime table name), `authorized` names the authenticated claims instead of "the JWT/Session context", and an unrecognised rule name must be an error rather than a skipped `match` arm. A **Semantic Parity** requirement is added to §1.1: for a `scope: "both"` rule the two implementations must agree on the edge cases, which is what `regex` had been quietly failing. **JFSS v2.0.1 is a Final Standard, so this lands as errata E-1** (new §13), not a version bump: no component, property, validation keyword or meta-schema construct changes, and `jfss-meta-v2.0.1.json` is untouched. **Polyglot Parity is not weakened.** It was never a claim about the number of languages — one JavaScript runtime and one Rust runtime still meet at a boundary where a schema can silently mean two different things, and every mechanism guarding that boundary is unchanged. **Rejected:** keeping the Go column "in case" a Go service ever appears — an unverifiable claim in an Active Standard is worse than an absent one, and this registry is the proof; and deferring into D-10, which would have held a settled question hostage to an open one. |
| **D-12** | `master-data:audit:read` opened a record's change history on its own — deliberately, and asserted by `the_history_needs_its_own_permission`. But a record's `oldValue` and `newValue` **are** the record's own field values: `Party.Created` carries the party code, its type and its status; `Party.Updated` carries `statusId`, `externalId` and `description`; `Facility.*` carries the name, the type and both references. A caller holding only that permission was refused `GET /parties/{id}` and answered `GET /parties/{id}/audit` with the same values. The same function already applied the opposite rule to the role half of the same list, on the reasoning #97 stated for the role views — "a row is made of both surfaces, and a view needing only one would be a way around the other". Raised as finding F4 of the [Sprint 6 surface verification](../verifications/03.%20Sprint%206%20Surface%20Verification.md) and filed as [#136](https://github.com/sujanto-gaws/kelir/issues/136). | **(2026-08-24) A history requires the record's own read permission alongside `master-data:audit:read`.** `master-data:party:read` on a party route and `master-data:facility:read` on a facility route, checked before anything is read; `master-data:party-role:read` still decides whether the role records are in the page, so the surface now states one rule instead of two. **Rejected:** keeping `audit:read` sufficient and documenting the asymmetry, which was the other coherent answer and is defensible on its own terms — a governance permission, separable from the content it governs. It was rejected because it leaves one function applying two rules for the same reason, and because a tenant granting `audit:read` to a compliance reviewer would not read it as granting master-data content; the payloads are separable today only because they are small, and they grow with every field a future `new_value` records. **Cost:** every caller of a history needs one permission more than before. Nothing but `ROLE-ADMIN` holds `audit:read` by default (`0012_master_data_audit_permission.sql`), so no seeded grant loses access and no deployment is migrated out of a working state. Applied to `service/audit_record.rs`, both `#[utoipa::path]` 403 descriptions, `the_history_needs_its_own_permission` and its new companion `the_history_needs_the_records_own_read_permission_too`, and `0013_master_data_audit_permission_scope.sql`, which rewrites the catalogue row's description to say what the permission grants. |
| **D-13** | The four Phase 2 carry-overs — #17 (FR-AUTH-006), #24 (FR-IDM-006), #27 (FR-ORG-001), #28 (FR-ORG-002) — had been carried twice: planned into Sprint 6 at Sprint 5 planning, moved to Sprint 7's tail at the Sprint 6 close. Twice carried is where a carry-over stops being real, and the [Sprint 6 status report](../status/07.%20Sprint%206%20Status.md) raised the disposition as a blocker for Sprint 7 planning. All four are `Should`; none gates a release. | **(2026-08-24) The four are split rather than dispositioned as a block, because they are not in the same situation.** **Scheduled into Sprint 7's tail:** FR-ORG-002 (#28), because `master_data/repository/role.rs` already runs `SELECT 1 FROM departments` to validate a party role's department id — a consumer depends on rows no surface can create, which is a live gap rather than an absent feature; and FR-AUTH-006 (#17), because `password_reset_tokens` shipped in `0006` and **nothing in the tree reads it** — no source reference, no prepared query, no route, no test — while the migration header and [Database Schema](../../docs/design/02.%20Database%20Schema.md) §3.9 both read as though Sprint 4 built the token flow. FR-IDM-008 follows FR-ORG-002 under **D-8**. **Moved to §7 unscheduled:** FR-IDM-006 (#24), whose `delegations` table has existed since `0002` and whose consumer is the Phase 5 assignment path, so the surface would be designed against an assignment path not yet written; and FR-ORG-001 (#27), which **D-7** has already answered — a deployment is single-tenant and the boot guard refuses multi-tenant mode, so the surface would manage rows nobody can sign in to. Each names the event that returns it rather than a sprint. **Rejected:** scheduling all four into Sprint 7, which would spend two items on surfaces whose dependencies do not exist and would load a sprint that already opens Phase 4; and unscheduling all four, which would leave the `departments` consumer validating against a table nothing can populate. **Also settled:** [#65](https://github.com/sujanto-gaws/kelir/issues/65) is bundled with FR-ORG-001 rather than fixed alone — it asks whether roles are tenant-scoped or global, which is the tenancy model **D-7** deferred, and it is unreachable while the boot guard holds. Applied to §4 (five target cells), §7 (two rows), the [sprint plan](01.%20Sprint%20Plan.md) §5, Database Schema §3.9, and the five issues, each carrying its disposition and reasoning. |
| **D-14** | Does Phase 4 get a browser-driving harness? The [Sprint 6 retrospective](../retrospectives/04.%20Sprint%206%20Retrospective.md) made this a Sprint 7 planning decision because #101, the first frontend feature since Phase 2 and the first ever covered by an independent pass, was **verified by reading only** — no browser driven, no E2E written, no probe aimed at the UI, and no harness in the repository that would have allowed one. | **(2026-08-24) Yes, and it lands in Sprint 7 rather than beside the thing it verifies.** Phase 4 delivers the JFSS form renderer, which is the most logic-heavy UI in the project: a form definition drives rendering, validation and submission, and S8.1 makes the backend re-evaluate what the client computed. Verifying that by reading source is the weakest point in the current process, and the harness has to exist *before* the renderer rather than arrive with it — the same sequencing coding standard §1.5 requires of a file split. **Rejected:** landing it in Sprint 8 beside the renderer, which repeats the mistake #112 was praised for avoiding; and deferring it to Phase 5, which would carry "verified by inspection only" through the phase that most needs the opposite. **Until it exists, a frontend item's status row says `verified by inspection only` in those words**, which the retrospective already requires. Applied to the [sprint plan](01.%20Sprint%20Plan.md) §5 Sprint 7 item 6. |
| **D-16** | Phase 4 was two sprints (7–8) and its `Must` scope does not fit in them. Sprint 7 planning counted it: six items in Sprint 7, six more in Sprint 8 (lookup fields, the JFSS renderer, document types through configuration, link to master data, the document list, the document detail workspace), and the four `Must` document items FR-DOC-001..007 that §4 targeted at Sprint 7 and that no honest reading fits there — **seventeen `Must` items against a two-sprint capacity of twelve to fourteen**, before the five `Should` items the sprint plan §6 already named as the first cut. The [Sprint 6 retrospective](../retrospectives/04.%20Sprint%206%20Retrospective.md) had just observed that verification produces work at twice to three times the item count while the plan still sizes a sprint by items built. | **(2026-08-24) Phase 4 becomes three sprints, 7–9, and `v0.4.0` moves with it.** Every later phase shifts by one: Phase 5 to Sprints 10–11, Phase 6 to 12–13, Phase 7 to 14–16, Phase 8 to 17, Phase 9 to 18–19, hardening to 20–21. The plan is **twenty-one sprints rather than twenty**. The MVP tag is unchanged in what it means — end of Phase 6 per **D-1** — and is now Sprint 13. Sprint 9 takes the four `Must` document items, link to master data, and the document list and detail workspace; it closes the phase. **Rejected:** cutting the five `Should` items alone, which reaches seventeen against twelve to fourteen and is therefore necessary arithmetic but not a sufficient answer — so they are *not* pre-cut, and sit at Sprint 9's tail as the first thing to go; and moving the document list and detail workspace to Phase 5, which would ship a document module whose documents cannot be listed, the trade #101 was offered in Sprint 6 and refused. **This is D-1's trade in the same situation** — a tag moved rather than a scope cut, costing calendar and nothing else — with one difference: it is taken *before* the sprint that would have overrun rather than after it. Applied to the [sprint plan](01.%20Sprint%20Plan.md) §3 map, §5 Sprint 7, §6 outline and §7 risk row; to §4 of this document wherever a target named a sprint from 9 onward; and to [Installation and Deployment](../../docs/operations/01.%20Installation%20and%20Deployment.md), whose recovery drill moves from Sprint 20 to 21. **Dated records are deliberately not renumbered** — the Sprint 0 status report's struck-through note about the spike keeps the sprint number it named on the day, for the reason the migration numbers keep theirs. |
| **D-17** | **D-13** moved FR-IDM-006 (user delegation, [#24](https://github.com/sujanto-gaws/kelir/issues/24)) to §7 unscheduled with a named return condition rather than a sprint: *"Returns at: Phase 5 planning."* The reasoning was that the `delegations` table has existed since `0002_identity.sql`, fully constrained and referenced by nothing, and its own comment names the consumer — "the workflow assignment" — which is Phase 5. Building the surface first would have meant deciding what a delegation *does* against an assignment path not yet written. Sprint 10 planning is that moment. | **(2026-08-24) The condition is met, and FR-IDM-006 returns to §4 targeted at Sprint 11.** Not Sprint 10: the assignment path itself lands there as [#176](https://github.com/sujanto-gaws/kelir/issues/176), and delegation is a modification of an assignment that must already exist. Sprint 11 is also where its two consuming surfaces sit — FR-WF-009 (delegate action) and FR-TASK-008 (task delegation) — and shipping the identity-side window in a different sprint from the workflow-side actions that honour it would leave a table with a writer and no reader for a second time, which is the exact condition D-13 refused to create. **#176 carries the obligation that makes this possible**: its acceptance criteria require assignment to resolve "who is this task for" through a named seam a delegation window can later participate in, rather than writing the assignee straight from the definition. Without that seam, delegation in Sprint 11 would be a redesign rather than an addition. **Rejected:** scheduling it into Sprint 10 beside the assignment path, which would build the window and its consumer in one sprint and give the sprint two things to be wrong about at once; and leaving it unscheduled for a third disposition, which is what D-13 was written to stop — a return condition that is met and not acted on is worse than no condition, because the next reader trusts it. Applied to §4 (FR-IDM-006 returns with a Sprint 11 target), §7 (its row is removed), §8 (scheduled 147 → 148, unscheduled 17 → 16, IDM 8 → 9 scheduled), and the [sprint plan](01.%20Sprint%20Plan.md) §5 Sprint 10. **#27 and #65 stay in §7**, unchanged: their return condition is the decision that supersedes **D-7**, and nothing in Phase 5 touches it. |
| **D-18** | **D-7** deferred per-request tenant resolution past 1.0 and turned [#67](https://github.com/sujanto-gaws/kelir/issues/67) into a boot guard, leaving three faces of one seam open: FR-ORG-001 ([#27](https://github.com/sujanto-gaws/kelir/issues/27)) had no surface because a surface would create tenants nobody can sign in to; [#65](https://github.com/sujanto-gaws/kelir/issues/65) asked whether roles are tenant-scoped or global and could not be answered without the tenancy model; and `KELIR_MULTI_TENANT` refused to start a deployment at all. **D-13** and **D-17** both declined to schedule them, each naming the same return condition: *the decision that supersedes D-7.* This is it, taken at the product owner's direction rather than at a sprint boundary. | **(2026-08-25) Multi-tenant mode runs, and the three faces close together.** **The transport is the sign-in body field, which already existed.** `SignInRequest.tenantCode` shipped in Sprint 4 and is in the published contract; a subdomain needs wildcard DNS and TLS, and a header carries the same trust as a body field while being invisible to every generated client. **What was missing was the client's half**, which is #67's original question: `GET /deployment` answers it — unauthenticated, because the login form needs the answer before it has credentials — and a build-time `VITE_` flag was rejected because the frontend image bakes only a *relative* API base, so one build serves every deployment and a build-time tenancy flag would end that. The login form also reveals its tenant field on a `tenantCode` validation detail, so a deployment that cannot be probed costs one attempt rather than locking anyone out. **#65 is answered: roles are tenant-scoped.** Every tenant has its own `ROLE-ADMIN` — `uq_roles_tenant_id_role_code` is per tenant, so these are different rows — the permission catalogue stays global, and `roles_of_user`, `permissions_for_user` and `role_codes_for_user` gain the `tenant_id` filter their siblings already had. `0017_tenant_administration.sql` makes the old shape unwritable rather than merely unwritten. **The surface answers D-13's objection in the endpoint's shape**: creating a tenant creates its first administrator in the same transaction, so a created tenant is one somebody can sign in to. **The boundary that makes tenant-scoped roles safe** is that tenant administration is performed only from the deployment's default tenant; withholding `organization:tenant:*` from a provisioned tenant's role is defence in depth, not the boundary, because that tenant's administrator holds `identity:role:update` and can grant themselves any code in a global catalogue. **Rejected:** roles global (option 1 of #65), which contradicts `roles.tenant_id` and would make a tenant administrator's new role visible to every tenant; roles scoped with shared system roles (option 3), which needs a rule for what happens when a tenant edits a shared row and buys nothing once provisioning exists; dropping the `KELIR_MULTI_TENANT` flag and always resolving per request, which would let a caller on a single-tenant deployment name somebody else's tenant — the flag-off path ignoring a supplied code is a security property, not a legacy; and a two-call create (tenant, then user), which puts D-13's "rows nobody can sign in to" back within reach of any client that makes the first call and not the second. Applied to [SRS](../../docs/requirements/srs.md) v0.8 (§1.4, §2.2, §4.2, §4.3, §10), [Database Schema](../../docs/design/02.%20Database%20Schema.md) (mapping table, §1.5, §2.1, §3.4, §3.6, §3.7, §14 deviation 19), [naming convention](../../docs/standards/02.%20Naming%20Convention.md) §5, [Installation and Deployment](../../docs/operations/01.%20Installation%20and%20Deployment.md) §7.1, §4 and §7 and §8 of this document, the [sprint plan](01.%20Sprint%20Plan.md) §5, and issues #27, #65 and #67. |
| **D-19** | [#107](https://github.com/sujanto-gaws/kelir/issues/107): `uq_mdm_parties_tenant_id_party_code` was partial on `deleted_at`, so deleting a party released its `partyId` — while every stored reference keeps a UUID and renders whatever code that row now carries. A customer's `billingPartyId` went on reading `PARTY-BILL` after a different legal entity took the freed code, and a consumer resolving it landed on the wrong party. The issue named three ways out and asked for one to be chosen, together with [#103](https://github.com/sujanto-gaws/kelir/issues/103), which releases the *profile* numbers on the same delete. | **(2026-08-26) A `partyId` is never released.** `0018_party_code_is_not_released.sql` makes the index total, so a deleted party keeps its code and creating a party with that code is a `409` rather than a `201` — the one behavioural change, and the reason the refusal says "including by a deleted party" in as many words: the caller cannot see that party in any list, so a bare "already in use" would read as a bug in the API. Chosen because it removes the failure class rather than reporting it, and because a business identifier that names one thing this year and another thing next year is not an identifier. **Rejected:** rendering a soft-deleted referent as `null`, which reports a dangling reference honestly but leaves the code free to be re-taken, so live references still change meaning; and documenting the behaviour as intended, which makes every consumer of a party code responsible for knowing a code is valid only as of the moment it was read. **What this does not decide** is #103's half. Codes and numbers now behave differently on delete, deliberately for now — a supplier number is a slot in a sequence a deleted supplier has no claim on — and #103 is where that is settled. Applied to [Database Schema](../../docs/design/02.%20Database%20Schema.md) §1.3.1 and §4.1 and its mapping table, and to `deleting_a_party_releases_the_business_numbers_it_held`, whose fixture had been reusing a deleted party's code. |
| **D-20** | [#145](https://github.com/sujanto-gaws/kelir/issues/145): `chain_hash` covered ten inputs and neither audit payload column was among them, nor `created_at` — so what a record *says it changed* could be rewritten without disturbing any hash, and the chain still verified. Adding fields invalidates every chain computed without them, and no row carries a format version, so the question was whether to change the format now or design a version stamp for later. | **(2026-08-26) Change the format now, and change it once.** Nothing verifies a chain — FR-AUD-003 is Phase 6 — no release has ever checked one, and the `previous_hash`/`current_hash` values in existing databases have never been read by anything. Today it is a code change with no data consequence; after verification ships the same change needs a re-chaining migration plus a decision about what a row predating the version means to a verifier. Because the break is only free once, the field framing was fixed in the same change: fields are length-prefixed, so a boundary between two of them cannot be moved. **Rejected:** stamping a format version on every row and leaving the old rows out of the payload coverage, which buys nothing while no verifier exists and costs a branch in every future verifier; and deferring to Phase 6, which is the option that turns a free change into a migration. Applied to [Database Schema](../../docs/design/02.%20Database%20Schema.md) §10.2 and its mapping table, the doc comment on `modules::audit::record`, and — through `0019_audit_hash_covers_the_payload.sql`, because a merged migration is never edited — the column comments in `0003_audit.sql`. |
| **D-21** | [#200](https://github.com/sujanto-gaws/kelir/issues/200), found by the [Sprint 7 surface verification](../verifications/04.%20Sprint%207%20Surface%20Verification.md): `document_type_numbering_rules` held one `sequence_key`/`next_sequence` pair, and [Database Schema](../../docs/design/02.%20Database%20Schema.md) §6.3 stated that as the design — *"One bucket per rule."* A `DEPARTMENT_YEAR` rule needs one bucket **per department**, live at the same time, and one row holds one. The allocator restarted the counter whenever the computed key differed from the stored key and was not judged *earlier*, and two departments are never earlier than one another, so every allocation that changed department reset the only bucket there was: allocating for A, then B, then A, then B issued `000001` four times. Release-blocking for `v0.4.0`, and unfixable without a migration. | **(2026-08-26) The counter moves to one row per scope value, and `DEPARTMENT_YEAR` stays.** `0020_numbering_buckets.sql` creates `document_type_sequence_buckets`, keyed `(document_type_id, sequence_key)`, and drops the two counter columns from the rule row. **Keyed on the document type rather than on the rule**, because a rule row is replaced on every edit and a sequence that reset when somebody corrected a template would re-issue numbers just as surely — which makes "replacing a rule does not un-issue the numbers the old one produced" true by construction instead of by the `highest_issued` check that used to carry it. Allocation becomes one `INSERT … ON CONFLICT DO UPDATE … RETURNING`, so there is no read to race and two scope values do not contend at all. **Rejected: dropping `DEPARTMENT_YEAR`**, which was one line in the migration and would have deleted a capability from a published standard — the [Document Type Definition Schema](../../docs/schema/Document%20Type%20Definition%20Schema.md) v1.0.0 names it in the `sequenceScope` enum, in the `{department}` token table, and in rule **S5**, which requires `{department}` in the template *iff* the scope is `DEPARTMENT_YEAR`. Breaking a v1.0.0 standard to work around an implementation defect is the wrong way round. **Also rejected: deprecating the two columns in place** per the release process §6 N−1 rule — the previous release is `v0.3.0`, whose migrations stop at `0013`, so no released binary has ever seen this table and there is no release N holding the columns to deprecate them in; a column left behind would be storage nothing moves, which is what §6.3 and `0011` both warn against. **What this does not settle** is the back-dating refusal. Reaching into a passed bucket used to *restart* it and re-issue numbers; buckets persist now, so it would resume correctly. It is still refused, on the different ground that a closed period should not gain new numbers — a policy rather than a safety property, and the product owner's to revisit. Applied to [Database Schema](../../docs/design/02.%20Database%20Schema.md) §6.3, the new §6.3.1 and the mapping table, `numbering_repository.rs`, `numbering_service.rs`, and `document_numbering.rs` — whose `two_departments_run_separate_sequences` made two moves and asserted the true half of a broken property, and now makes three. |
| **D-22** | [#201](https://github.com/sujanto-gaws/kelir/issues/201), found by the [Sprint 7 surface verification](../verifications/04.%20Sprint%207%20Surface%20Verification.md): `{"sum": [a, b]}` evaluates to `0` on both engines, silently. Nothing on the path refuses it — the meta-schema's `jsonLogic` accepts any single-key object and defers arity by design, `sum` is in the `calculate` allow-list, the client renders `0`, and JFSS S8.1's server-side re-evaluation recomputes `0` and stores it. **Parity is what makes it invisible**: the Tamper-Proof Pattern catches a client that disagrees with the server, and here they agree. The mistake is a natural one — `+` sits beside `sum` in the registry, in the same allow-list, with the same bracket syntax, and it *does* take a list of operands. The [Calculation Rule Registry](../../docs/schema/JFSS%20Calculation%20Rule%20Registry.md) §3.2 specifies `{ "sum": [array] }` and the empty-array answer and is silent on every other shape, so this was a specification gap first and an implementation behaviour second. | **(2026-08-26) Refused when the definition is saved, and the engines are not touched.** `validate_definition` rejects a `sum` whose argument **list** is any length but one, with `SUM_TAKES_ONE_ARRAY`. A definition is written once and rendered thousands of times, so refusing at save costs one round trip to the person who can fix it; refusing at render costs a form that half-works for everyone else. **A non-array argument is left alone, and that was measured rather than assumed**: JSON Logic's shorthand — `{"sum": {"var": "line_totals"}}` — is one argument and evaluates correctly on both engines, so the obvious rule ("the argument list must have one element") would have refused definitions that work. **Rejected: raising an evaluation error in the operator**, which is louder and catches definitions stored before the rule existed, but changes the documented non-array answer and has to land on both engines in step — a parity-affecting change made to catch an authoring mistake, and the one class of change D-10 was adopted to make rare. **Rejected: documenting `0` as intended**, which is the cheapest and leaves the registry's flagship operator failing silently on its most natural misuse. **The corpus gap is closed with it**: both implementations' comments name three edge cases they must agree on — a non-array argument, an empty array, and non-numeric members — and `parity/cases.json` carried only the empty array. All three are in it now, plus the shorthand and the multi-argument shape, so the runtime answers are pinned even though the refusal happens earlier. Applied to `modules/rad/domain/jfss.rs`, `parity/cases.json` and `expectations.json`, Calculation Rule Registry §3.2, and `rad_forms.rs`. |
| **D-23** | [#161](https://github.com/sujanto-gaws/kelir/issues/161) needs a form definition to declare a lookup field naming a master-data source, and JFSS has nowhere to put one. The meta-schema closes a component with `unevaluatedProperties: false`, so `dataSource` on the field is not a conformant v2.0.1 document; and the specification is a **`Final Standard`** — frozen at v2.0.1, errata only, **a successor takes a new major version** ([naming convention](../../docs/standards/02.%20Naming%20Convention.md) §10.1) — so a v2.1.0 that adds an optional property is not available either. The question is where the binding lives. | **(2026-08-26) In the form's `settings`, mapping a component `id` to a source key, and JFSS is not touched.** `settings` is the one object the specification declares `additionalProperties: true` and describes as global form configuration, which makes it the extension point a frozen standard leaves an implementation — so a Kelir form carrying a lookup is a conformant JFSS **v2.0.1** document, and every existing definition stays valid. Bound on `id` rather than `key` because §4.1 makes `id` unique per component *instance*, while a `key` inside a datagrid row template lives in the row's scope and two components may legitimately share one. **The cost is real and is recorded rather than hidden:** the binding does not sit on the component it binds, which departs from JFSS's own component-driven philosophy, and a reader of a definition has to look in two places. **Rejected: JFSS v3.0.0 with a `dataSource` property.** It is the design the standard's philosophy wants, and it is a phase-scale decision — a new major version of a Final Standard means a new meta-schema, a migration story for every stored definition, and a conformance clause that has to say what a v2 document means to a v3 validator. That is not a sprint item, and taking it inside one would have been deciding the standard's future to save a level of nesting. **Rejected: editing `jfss-meta-v2.0.1.json` in place** to add the property — the same class of mistake as editing a merged migration: the file is the published artefact its `$id` names, and a definition validated by two different v2.0.1 schemas is the drift the byte-comparison test in `rad_jfss_meta_schema.rs` exists to prevent. **Rejected: a `settings.kelir` namespace** under it. `settings` is already the implementation's namespace, since the specification defines nothing inside it, so a `kelir` level would namespace Kelir against itself; a future plugin surface takes its own key beside `lookups`. **Left open for whoever revisits it:** if a second Kelir extension needs `settings`, the shape of that object becomes a contract worth writing down, and the place for it is this document rather than JFSS. Applied to `modules/rad/domain/jfss.rs` and `modules/rad/domain/lookup.rs`, [SDD](../../docs/design/01.%20System%20Design%20Document.md) §8.2.1, [Database Schema](../../docs/design/02.%20Database%20Schema.md) §5.12 and §5.13, and `rad_lookups.rs`. |
| **D-24** | **Division by zero is specified as `0` and the adopted engine does not deliver that uniformly.** [Calculation Rule Registry](../../docs/schema/JFSS%20Calculation%20Rule%20Registry.md) §3.1 requires `0`; measured against the engine, `10.5 / 0` returns `null` and normalizes to `0` as asked, while `10 / 0`, `0 / 0` and `10 % 0` **throw** — the integer path ignores the `division_by_zero` setting entirely. Both sides measure identically, so parity holds and the gap is registry-versus-engine rather than a divergence. It cannot stay measured-but-undecided past this sprint: [#163](https://github.com/sujanto-gaws/kelir/issues/163) has to render something and [#164](https://github.com/sujanto-gaws/kelir/issues/164) has to return something. Raised by the [operator-parity spike](../spikes/01.%20JFSS%20Operator%20Parity.md) §2.3, and **attributed in the registry to D-15, which does not cover it** — D-15's text is the `conditional.logic` tier and the `regex` constraint. | **(2026-08-26) Every division by zero is an evaluation error.** `DivisionByZeroHandling::ThrowError` on both sides in place of `ReturnNull`, so the four cases behave identically instead of splitting on whether the numerator happens to be fractional. A field whose calculation errors renders **blank** and does not block typing; the refusal happens at submit, with the S10.3 envelope naming the field. **Why refusal rather than the `0` §3.1 asks for.** A uniform `0` is not reachable by configuration — the engine offers `ReturnSaturated`, `ThrowError`, `ReturnNull` and `ReturnInfinity`, and the integer path throws under all of them — so it would take catching `EvaluationError` and discriminating on the engine's message text. That text is not API, and a version bump would change it silently, which is precisely what the pinned tests on both sides exist to make loud. It would also swallow the `min`-with-an-absent-operand refusal that both sides ship deliberately and that spike §2.5 called the better of the two failures. And an average over zero scores is not `0`: persisting `0` for it is the class of silent wrong answer the spike was written about. **Rejected: keep `ReturnNull` and define only what the throw means.** Smaller diff and no standards change, but it ships the asymmetry — `{"/": [{"var": "total"}, {"var": "count"}]}` against a zero-filled `count` would render `0` when `total` is fractional and refuse when it is an integer, from one expression. **Cost, accepted:** the registry is an `Active Standard`, so this is a version bump to **1.6.0** rather than an errata — §3.1's division entry and §7.3 change what they require of an implementation. JFSS itself is untouched; S8.1 and S10.2 say nothing about division. **Applied to** the registry §3.1 and §7.3, [`parity/README.md`](../../parity/README.md), and the [Sprint 8 construction plan](03.%20Sprint%208%20Form%20Renderer%20Construction%20Plan.md) §8. **The code landed with [#163](https://github.com/sujanto-gaws/kelir/issues/163) on 2026-08-26**, first within it as the plan §5 required: `ThrowError` on both sides, the two pinned division tests inverted, and `mod-by-zero` added to the parity corpus — the fourth of the four expressions the decision names, and the one the corpus could not have seen move because it carried `10 % 3` and no modulo by zero at all. The gap between the registry stating the requirement and the code meeting it lasted one day and is closed. |
| **D-25** | **When does a rendered form show what it thinks of what has been typed?** #163 makes a form evaluate its own `validation` and `rules` continuously, and nothing in JFSS or either registry says when the results become visible. The obvious reading — show every violation as soon as it is computed — puts a `required` message under every empty box on a form nobody has touched. | **(2026-08-26) A form computes its verdict continuously and shows it only once a submit has been attempted.** After that the messages track live, so a corrected field clears immediately. The reasoning is the [Sprint 8 construction plan](03.%20Sprint%208%20Form%20Renderer%20Construction%20Plan.md) §5.6's, applied to the other half of the evaluation: that section already refuses to mark a *calculated* field wrong on arrival, because on a zero-filled payload an average fails before the first keystroke and *a form that greets its user with three red fields it filled in itself is a worse failure than the one D-24 prevented*. The same sentence is true of a required marker on a box nobody has reached. **Rejected: per-field "touched" tracking**, which is the conventional answer and shows a message once a person has left the field. It needs a touch key per rendered instance, and a repeater's rows share their template's `id` — so either a row's fields all become touched together, or the key has to carry the row, which is a second scope mechanism beside `useFieldScope`. Submit-attempt is one flag, and it is what #163 AC5 describes anyway: *a required field left empty and the submit seen to be blocked*. **Cost, accepted:** somebody who fills a long form top to bottom learns about a bad cost centre at the end rather than in passing. **Consequence for #164:** `JfssForm` gates the `submit` action and passes every other action through, so what a submit *does* stays that issue's and whether it may happen at all is the definition's rules. **Applied to** `features/rad/JfssForm.vue`, `features/rad/renderer/useFormEvaluation.ts` and both of their specs. |
| **D-26** | **What does a form do with a rule the Validation Rule Registry defines and the frontend cannot decide?** The [construction plan](03.%20Sprint%208%20Form%20Renderer%20Construction%20Plan.md) §5.3 gives two answers — decide it, or raise on a name nobody defines — and the registry contains rules that are neither. §3.2's `passwordStrength` needs a scoring library (it names `zxcvbn`) that `kelir-frontend` does not carry, and its `async` needs an allow-listed read-only endpoint that is not served. Both are scoped `client`, so no server re-evaluation covers them. | **(2026-08-26) A third tier, declared in one place: the rule is named on the form as one that is not checked as you type, with the registry's reason.** `VALIDATION_RULES` in `features/rad/renderer/validation.ts` gives every catalogue entry either a decision function or a stated reason, never neither and never both, and a spec asserts that. `server`-scoped rules are filtered out of the notice — those are not a gap, they are registry §3.3 working as written. **Why not simply skip them.** *A rule that fails open is a rule that is not there, reported as a rule that passed* — §5.3's own sentence, and the reference implementation's `backendValidator.ts` returning `true` for an unknown rule is the shape it warns about. **Why not raise, as an unknown name does.** A name nobody defines is a defect in the definition; a rule the registry defines and this runtime cannot run is a gap in this runtime, and refusing to render a form over it would take a working form off the screen for a strength meter. **Why not add `zxcvbn`.** Adding a dependency is a decision rather than an import — the same position the construction plan §2 takes on `zod` — and a hand-rolled scorer would give `minScore: 3` a different meaning on each side, which is the divergence class the operator-parity spike was written about. **Cost, accepted:** a form using `passwordStrength` shows a notice explaining that the check happens at submit; it is visible to the person filling the form rather than only to whoever reads the code. **Applied to** `features/rad/renderer/validation.ts`, `features/rad/JfssForm.vue` and their specs. |
| **D-27** | **In what order does the server re-evaluate `calculate` and `conditional`?** The [Sprint 8 construction plan](03.%20Sprint%208%20Form%20Renderer%20Construction%20Plan.md) §6.3 numbers conditional stripping as step 3 and the calculation overwrite as step 4; [JFSS](../../docs/schema/JSON%20Form%20Schema.md) §9.2 lists *Calculation Overwrite* before *Conditional Stripping*. The plan and the specification disagree, and a submission has to pick one. | **(2026-08-27) The calculations run first, as §9.2 has them, and the plan's numbering is the one that gives.** The plan §1 names JFSS as the specification authority for these three items, so where the two disagree this is not a close call — but it would be worth taking even if it were, because the order is a security property rather than a preference. **A `conditional` keyed on a computed field is decided from whichever number is in the payload when it runs.** Strip first and that number is the *client's*: an attacker who claims a grand total of five million opens an approval branch the rules would have kept shut, and the value they smuggled in beside it is persisted. Calculate first and the branch is decided from the server's own figure, which is the whole of S8.1 applied one layer further than S8.1 states it. `a_conditional_over_a_computed_field_reads_the_servers_number` is that case, and it fails against the other order. **What it costs**: a `calculate` that reads a field a conditional later hides is evaluated against that field's live value rather than against its absence. That is correct rather than a compromise — the browser does exactly the same, because it has no stripping step at all, so the two sides agree by construction, which is what S10.1.1's whole argument is about. **Rejected: two passes**, calculating, stripping, then calculating again over the stripped payload. It converges to a different answer from the browser's on any form where a hidden field feeds a total, which trades a real parity property for a tidier reading of "absent". **Also settled here, from the same paragraph:** a hidden component is *"treated as absent for validation"* (§9.2), so a `matchesField` targeting one compares against absent on this side and against the live value in the browser. That divergence is the specification's own instruction and is named in `service/evaluation.rs` where it happens. **Applied to** `modules/rad/service/evaluation.rs`, [SDD](../../docs/design/01.%20System%20Design%20Document.md) §8.2.2, and the construction plan §8. |
| **D-28** | **What does the *server* do with a rule the Validation Rule Registry defines, scopes to the server, and this build does not implement?** **D-26** answered the browser's version of the question and could defer: its `undecided` tier says *"checked when this form is submitted"* and means the backend. The backend has nobody below it. Registry §3.3 defines `unique`, `exists` and `authorized`; a form definition may carry any of them today, because `validate_definition` checks the meta-schema, the approved operator set and the lookup allow-list at save, and a rule *name* is none of the three. | **(2026-08-27) The submission is refused, naming the rule and saying why.** `RULE_NOT_ENFORCED` in the S10.3 envelope, against the field that carries it. A definition using one therefore cannot be submitted at all until the rule is built — which is loud, and is the point: *a rule that fails open is a rule that is not there, reported as a rule that passed*, and "nobody has written it yet" is that same failure with a better excuse. **Rejected: skipping them**, which is the reference implementation's `backendValidator.ts` returning `true` and the exact shape §5.3 warns about. **Rejected: refusing the *definition* at save**, which is where every other definition-level refusal lives and is where this one should eventually go — but a rule name is not checked at save today, so adding it would change #156's contract and refuse definitions that are already stored. **Rejected: implementing them.** `unique` and `exists` are queries against the table a form writes to, and a form does not write to a table yet — documents are Sprint 9 — while `authorized` is FR-DTYPE-008's document security level, which is unbuilt and on Sprint 9's cut tail. **Cost, accepted:** the three are unusable until then, and a form author who reaches for one meets a refusal rather than a silence. The construction plan §8 had already taken the same position from the other end by keeping a `unique` out of the shared fixture. **The other half of the same question is what the server now *does* decide**, and it needed a dependency: the five `both`-scoped rules of registry §3.1 include `regex`, which no Rust standard library decides, so `kelir-backend` takes the `regex` crate. Adding a dependency is a decision rather than an import (D-26's own words), and the alternative was worse — a `both`-scoped data-integrity rule passing by default on the side that is not trusted. The two engines read a pattern differently and the registry already says so under its `regex` warning; that gap stays **D-15**'s and is not widened here. **Applied to** `modules/rad/domain/validation.rs`, `modules/rad/service/evaluation.rs`, `Cargo.toml`, and the construction plan §8. |
| **D-29** | **JFSS S10.3 shows its error envelope with `"status": 400` and every field-level refusal in this API answers 422.** The [Sprint 8 construction plan](03.%20Sprint%208%20Form%20Renderer%20Construction%20Plan.md) §6.3 asks for *"a 4xx carrying the S10.3 envelope"* at step 6 and for a `400` on an unknown rule at step 5, and S8.1.1 says `400 Bad Request` of an unknown custom operator. A submission that refuses has to answer with one number. | **(2026-08-27) 422 with the S10.3 envelope, for every refusal a field can carry.** `AppError::Validation` is the only variant that carries `details`, and `ValidationDetail` **is** S10.3's shape — `{path, rule, code, message}` — documented as such in `error.rs` since Phase 1. Every other field-level refusal in the product already answers 422 through it, including `validate_definition`'s refusal of a non-conformant JFSS document, so a submission answering 400 would give one failure class two status codes and leave a client branching on which endpoint it called. **What is preserved is what S10.3 actually specifies**: the envelope, the dot-notation `path`, and the stable machine-readable `code` a client branches on — `RULE_NOT_REGISTERED` for the unknown-rule case S8.1.1 names, `EVALUATION_FAILED` for an expression that produced no value. The `400` in S10.3 sits inside an illustrative body rather than in a MUST, and 422 is a 4xx, which is what the plan asks for. **Rejected: 400 for the two structural cases and 422 for the rest**, which is the most literal reading and the worst outcome for a caller: the same refused submission would answer differently depending on which of its fields was wrong. **Cost, accepted:** a reader comparing the response to S10.3's example sees a different number beside an identical body. Recorded here so it reads as a decision rather than an oversight. **Applied to** `modules/rad/service/submission.rs`, `modules/rad/handlers.rs`, [SDD](../../docs/design/01.%20System%20Design%20Document.md) §8.2.2, and `tests/rad_form_submissions.rs`. |
| **D-30** | **What happens when an administrator re-points a document type at a different form, and documents of that type already exist?** [#165](https://github.com/sujanto-gaws/kelir/issues/165) AC3 refuses to let this default: *a document rendered against a form it was not filled against is a data-integrity problem that looks like a UI bug*. #157 built the update path without deciding it — `formId` was a field like any other, and changing it on a type with a thousand documents was one PUT. | **(2026-08-27) Re-pointing is allowed and existing documents keep the revision they pinned; it is refused only while a document exists that pinned none.** **Why not simply refuse.** A form is revised by publishing the next revision, so a type that could never be re-pointed is stuck on revision 1 from the moment its first document is created — which does not protect the revision mechanism, it deletes it. The operation an administrator needs is exactly this one: point the type at the new revision so *new* documents use it. **Why the exception is where it is.** [Database Schema](../../docs/design/02.%20Database%20Schema.md) §6.6 pins a document's own `form_id` at creation, and a document that pinned one is unreachable from the type's binding — the guarantee holds for it by construction. But §6.6 makes that column **nullable**, and a document that pinned nothing has only its type's *current* binding to render against. Those documents, and only those, are what a rebinding can damage, so those are what it is refused over. That is the difference between a guarantee and a comment describing one. **Rejected: `NOT NULL` on `documents.form_id`.** It is the structural fix and it is the wrong scope — it would forbid a document of a type that binds no form, which §6.2 permits, and it would settle a column [#167](https://github.com/sujanto-gaws/kelir/issues/167) has not written to yet. A migration reaching forward into an unbuilt surface is a constraint whoever builds that surface has to argue with rather than design against. **Rejected: versioning the binding** — keeping a history of which form a type pointed at and when, so a document could resolve its revision by date. That is FR-RAD-012 metadata versioning, which is unscheduled, and it reconstructs by inference what a pinned column already states. **What it costs, accepted:** #167 must pin `form_id` when it creates a document, or every type its documents belong to becomes un-rebindable. That obligation is not left as a note — inserting into `documents` takes a `FOR KEY SHARE` on the `document_types` row, and the rebinding's `FOR UPDATE` conflicts with it, so the check and a concurrent creation serialize by the foreign key rather than by anything Sprint 9 remembers. `a_document_created_during_the_rebinding_is_not_missed` holds that and goes green when the lock is weakened one step. **Applied to** `modules/document_type/service.rs` and `repository.rs`, [Database Schema](../../docs/design/02.%20Database%20Schema.md) §6.2 and §6.6, and `tests/document_types.rs`. |
| **D-31** | **The forgot-password endpoint is an account-enumeration oracle by timing, and how much of it is worth closing?** [#202](https://github.com/sujanto-gaws/kelir/issues/202) measured it: `request_reset` awaited a complete SMTP transaction before the `202` was written, so a known account answered in a p50 of 90.1ms and an unknown identifier in 9.8ms, ranges not overlapping, on a route deliberately left off the rate limiter. The module header claimed "no branch a caller can time". | **(2026-08-27) The send is detached and the residual is documented rather than padded.** `Mailer::send_detached` hands the message to the runtime, which costs nothing — `send` already reports no failure to anybody, so awaiting it only ever bought the caller a measurement. Re-measured the same way: 26.4ms against 10.5ms. **The oracle is narrowed, not closed**, and the header says so with both numbers rather than repeating a claim that was false either way. **Rejected: detaching the token issue as well**, which would flatten both paths to one indexed `SELECT` and would break the resend throttle — `RESEND_COOLDOWN_SECONDS` is enforced by reading the last issued row before inserting the next, and nothing in the schema stops two of those interleaving, so concurrent requests would each see no recent token and each send a link. Trading a mailbox-flooding control for 16ms of a timing side channel is the wrong side of that trade. **Rejected: padding the fast path to a fixed budget** — the control that would actually close it, and a different control with a different failure mode: a budget too low is a new oracle and a budget too high is a request-holding denial-of-service surface, on the one route that must stay cheap. It is worth taking when the endpoint gets a threat model rather than as a finding fix. **What it costs, accepted:** an enumeration oracle stays open at 16ms, reachable by a caller who can measure a loopback-quality connection to the deployment. Recorded so the next reader finds a decision rather than an oversight. **Applied to** `mail.rs`, `modules/auth/reset.rs` and `tests/auth_password_reset.rs` — `the_answer_does_not_wait_for_the_mail_to_be_sent` is the property, held by an injectable slow transport the suite did not have. |

| **D-32** | **A document's form data is arbitrary tenant content. What does an audit record say when it changes?** [#167](https://github.com/sujanto-gaws/kelir/issues/167) AC5 requires an update's record to name only the fields that moved, and refuses to let a JSON blob default: *"the whole document changed" is not an audit record*. The values inside it are salaries, bank details, the medical grounds for a leave request — whatever a tenant's form author put there. | **(2026-08-27) The record names the data keys whose values moved, and carries neither the old value nor the new one.** `{"formData": {"changedKeys": ["amount", "line_items"]}}`, the same list on both halves of the `ChangeSet`. **The load-bearing reason is the permission boundary, not brevity.** The audit trail is read through its own permission by people who hold none over the document, and **D-12** already refused to hand a record's field values back through its change history without the record's own read permission. Copying every keystroke of every form into that table would be D-12's finding at scale, over data nobody classified. **The changed-key list is also what an auditor asks for** — *when did the amount last move, and who moved it* — and the values themselves belong to `document_versions` behind the document's own permission when FR-DOC-008 lands. **Rejected: the values, as every other field gets.** Every other field on a document is platform-defined and its meaning is fixed; a form's keys are tenant-defined and its values are unclassified. **Rejected: a hash of the payload**, which answers "did it change" and nothing an auditor asks. **Rejected: nothing at all**, which would make an edit to a document indistinguishable from an edit that touched no form data. **What it costs, accepted:** an auditor cannot see *what* a figure changed from without the document's own read permission and FR-DOC-008's version history — which is the same trade D-12 took and stated. **Applied to** `modules/document/service/document.rs`, and held by `a_form_data_change_audits_its_keys_and_not_its_values`, seen red against a build carrying the values. |
| **D-33** | **A document is validated on every write, and `required` is the rule an unfinished draft breaks. Which of them refuse before submit?** #167 AC2 asks for server-side validation on *every* write, not only at submit, because *a draft holding data its own form would reject is a draft that cannot be submitted, discovered late*. Taken literally it makes an empty draft unsaveable, which is the state every document starts in. | **(2026-08-27) A draft write refuses everything except absence, through a `Strictness` parameter on the one evaluator.** The line is drawn where the sentence is: **a value that is present and wrong is refused; a value that is missing is not wrong, it is unfinished.** Three failure classes are forgiven on a draft and only on a draft, matched on the `(code, rule)` pair the walk emits rather than on message text — `VALIDATION_FAILED`/`required` (the definition of a draft, and the *only* verdict an empty field can produce), `RULE_NOT_ENFORCED` (which is **D-28**'s own reasoning rather than an exception to it: it refuses because *"this side has nobody below it"*, and on a draft it does — the submit), and `EVALUATION_FAILED` (which is **D-24** applied to the server's side of the same moment: an average over zero rows fails before the first keystroke, and the Sprint 8 plan §5.6 already settled that the answer while a form is being filled in is a blank field rather than an error). `RULE_NOT_REGISTERED` still refuses, because a rule nobody defines is a defect in the *definition* and no amount of finishing makes it go away (JFSS S8.1.1). **Rejected: a second, laxer pipeline for drafts.** Two answers to what the Tamper-Proof Pattern does is the failure the pattern is about; the submit calls the same function with `Strictness::Submit` and gets today's behaviour byte for byte. **Rejected: no validation until submit**, which is AC2 refused rather than interpreted. **Rejected: filtering by message text**, which a rewording would silently widen or narrow. **What it costs, accepted:** a draft can hold a payload the submit will refuse — but only by being unfinished, which is what a draft is. **Applied to** `modules/rad/service/evaluation.rs` (`Strictness`), `modules/document/service/document.rs`, and the [Sprint 9 construction plan](04.%20Sprint%209%20Document%20Construction%20Plan.md) §4.2 — whose table gained its third row during construction rather than at planning. |
| **D-34** | **A section header in the Database Schema names the migration that will create its tables, and seven of them named files two numbers out of date.** [#205](https://github.com/sujanto-gaws/kelir/issues/205) established that a pointer *elsewhere* in that document names a migration by what it creates rather than by a number, because six of them had drifted. The section headers were not covered, and `0021`, `0022` and `0023` moved every unwritten migration below them twice more. | **(2026-08-27) A section header stops restating a number for a migration that is not written yet, and says what the migration creates instead.** *"# 7. Workflow — the workflow migration"*. A header for a migration that **has** been written keeps its filename, because that filename is now a fact rather than a plan. **The mapping table is the sequence and a header is not**, which the table's own paragraph now says. **Rejected: bumping the numbers**, which is what a reader would do and what would drift again on the next early arrival — the fourth time in this document. **Rejected: freezing numbers for unwritten migrations**, which is [naming convention](../../docs/standards/02.%20Naming%20Convention.md) §4.3 reversed: a migration takes the next free number when it is written, precisely so that an early arrival costs a table edit rather than a renumbering. **What it costs, accepted:** a reader who wants the filename for §8 looks it up in the mapping table, one scroll away, instead of reading it in the header. **Applied to** [Database Schema](../../docs/design/02.%20Database%20Schema.md) §§7–13 and its mapping-table paragraph. |

| **D-35** | **A submit must not hold two pooled connections, and a gap-tolerant rule's number is committed in a transaction of its own. Where does the allocation go?** [Coding standard](../../docs/standards/01.%20Coding%20Standard.md) §2.5 forbids a request from holding more than one pooled connection at a time. `allocate_committed` opens a transaction of its own — that is what `AllowGaps` *means* — so calling it from inside the submit's transaction was two connections per submit, and twenty-four concurrent submits of a gap-tolerant type answered `500` after the acquire timeout. It is [#118](https://github.com/sujanto-gaws/kelir/issues/118)'s shape, and this path carried it twice: once in the policy *read*, fixed when a concurrency test went red, and once in the allocation itself, found only by re-reading against the rule. | **(2026-08-28) The number is taken before the submit's transaction opens, which is where §2.5 says to resolve what a request points at.** The document's status, type and department are read on the pool; the policy is read on the pool; and a gap-tolerant rule allocates there, committed, with nothing else held. A gapless rule is unchanged — its allocation is inside the transaction and rolls back with it, which is what makes it gapless. **This changes behaviour: a gap-tolerant rule now loses the number a refused submission took.** That is the trade **#158** named — *a number allocated to a submission that then fails is gone* — so it restores the policy rather than weakening it; the previous behaviour kept the number, which was an accident of ordering that `service::submit` had written down as a property and had bought with the rule violation. **Rejected: re-evaluating on the pool before allocating**, then writing under a lock that re-checks the payload has not moved. It keeps the no-burn behaviour and satisfies §2.5, and buys that with a new failure class — a 409 a concurrent edit can raise on a submit that was going to succeed — while making the two gap policies differ by less, which is the opposite of what having two of them is for. **Rejected: making every rule gapless**, which deletes a capability #158 built and makes every submit of a busy type serialise. **What it costs, accepted:** a gap-tolerant sequence has holes where submissions were refused, which is what a deployment chooses that policy knowing. **The rule the finding argues for, and it is the larger half:** *a fix for a rule violation is checked against the rule, not against the test that exposed it* — both halves of this defect were twelve lines apart in one function. **Applied to** `modules/document/service/submit.rs`, `modules/document/repository/document.rs` (`find_submission_subject`), and `tests/documents_submit.rs`, where `concurrent_submits_of_a_gap_tolerant_type_do_not_exhaust_the_pool` is red under the allocation moved back inside. |

| **D-36** | **A document with a live workflow has two things that could set its status, and only one of them may.** [#178](https://github.com/sujanto-gaws/kelir/issues/178) AC2 requires the synchronization to be one-way — a workflow transition sets `documents.status`, and setting `documents.status` does not move the workflow — and then leaves open what the *existing* manual route does about it. Sprint 9 shipped `PUT /documents/{id}/status` behind its own permission, with a legality table and a compare-and-swap; Sprint 10 gave the same column a second writer. Leaving both is a document whose status disagrees with the process deciding it, produced silently, on the screen a person is most likely to trust. | **(2026-08-28) `PUT /documents/{id}/status` refuses a document that has a live process instance, naming the instance.** The projection is one-way and the route is where that is enforced. A document whose process has *finished* is transitionable again, which is not a loophole: nothing is deciding it, and the legality table is back to being the only rule. **Rejected: letting the manual transition win**, which makes the workflow a suggestion; and **rejected: silently ignoring it**, which is the same outcome with no message. **Cost, accepted:** this withdraws a capability the document surface shipped one sprint earlier, for documents of types that bind a workflow. #178 AC2 allows for a manual override as *"a separate, audited action rather than a side effect"* — none is built, because nothing has asked for one and the refusal is what makes the need visible if anything does. **Applied to** `document::service::status`, `modules/workflow/mod.rs`, [SDD](../../docs/design/01.%20System%20Design%20Document.md) §8.5.2, and `workflow_engine.rs`'s `a_document_under_a_workflow_cannot_have_its_status_set_by_hand`. |

| **D-37** | **JWSS declares six `assigneeType` values and Kelir can resolve four. What happens to the other two?** `MANAGER_OF_OWNER` needs a user-to-manager edge: `departments.manager_party_id` names a **party**, and party-to-user is not a resolvable relation in this schema — FR-ORG-002's reporting line is unbuilt. `EXPRESSION` needs a directory context that JWSS §6.1's condition context (document, formData, variables, actor) does not carry. A definition using either would publish cleanly and then fail to assign its first task. | **(2026-08-28) Both are refused at *save*, with a message naming what to use instead.** A definition using one cannot be stored, let alone published. It is `jfss.rs`'s discipline applied to the other definition kind — *a definition is written once and executed many times, and the execution path has no good failure* — and it is a registry-level narrowing of an open vocabulary rather than a departure from the specification, exactly as `LookupSource` narrows JFSS's open component `type`. **Rejected: accepting them and failing at the transition**, which is the stalled instance nobody is told about that [#176](https://github.com/sujanto-gaws/kelir/issues/176) AC1 names. **Rejected: resolving `MANAGER_OF_OWNER` through `manager_party_id` anyway** — it would route an approval to somebody chosen by a coincidence of data, which is worse than refusing. **Recorded in the standard, not only in code:** [JWSS §5.3](../../docs/schema/JSON%20Workflow%20Schema.md) carries the table, because a specification that lists six and an implementation that serves four is exactly the gap a workflow author discovers at the worst moment. **Applied to** `workflow::domain::jwss`, `workflow::domain::graph::AssigneeType` — which has no variant for either, so an unrepresentable state cannot be forgotten in a `match` — and JWSS §5.3. |

| **D-41** | **FR-TASK-006 asks for a comment with the approval decision; [#182](https://github.com/sujanto-gaws/kelir/issues/182) AC4 asks for it to be *refused* where it is missing — "where the definition makes a comment mandatory, both ends agree". Nothing in Kelir let a definition make it mandatory.** [JWSS](../../docs/schema/JSON%20Workflow%20Schema.md) v1.0.0 declares no such property, so the requirement had three possible homes: a hard-coded rule (*a rejection needs a reason, an approval does not*), a workflow-level setting, or a property on the transition. | **(2026-08-28) A `requiresComment` boolean on the transition object, defaulting to `false`.** **Why the transition and not a rule in code.** That is the granularity at which the answer actually differs — an approval explains itself and a refusal does not, so `REJECT` and `RETURN` are the edges an author marks and `APPROVE` is the one they do not — and a hard-coded rule would settle for every deployment a question that belongs to whoever wrote the workflow. A workflow-level setting could not express the difference at all. **Enforced in `engine::fire`, against the chosen edge and after the `allowedBy` check.** Against the *chosen* edge because `condition` selects between two same-action transitions when the decision arrives (S7), so neither the client nor the request type can know which edge will fire. After the authorization because the two refusals disclose different things: a 422 saying *this edge wants a reason* is feedback about the payload, and offering it to somebody who may not take the edge answers a question they have not earned. **The spec version is not bumped, and §12 says so rather than leaving it to be noticed.** JWSS is a `Draft Standard` — [naming convention](../../docs/standards/02.%20Naming%20Convention.md) §10.1: *the shape may still change* — and that status is the signal, which is why JWSS gained a Revision History instead. The version starts carrying compatibility when the specification becomes `Active Standard`. The addition is a **strict widening**: optional, defaulting to `false`, so every stored definition stays valid. **S12 refuses it on an `AUTO` transition, at save.** An `AUTO` edge has no caller, so one demanding a reason could never fire — a stalled instance nobody is told about, produced by a definition that published cleanly, which is the failure **D-37** refuses two assignee types to avoid. Expressed in the meta-schema beside S5 rather than in `structural_errors`, for S5's reason: the normative artifact already answers it. **Rejected: making the comment mandatory for every `REJECT`.** It is the intuition, and it is wrong often enough to matter — an author who wants a reason on an approval over a threshold has no way to ask for one, and an author whose rejections are a one-click triage step is forced into prose. **Rejected: a client-side rule.** #182 AC4 is that both ends agree, and two implementations of one rule agree until the first workflow marks an `APPROVE`; `AvailableDecision.requiresComment` carries the server's answer to the screen instead. **What it costs, accepted:** where a state offers two same-action edges that differ on `requiresComment`, the screen asks for a reason if *either* does, so it can be stricter than the edge that actually fires. Of the two ways to be wrong, asking for a reason that turns out to be optional costs a sentence; not asking produces a refusal from a button the product drew. **And the comment is not copied into `audit_events`** — the audit record carries `commented` as a boolean and nothing more. The trail is read through `master-data:audit:read` by people holding no permission over the document, and a decision comment is prose an approver wrote about somebody's requisition; **D-12** and **D-32** drew that line for field values and form data, and this is the same line. Whether a decision the workflow required a reason for was recorded with one is a question about the control, and it is answerable without reading the answer. **Applied to** [JWSS](../../docs/schema/JSON%20Workflow%20Schema.md) §4, §4.1, S12, §10, §12 and the meta-schema; `workflow::domain::graph`, `workflow::domain::task` (`normalize_comment`, `comment_required`), `workflow::service::engine`, `workflow::service::task`, `workflow::service::inbox`, `workflow::repository::task`; `TaskDetailPage.vue` and `WorkflowTab.vue`; and `tests/workflow_engine.rs`, where `a_transition_that_requires_a_reason_refuses_a_decision_without_one` is red against the check removed. |

| **D-42** | **A return sends a document back; a resubmission brings it up again. Where does the resubmission come from?** [#183](https://github.com/sujanto-gaws/kelir/issues/183) AC5 requires a returned-then-resubmitted document to keep its number, which makes the resubmission part of the item rather than a later one. But [JWSS](../../docs/schema/JSON%20Workflow%20Schema.md) §10's own example leaves `RETURNED` **stateless** — no `task` — with a `RESUBMIT` edge `allowedBy: "OWNER"`. Every transition Kelir could fire went through `POST /workflow/tasks/{id}/decision`, which is addressed by a task id, and there is no task. The options were: give `RETURNED` a task assigned to the owner so the existing endpoint works; add a document-addressed route for the edge; or route it through the submit the owner already presses. | **(2026-08-29) `POST /documents/{id}/submission` fires `RESUBMIT` when the document is `RETURNED` and its process offers that edge.** **Why the submit and not a new route.** It is the same button, and it is the same act: the author corrects the document and sends it. A second endpoint would ask the screen to know which of two verbs its submit button means, and the answer would be *look at the status* — which is what the server already does. **Why not give `RETURNED` a task.** It would work and it would be a lie about the queue: a task is what puts work in somebody's inbox, and a returned document is with its author, not in an approver's list. Manufacturing one to reuse an endpoint would make #179's inbox count work nobody is waiting on, and it would contradict JWSS's own example — which the engine is supposed to execute, not correct. **Three consequences, each taken deliberately.** `is_editable` **split** from a new `is_discardable`: a returned document may be corrected and may **not** be deleted, because it holds a number, a status history and a live process, and deleting it would strand the instance that returned it. The submit **allocates nothing** on a resubmission under either gap policy — not *allocates and discards*, which would leave a permanent hole per correction round on a gap-tolerant rule — and `mark_submitted` `COALESCE`s the number so keeping it is a property of the statement rather than of the caller. And the resubmission's `AssignmentContext` carries the document's **`created_by`**, not the caller: the two coincide on a first submit and need not here, and `allowedBy: "OWNER"` on the `RESUBMIT` edge must refuse somebody who merely holds `document:submit`. That last one also corrects `start_workflow`, which passed the caller into the owner slot — latent while the creator was always the submitter, and visible the moment a second path could reach it. **Rejected: inferring the target — "send it back one step".** #183 AC2 refuses it and is right: the moment a definition has a branch, *one step back* is ambiguous, and each reader would resolve it differently. The definition's `to` already names the target, so **no JWSS change was needed for this item** — the property that would have been added already exists. **What it costs, accepted:** the submit path now has two shapes behind one endpoint, and the module header has to say which is which. The alternative was two endpoints and a client that chooses between them, which moves the same branch somewhere it can be got wrong by more people. **Applied to** `document::service::submit` (`resubmit_workflow`, `refuse_unless_submittable`), `document::domain::status` (`is_editable` / `is_discardable`), `document::repository::document` (`mark_submitted`, `update_document`, `soft_delete`, `SubmissionSubject`), `workflow::domain::task::DecisionAction`, and `tests/workflow_engine.rs`, where `a_returned_document_is_corrected_resubmitted_and_keeps_its_number` is red against the `COALESCE` removed. |
| **D-43** | **Four `Should` items were targeted at Sprint 11 and cut from it, and the [sprint plan](01.%20Sprint%20Plan.md) §5 named them as cuts without re-targeting them here.** System tasks (FR-WF-005), the completed task view (FR-TASK-009), task search (FR-SRH-003) and master-data change requests through workflow (FR-MDM-010) still read `Sprint 11` in §4 while Sprint 11 shipped without them. A backlog that targets a sprint which has closed is not a plan, and the next reader cannot tell a cut from an oversight. | **(2026-08-29) All four are re-targeted rather than unscheduled, and none becomes a `Could`.** **FR-MDM-010 → Sprint 13**, the tail of Phase 6, which is where the sprint plan already said it belonged: it needed this entire phase to exist first, and it now does. **FR-TASK-009 and FR-SRH-003 → Sprint 13** together, because they are the same surface twice — a completed-task view and a task search both refine the inbox #179 built, and splitting them across sprints would build the query twice. **FR-WF-005 (system tasks) → Sprint 14**, at the head of Phase 7 rather than in Phase 6: it is the driver an `AUTO` transition has been waiting for since Sprint 10, and `engine`'s own documentation names the paragraph the invocation goes in — but it belongs beside the rule engines of Phase 7, not beside attachments and comments. **Rejected: unscheduling any of them.** Each has a consumer that exists — an `AUTO` transition with no driver, an inbox that cannot show what it has finished — and **D-13** is the standing lesson that a `Should` sent to §7 without a return condition is one nobody comes back to. **Rejected: squeezing them into Sprint 12**, which opens Phase 6 with three MVP-gating epics; the sprint plan §7 row on Phase 6 being MVP-critical is the reason, and it has not changed. **§8's totals do not move**: all four were scheduled before and are scheduled after, so the reconciliation is untouched — stated here because §8's own rule asks for the counts in the same change, and the honest entry is that they did not change. Applied to §4 (four rows), §5 (FR-SRH-003's second row), and the [sprint plan](01.%20Sprint%20Plan.md) §5 Sprint 12 and 13. |
| **D-44** | **Phase 6 is planned to build an audit trail that shipped in Phase 2.** §4 targets FR-AUD-001 (formal audit events with actor, action, object) and FR-AUD-003 (append-only, hash-chained) at Sprint 12, and both have been in the product since Sprint 3: `audit_events` is `0003_audit.sql`, `modules::audit` writes it, `tests/audit_hash_chain.rs` holds the chain, and `0019` and `0022` have since corrected what the hash covers. FR-AUD-002's old/new snapshots landed with **D-12** and #135. Planning a sprint around work already delivered is how a phase discovers, mid-sprint, that it has nothing to do and something else undone. | **(2026-08-29) Three FR-AUD rows are marked delivered against the sprint that built them, and the one genuine gap is separated out and named.** FR-AUD-001, 002 and 003 are `Sprints 3–6 — built`. **FR-AUD-005 is the gap, and it is sharper than "unbuilt":** `audit_events.ip_address` exists, `middleware::client_address` resolves the caller's address and defends it against a spoofed `X-Forwarded-For`, the rate limiter consumes it — and **every one of the 53 audit call sites passes `None`.** The middleware's own module documentation says *"two things key off this value: the authentication rate limiter and the `ip_address` column on every audit row"*, which is half true and reads as wholly true. An audit column that is always null is worse than an absent one, because a reader takes it as evidence that the address was not available. It stays `Should` and moves to **Sprint 12**, where it is hours rather than an item. FR-AUD-004 (audit search) is genuinely unbuilt as a general surface — what exists is the master-data change-history read behind `master-data:audit:read`, which **D-12** deliberately scoped to one module — and stays at Sprint 13. **Rejected: leaving the rows alone and letting Sprint 12 discover it.** Sprint 11's plan corrected Sprint 10's FR-RAD-009 gap at exactly this point in exactly this way, and the reason given there holds here: a sprint whose items are already built is not a plan. **§8's totals do not move**: nothing changes between scheduled and unscheduled. Applied to §4 (five FR-AUD rows), §9 criterion 13, and the [sprint plan](01.%20Sprint%20Plan.md) §5 Sprint 12. |
| **D-45** | **A document's timeline discloses what the surfaces beside it protect.** [#292](https://github.com/sujanto-gaws/kelir/issues/292), from [record 12](../verifications/12.%20Sprint%2012%20Surface%20Read.md) §2: `activity::service::list_activity` requires `activity:read` and the document's own read and nothing else, and the events it returned carried, in `details_json`, an attachment's **original file name** and size, a comment's length, and the second party to a delegated decision. So a caller holding `activity:read` and `document:read` and neither `attachment:read` nor `comment:read` learned the name of every file on the document — and a file name is routinely the sensitive part: *2026-redundancy-list.pdf* needs no contents to do damage. Not a leak across tenants or documents, both of which are in the statement; a **missing second permission**, and [#263](https://github.com/sujanto-gaws/kelir/issues/263)'s shape one sprint on and pointing the other way — that was a screen showing too little to a caller lacking a permission, this was a surface showing too much to one. Two shapes were available and #292 declined to pick between them: **filter the entries** by what the caller may read, or **carry no subject detail** and let the timeline link by id. | **(2026-09-01) The timeline carries the link and not the subject.** **Nothing is lost, only relocated:** the file's name is in `attachments` behind `attachment:read`, the comment and its length in `comments` behind `comment:read`, the delegation's second party in `workflow_history` behind the workflow's read. The timeline was answering three other records' questions, which the four-record table in `modules::activity`'s header and [Database Schema](../../docs/design/02.%20Database%20Schema.md) §10 says it does not do — so this enforces that distinction rather than adding a rule. **Rejected: filtering the entries.** Three reasons, in order of weight. (1) *It has to be remembered.* Every module that adds an event type must extend the filter, and forgetting is silent — which is exactly what #292 **is**. An entry that has no field for a subject detail cannot forget. (2) *It hides the event, not the detail.* What a timeline is for is saying what happened to the document, and *a file was attached, by Sara, at 09:05* is the document's own history, already covered by `document:read`; only the **name** is the attachment's. (3) *It splits the page from its count.* #247 AC6 draws `meta.total` under the same predicate as the page; a per-reader filter needs that predicate in both statements or the two disagree, and a reader who counts learns what was filtered anyway. Redaction changes what an entry says and never whether it is there. **Applied in two halves, and they are not redundant.** The writers stopped producing the keys — `Attachment.Added`, `Attachment.Downloaded`, `Comment.Added` and `Workflow.TaskDelegated` carry `{}` and their link column, and `Workflow.Decided` keeps `action`, `from` and `to`, which are what moved *this document*, and drops `onBehalfOfUserId`. And `activity::domain::disclosable` holds it at the read, as an allow-list by event type serving `{}` for one it does not know, because a fix to a writer reaches no row already written and `activity_events` is append-only — every deployment that has run Sprint 12 holds rows carrying these names. `ActivityEvent` now serves `workflowInstanceId`, `taskId`, `attachmentId` and `commentId`, which #248 wrote and nothing read, so *link by id* is a thing a caller can actually do. **What this does not settle:** a deployment that wants the file name on the timeline for readers who *do* hold `attachment:read` has no way to ask for it. That is the rejected shape, per entry, and it is a feature request against FR-ACT-005's screen — which has the link and can go and ask — rather than a gap in this fix. Applied to `modules::activity` (`mod.rs`, `domain.rs`, `service.rs`, `repository.rs`), the four write sites in `modules::attachment`, `modules::comment` and `modules::workflow`, [Database Schema](../../docs/design/02.%20Database%20Schema.md) §10.1, and six tests in `document_activity.rs` and `workflow_delegation.rs`, five of which were seen red against a reintroduced defect. |

### 6.2 Open

| # | Question | Raised | Needed by |
|---|---|---|---|
| **D-15** | Two spike findings survive **D-10** and belong to whoever builds the rule engines: `conditional.logic` is governed by no registry (spike §2.6), so a schema can carry an operator set nothing bounds; and the `regex` validation rule cannot be honoured as specified by any Rust backend (§2.7), which **D-11** made explicit rather than fixed when it converted the notes to Rust. | **Sprints 14–16**, with the rule engines. Neither blocks Phase 4: the read-only renderer of FR-RAD-010 evaluates `calculate` and `conditional` expressions that a form definition already carries, and a definition carrying an unbounded `conditional` operator set is refused at save time by the registry's "not in this registry, therefore FORBIDDEN" rule, which **D-10**'s adoption keeps enforced. Recorded here so that resolving D-10 does not read as resolving the whole spike. |
| **D-38** | **The commit-message convention is checked on a branch and this repository squash-merges, so the header that lands on `main` is one nothing reads.** [`check-commit-messages.sh`](../../scripts/check-commit-messages.sh) runs on pull requests, over the branch's own commits. A squash takes the *pull request title* instead, which no job sees: four of the last eight headers on `main` are over the 72-character maximum, the longest at 117. **[Git workflow §4](../../docs/standards/05.%20Git%20Workflow.md) already answers which artefact governs** — *the PR title MUST follow the commit convention, it becomes the squash commit's header* — so this is not a question about the rule but about its enforcement: the rule is stated, unchecked, and violated in four of the last eight commits on `main`. The options are a job that lints the title, a merge queue that rewrites the header from the branch commit, or accepting that the rule is advisory and saying so where it is stated. | 2026-08-28, by the pipeline on the Sprint 10 pull request ([record 07](../verifications/07.%20Sprint%2010%20Surface%20Verification.md) F9) | **Before the `v0.5.0` tag.** A release note that quotes `main`'s history is the first reader of these headers, and the `v0.4.0` record already quotes two of the over-long ones. |
| **D-39** | **`user_roles.department_id` is nullable, and `DEPARTMENT_ROLE` had to decide what a null means before it could be enforced.** [#225](https://github.com/sujanto-gaws/kelir/issues/225) found the column written by the assignment resolver and read by nothing — no authorization check, no visibility rule — so a task offered to Finance's approver was decidable by any holder of that role anywhere. Enforcing it needs an answer for the grant that names no department, and both readings are defensible: *scoped-or-nothing* (a null grant satisfies only an unscoped task) is the stricter rule, and *a null grant is the role held generally* is the broader one. **(2026-08-28) The null grant satisfies a department-scoped task.** `0002` calls the column an *optional* department-scoped grant, and the natural reading of that is a role held generally rather than a role held nowhere — a scoped grant is the *narrowing*, so treating its absence as the narrowest case inverts the column's meaning. The stricter rule also fails on contact with the data: no deployment has ever populated this column, because nothing read it, so scoped-or-nothing would make **every** `DEPARTMENT_ROLE` task in every existing deployment undecidable the moment the fix shipped — stranding live approvals, which is the outcome [JWSS §5.3](../../docs/schema/JSON%20Workflow%20Schema.md) refuses two assignee types at save time to avoid (**D-37**). Applied in `repository::task::holds_role`, which carries the reasoning, and in the three candidate arms of `repository::inbox` so the queue and the decision answer identically. **What this does not settle** is whether a deployment should be able to *require* scoping — a tenant that wants unscoped grants to mean nothing has no way to say so, and that is a configuration question rather than a correctness one. |
| **D-40** | **A transition's `allowedBy` was not enforced, and enforcing it needs a choice: does it select the edge, or authorize the one already selected?** [#226](https://github.com/sujanto-gaws/kelir/issues/226) found it parsed, validated at save, projected to `workflow_transitions.allowed_by_json` and read by nothing. Where a state offers two transitions for one action, the engine already chooses between them by `condition` (S7, fallback last); adding `allowedBy` to that choice would make it a **selector** — an edge the caller may not take is skipped and the next one tried. Treating it as an **authorizer** refuses instead. **(2026-08-28) It authorizes; it does not select.** A selector routes silently: an approver who may not take the APPROVE edge would be sent down whatever comes next — a return, or a fallback — and the resulting record would read as their own decision rather than as a refusal they never saw. *A rejection routed as a return is a signature on the wrong document*, which is the sentence [#177](https://github.com/sujanto-gaws/kelir/issues/177) already earned one layer down. The authorizer also keeps `condition` and `allowedBy` doing one job each, which is how [JWSS](../../docs/schema/JSON%20Workflow%20Schema.md) §4 describes them. Applied in `engine::fire`, against the chosen transition, before the instance moves; `assignment::permits` does the resolution and is built on `resolve` so an edge and a task read the four assignee types identically — and inherit **D-39**'s department rule rather than restating it. **What this does not settle** is a state offering two same-action edges to *different* roles as a deliberate routing device. That now refuses for one of the two callers rather than routing, and if a workflow author wants the other behaviour the answer is a `condition`, not an `allowedBy`. |

The five decisions raised during the 2026-08-11 planning pass are resolved, as are the three that Sprint 4's verification pass raised against the Phase 2 record (**D-6**, **D-7**, **D-8**, all 2026-08-20), the standing environment question **D-9** and the backend-language question **D-11** (both 2026-08-21), the two the defect sweep of 2026-08-26 forced — **D-19** on what a `partyId` means over time and **D-20** on when to break the audit chain's hash format — the six Sprint 8's construction forced on the same day — **D-21** on where a numbering counter lives, **D-22** on a `sum` that totals nothing, **D-23** on where a frozen JFSS lets a lookup binding go, **D-24** on what a division by zero does, **D-25** on when a rendered form shows its verdict and **D-26** on what it does with a rule it cannot decide — the three its submit path forced the day after, **D-27** on the order the server re-evaluates in, **D-28** on what it does with a rule it is the right side for and has not built, and **D-29** on which 4xx the S10.3 envelope travels in, **D-30** on what re-pointing a document type at a new form does to the documents it already has, and **D-31** on how much of the forgot-password timing oracle is worth closing — and four taken on 2026-08-24 — the audit-permission question **D-12** from Sprint 6's verification pass, the carry-over disposition **D-13** at the sprint close, and at Sprint 7 planning the evaluator question **D-10**, open since 2026-08-21 and Phase 4's entry criterion, the browser-harness question **D-14** the Sprint 6 retrospective raised, and **D-16**, which gave Phase 4 a third sprint once that planning pass had counted what it actually held.

**§6.2 is not empty, and that is deliberate.** Resolving D-10 answers which evaluator Kelir takes; it does not answer the two spike findings that outlive it, so those are carried as **D-15** rather than closed by association. A spike with seven findings does not resolve because its headline one did.

New decisions are added here as they surface, resolved before the sprint that depends on them, and moved to §6.1 with the date and what was applied where. Decisions that change the release mapping or a phase boundary are then applied to the [sprint plan](01.%20Sprint%20Plan.md) — the plan is the operative document, this table is the reason it says what it says.

**A decision that supersedes another is greppable, and gets grepped before it is recorded** ([Sprint 7 retrospective](../retrospectives/05.%20Sprint%207%20Retrospective.md), from [#204](https://github.com/sujanto-gaws/kelir/issues/204)). Search the tree for the superseded decision's identifier — `D-7`, not "the tenancy decision" — and put what the search found in the new decision's **Applied to** list, including code: a `#[ignore]`, an `#[allow]`, a boot guard, a comment. **D-18** listed eleven documents and three issues and did not list the one `#[ignore]` **D-7** was holding down, because nothing enumerates what a decision is load-bearing for. The grep does, in one command, and its absence cost a security assertion that no run of the suite had ever made.

---

## 7. Unscheduled Backlog

No phase currently claims these. All are `Could` priority except where noted; none block the MVP.

**Both `Should` items D-13 put here on 2026-08-24 have since left it, and the mechanism worked.** Each named the event that returned it rather than a sprint: FR-IDM-006's condition was Phase 5 planning (**D-17**, 2026-08-24) and FR-ORG-001's was the decision that supersedes **D-7** (**D-18**, 2026-08-25). Neither was dropped and neither was carried a third time, which is what D-13 was written to prevent.

| FRs | Why unscheduled |
|---|---|
| FR-AUTH-007 | SRS §4.1 defers SSO/OIDC to a later phase explicitly |
| FR-ORG-003, 004, 005 | Positions, workgroups, hierarchy — no consuming feature yet |
| FR-RAD-012 | Metadata versioning — depends on a published/draft lifecycle not yet designed |
| FR-DTYPE-007 (purge behaviour) | The reference column lands in Phase 4; retention execution needs the lifecycle Purge stage |
| FR-WF-010, FR-WF-016 | Escalation and parallel approval; FR-WF-016 deferred by SRS §4.8 |
| FR-NTF-006, 007 | Reminders and escalation notifications depend on FR-WF-010 |
| FR-RPT-007, 008 | Workload report and CSV/Excel export |
| FR-SRH-004 | Global search — needs a cross-entity index decision |
| FR-INT-007, 008, 009 | Retry, master-data sync, file exchange |

## 8. Coverage Reconciliation

| Epic | FRs | Scheduled | Unscheduled |
|---|---|---|---|
| AUTH | 8 | 7 | 1 |
| IDM | 9 | 9 | 0 |
| ORG | 5 | 2 | 3 |
| MDM | 11 | 11 | 0 |
| RAD | 12 | 11 | 1 |
| DTYPE | 8 | 8 | 0 |
| DOC | 14 | 14 | 0 |
| WF | 16 | 14 | 2 |
| TASK | 9 | 9 | 0 |
| ATT | 10 | 10 | 0 |
| CMT | 7 | 7 | 0 |
| ACT | 5 | 5 | 0 |
| AUD | 5 | 5 | 0 |
| NTF | 7 | 5 | 2 |
| RPT | 8 | 6 | 2 |
| SRH | 4 | 3 | 1 |
| INT | 10 | 7 | 3 |
| PLG | 8 | 8 | 0 |
| API | 8 | 8 | 0 |
| **Total** | **164** | **149** | **15** |

All 40 NFRs (SRS §5) are covered by §4's NFR table. Every `Must`-priority FR is scheduled, and every FR named by an SRS §9 criterion lands at or before the `v0.6.0` MVP tag (§5).

**Scheduled moved three times, and two of the three were returns.** **D-13** sent FR-IDM-006 and FR-ORG-001 to §7 on 2026-08-24, taking scheduled from 149 to 147. **D-17** brought FR-IDM-006 back the same day with a Sprint 11 target, once its stated return condition — Phase 5 planning — was met, taking it to 148. **D-18** brought FR-ORG-001 back on 2026-08-25 against its own condition, the decision superseding **D-7**, so scheduled is **149** and unscheduled **15** (IDM 9 of 9; ORG 2 of 5, FR-ORG-001 joining FR-ORG-002). All three FRs are `Should` and none is named by an SRS §9 criterion, so the two claims above hold throughout: no `Must` ever moved, and MVP coverage is untouched. No FR was added or removed — 164 still reconciles.

**Both §7 return conditions have now been met and acted on**, which is the outcome D-13 designed for and the [Sprint 6 retrospective](../retrospectives/04.%20Sprint%206%20Retrospective.md) asked for: a deferral that names its trigger is a deferral somebody can discharge, and neither item was carried a third time.

## 9. Change Control

- Items enter and leave through backlog refinement, priced against the [sprint map](01.%20Sprint%20Plan.md#3-sprint-map). `Must` displaces `Should`, never the reverse.
- Adding a requirement means adding it to the SRS first, then to an epic here — this document never originates scope.
- Update the coverage reconciliation (§8) in the same change that moves an item between scheduled and unscheduled; a stale total is how coverage claims rot.
- Re-verify §5 against SRS §9 at every phase boundary and before tagging the MVP release.
