# Release v0.5.0 — 2026-08-30

**Status:** Draft · **Last updated:** 2026-08-30

**Phase 5 — Workflow.** **Tagged `v0.5.0` at `c91433a` on 2026-08-30.** Opened by the rehearsal ([#242](https://github.com/sujanto-gaws/kelir/issues/242)), which the [release process](../../docs/standards/04.%20Release%20Process.md) §6 asks be run at the close rather than at the tag, so that a failure is a finding instead of a hotfix; completed by [#243](https://github.com/sujanto-gaws/kelir/issues/243).

> **Still `Draft`, and the reason is one row.** The tag is pushed and the record is otherwise complete, but **the images have not been built from the tag** — that needs Docker, which the session doing this does not have, and CI has no tag trigger to do it instead. That row says *not done* rather than being left blank, and this record becomes `Final` when it is filled. An empty row reads as "passed" to everyone who comes later, which is the distinction [record 04](04.%20Release%20v0.4.0.md) and §6 both insist on.

> **`v0.5.0` is not the MVP.** **D-1** puts that at `v0.6.0`, at the end of Phase 6 — attachments, comments, the activity timeline, notifications and audit search are not in this release. Five of the fifteen SRS §9 acceptance criteria are Phase 6 work, so Phase 5 delivers end-to-end approvals and does not satisfy §9. It is a real release and an approval-workflow milestone; it is not the thing a reader meeting *"approvals, end to end"* is likely to assume it is.

---

## Pre-flight

| Step | Check | Evidence |
|---|---|---|
| Version bumped | **`0.5.0`** in `kelir-backend/Cargo.toml`, `Cargo.lock`, `kelir-frontend/package.json` and `package-lock.json`, bumped together per §1. `feat` commits since `v0.4.0` and no `!`/`BREAKING CHANGE` → **minor** | this pull request |
| Changelog curated | `[Unreleased]` → **`[0.5.0] — 2026-08-30`**. Three corrections it needed: the migration count read **six** and there are **seven** (`0030` arrived with [#259](https://github.com/sujanto-gaws/kelir/issues/259)); the *Known limitations* list still said delegation, conditional routing and due dates were not built, and all three shipped in this release; and a `Fixed` section was missing entirely for the five defects the exit steps found | `CHANGELOG.md` |
| It says what it is not | **`v0.5.0` is not the MVP**, stated in the notes rather than left to the roadmap. **D-1** puts the MVP at `v0.6.0`; a reader meeting *"approvals, end to end"* will otherwise assume this is it | `CHANGELOG.md` |
| Migration review | **Thirty migrations, sequential `0001`–`0030`, no gaps.** Seven are new since `v0.4.0`. **(a) empty database:** all thirty applied by the `v0.5.0` migrator against a throwaway PostgreSQL 18 → `_sqlx_migrations` holding **30 successful rows, highest version 30**. **(b) N−1: passed in both halves** — below | rehearsed 2026-08-30 |
| Exit demo | **Run, not described** — 13 browser flows against the release stack CI builds, including the three items Sprint 11 had never driven | [record 10](../verifications/10.%20Phase%205%20Exit%20Demo.md) |
| Independent pass | **Partial and recorded as partial.** Four findings, all closed: [#259](https://github.com/sujanto-gaws/kelir/issues/259), [#260](https://github.com/sujanto-gaws/kelir/issues/260), [#263](https://github.com/sujanto-gaws/kelir/issues/263), [#264](https://github.com/sujanto-gaws/kelir/issues/264) | [record 09](../verifications/09.%20Sprint%2011%20Independent%20Pass.md) |

---

## The rollback rehearsal

**§6's procedure, all three steps, run on 2026-08-30.** The `v0.4.0` backend knows migrations `0001`–`0023`. It was started against a database the `v0.5.0` migrator had just taken to `0030`.

| Check | Result |
|---|---|
| `v0.5.0` migrates an empty database | **30 of 30 applied**, highest version 30 |
| `v0.4.0` **source compiles** against that schema, SQLx online | **Yes** — `cargo build --release` with `SQLX_OFFLINE=false` against the migrated database. **This is §6 step 3, and it is the schema half proven by the compiler**: SQLx verifies every query at build time, so `v0.4.0` compiling means every statement it holds still type-checks against `v0.5.0`'s schema |
| `v0.4.0` binary starts against a `0030` schema | **Yes.** `database migrations applied` — `set_ignore_missing` (the [#76](https://github.com/sujanto-gaws/kelir/issues/76) fix) tolerates the seven versions it has never heard of. This is the bookkeeping half |
| `/health`, `/health/ready`, `/health/live` | **200, 200, 200** |
| `/version` on the rolled-back binary | `{"version":"0.4.0","commit":"7401094",…}` — the tag's own commit |
| It writes, not just starts | **Yes.** It created the bootstrap administrator on that schema |
| `GET /api/v1/workflow/definitions` | **404** — the workflow surface is correctly absent from `v0.4.0`, which is what a rollback means |
| `GET /api/v1/documents` | **401** — `v0.4.0`'s own surface is present and asking for a token |
| `0024`'s index on the migrated schema | `uq_mdm_party_roles_party_id_role_type_id ON (party_id, role_type_id) WHERE deleted_at IS NULL` — present and partial, as `0024` intends |

**Why it passed.** Six of the seven new migrations are additive — new tables `v0.4.0` never saw, and two nullable columns on one of them — so there is nothing for the older binary to be wrong about. `0030` **drops** a `CHECK`, which only widens what the table accepts. The seventh, `0024_one_live_role_per_party.sql`, is the one this step exists for, and it is treated separately below.

### What was not rehearsed, and it is the part that matters most

**`0024`'s N−1 safety still rests on a claim, and this rehearsal did not test it.**

The claim, from the changelog: *`v0.4.0`'s only writer into `mdm_party_roles` holds the party row under `FOR UPDATE` and updates in place, so it cannot reach the violation the tightened index now refuses.* [#242](https://github.com/sujanto-gaws/kelir/issues/242) says plainly that this claim *"is exactly what the rehearsal tests"*.

What this rehearsal established is weaker, and the difference is worth being exact about:

- **Proven:** the index exists on the migrated schema; the `v0.4.0` binary starts, migrates and writes against that schema; and every SQL statement `v0.4.0` holds — `assign_role`'s included — still type-checks against it.
- **Not proven:** that `assign_role`, *executed* on the rolled-back binary, cannot produce two live rows for one party and role type. Driving it takes a party, a role type, a `fromDate` and a role profile through the API, and the attempt was abandoned after several wrong payload shapes rather than run to a result.

**Recorded as a gap rather than left blank**, because a blank row reads as "passed" to everyone who comes later — §6's own warning, and the reason `v0.2.0` shipped unrollable.

**It does not block the tag.** The failure mode it guards against is a *pre-existing* duplicate: the migration refuses to apply at all where two live rows already exist, naming every offending pair, and no released binary can have written one. What is untested is the narrower question of whether a rolled-back `v0.4.0` could create one afterwards. **The cheapest way to close it is not more curl**: run `v0.4.0`'s own `master_data_party_roles` suite — 39 tests, `assign_role` exercised exhaustively — against a `v0.5.0`-migrated database. That needs the harness to accept a pre-migrated database rather than provisioning its own, which it does not today.

---

## Ship

| # | Step | Check | Evidence |
|---|---|---|---|
| 1 | CI green on the tagged commit | **Verified before tagging.** "Backend (Rust)", "Frontend (Vue)" and "End-to-end (browser)" all `success` on `c91433a`. "Commit messages" is `skipped` by design — it runs only on pull requests, and it passed on #276 | [run 33322629591](https://github.com/sujanto-gaws/kelir/actions/runs/33322629591) |
| 5 | Annotated tag `v0.5.0` | **Done.** Tag object `0b8b9e4` on `c91433a`, its message the `[0.5.0]` changelog section **verbatim** — 446 lines | `git rev-list -n1 v0.5.0` → `c91433a8bc0d6ad46fd5c7263bbdf1592d24ab5f` |
| 6 | Images built from the tag | **NOT DONE.** Needs Docker; CI has no tag trigger. See below | — |
| 7 | Staging deploy + smoke tests | **Stood in for, and by what is named below** — there is no staging host (**D-9**) | [record 10](../verifications/10.%20Phase%205%20Exit%20Demo.md) |
| 8 | Production deploy verified via `/version` | **Not applicable.** No production environment and none scheduled (**D-9**) | — |

### The tag message was nearly mangled, and it is worth knowing why

`git tag -F` **strips every line beginning with `#` as a comment** unless `--cleanup=verbatim` is given. The first attempt lost the `## [0.5.0]` heading and all four `###` section headings — ten lines, silently, from a 446-line message. It was caught by counting the headings in the tag against the source, the tag was deleted and remade verbatim, and nothing was pushed in between.

**`v0.4.0`'s record says its message was the changelog section "in full — 622 lines, verbatim"**, so the convention was already right; what was missing was the flag that makes it true.

### Checklist items 7 and 8, dispositioned rather than left blank

[#243](https://github.com/sujanto-gaws/kelir/issues/243) AC5: both rows are blocking, and two blocking rows left empty read as passed.

- **Item 7, staging deploy and smoke.** There is no staging host and none scheduled; **D-9** replaced the clause with *"demoable from the Docker Compose stack built from release images"*. What stands in its place: CI's `End-to-end (browser)` job builds the release images, brings the stack up with `deploy-local.sh`, smoke-tests `/version` against the version it deployed, and drives **13 browser flows** against it — including the Phase 5 exit demo. **What it does not do is build from the tag**, which is item 6's gap and not this one's.
- **Item 8, production deploy.** Not applicable, for the same decision. The compose check stands in its place and **does not serve TLS**, so NFR-SEC-010 remains unexercised — as it has been since `v0.1.0`.

### What is outstanding: images from the tag

The rehearsal and the demo both ran against release images, but **built from a branch rather than from `v0.5.0`**. That distinction is not a formality: `KELIR_BUILD_SHA` is baked in at build time and is what `/version` reports, so images from the tag are the only ones whose `/version` proves which commit is running. `v0.4.0`'s record rebuilt from the tag and re-verified against it for exactly this reason, and called the rehearsal images "rehearsal artefacts" that were replaced.

To close it, from a checkout of the tag:

```bash
git checkout v0.5.0
SHA=$(git rev-parse --short HEAD)      # c91433a

docker build -f deploy/docker/backend.Dockerfile \
  --build-arg KELIR_BUILD_SHA="$SHA" \
  -t kelir-backend:0.5.0 -t kelir-backend:"$SHA" kelir-backend

docker build -f deploy/docker/frontend.Dockerfile \
  -t kelir-frontend:0.5.0 -t kelir-frontend:"$SHA" kelir-frontend

cd deploy/staging && KELIR_HOST_IP=127.0.0.1 ./deploy-local.sh 0.5.0 18080
curl -s http://127.0.0.1:18080/version   # must report 0.5.0 and c91433a
```

**A tag trigger in `ci.yml` would remove this gap for every later release**, and is not in this one's scope.
