# Release v0.6.0 — 2026-09-04

**Status:** In progress · **Last updated:** 2026-09-04

**Phase 6 — the MVP.** **Not tagged.** The release commit is on `main` at `0c4ad18` ([#331](https://github.com/sujanto-gaws/kelir/pull/331)), the rollback rehearsal is run and passed, and the [close plan](../planning/09.%20Phase%206%20MVP%20Close%20Plan.md) §5 step this record opens with is complete. What remains is §6 — the tag, images built from it, and the smoke pass those images make possible.

> **Two things stand between this record and `Final`, and only one of them is work.**
>
> The first is §6: an annotated `v0.6.0`, images built **from the tag** with `KELIR_BUILD_SHA` supplied, and the smoke pass run against them. [Record 05](05.%20Release%20v0.5.0.md) was `Draft` for two days on exactly that row, and its note says why an empty row is worse than a *not done* one.
>
> ~~The second is the decision close plan §7 names.~~ **Settled 2026-09-04, before the tag and not by silence** — sprint plan §2 gained an owner, a fixed point and a checkable criterion, which is the first of the two branches the [Sprint 13 retrospective](../retrospectives/11.%20Sprint%2013%20Retrospective.md)'s first action offered. *What the tag rests on* below is the record that action asked for.

> **`v0.6.0` is the MVP**, and it is said here as plainly as [record 05](05.%20Release%20v0.5.0.md) said `v0.5.0` was not. **D-1** has placed the MVP at `v0.6.0` since 2026-08-11. SRS §9 is met in full — fifteen criteria, verified one at a time against a running deployment rather than against this project's own coverage table, in [MVP Verification](../verifications/14.%20MVP%20Verification.md). **Fourteen rest on evidence produced by executing them; the fifteenth is accepted on a stated basis rather than ticked** — criterion 4, *administrators can configure document types*, is served by the API and by no screen, and **D-64** records both the decision and what it obliges of these release notes.

---

## Pre-flight

| Step | Check | Evidence |
|---|---|---|
| Version bumped | **`0.6.0`** in `kelir-backend/Cargo.toml`, `Cargo.lock`, `kelir-frontend/package.json` and `package-lock.json`, bumped together per §1. `feat` commits since `v0.5.0` and no `!`/`BREAKING CHANGE` → **minor** | `aa5b5b0`, merged as `0c4ad18` |
| Changelog curated | `[Unreleased]` → **`[0.6.0] — 2026-09-03`**. Three things it needed beyond the heading: it opened *"Phase 6 opens"*, which is not the heading of a closing phase; it carried **two `### Changed` sections and two `### Fixed`**, from entries appended across the phase, now merged and ordered as `0.1.0`–`0.5.0` are (**42 bullets before, 42 after — nothing recategorised**); and the link references had never gained a `[0.5.0]` row while `[Unreleased]` still compared from `v0.4.0` | `CHANGELOG.md` |
| It says what it **is** | **`v0.6.0` is the MVP**, stated in the notes rather than left to the roadmap — the inverse of the sentence **D-1** obliged `v0.5.0` to carry. The notes also say that **document types are configured through the API**, with the builders scheduled for Phase 7, which is what **D-64** obliges | `CHANGELOG.md` |
| Scope verified | **SRS §9, fifteen criteria, one at a time against a running deployment.** Fourteen on executed evidence, criterion 4 accepted on a stated basis. Two findings, neither blocking | [record 14](../verifications/14.%20MVP%20Verification.md) |
| Independent pass | **All ten Sprint 13 items, independent of every one of them.** Three findings, all fixed and merged ([#321](https://github.com/sujanto-gaws/kelir/pull/321)) | [record 13](../verifications/13.%20Sprint%2013%20Independent%20Pass.md) |
| Tests green | **All five checks green on the release commit** — backend, frontend, end-to-end, commit messages, pull-request title | [run 33781870377](https://github.com/sujanto-gaws/kelir/actions/runs/33781870377) |
| Migration review | **Thirty-nine migrations, sequential `0001`–`0039`, no gaps.** Nine are new since `v0.5.0`. **(a) empty database:** all thirty-nine applied by the v0.6.0 migrator against a throwaway PostgreSQL 16 → `_sqlx_migrations` holding **39 successful rows, highest version 39**. **(b) N−1: passed** — below | rehearsed 2026-09-03 |
| OpenAPI current | **Nothing to drift.** The document is derived from the handlers at compile time by `utoipa` and served at `/api/docs/openapi.json` (`router.rs:382`); there is no committed artifact that can fall behind the surface, and `router.rs` tests the endpoint | `kelir-backend/src/router.rs` |
| Docs current | The convention's §1 says where the title check runs (**D-65**), the backlog carries **D-64** and **D-65**, and [record 14](../verifications/14.%20MVP%20Verification.md) is `Final` | `0c4ad18`, `23344bc`, `e08c52e` |

---

## The rollback rehearsal

**[Release process](../../docs/standards/04.%20Release%20Process.md) §6, run on 2026-09-03.** The `v0.5.0` backend knows migrations `0001`–`0030`. It was started against a database the v0.6.0 migrator had just taken to `0039`.

| Check | Result |
|---|---|
| v0.6.0 migrates an empty database | **39 of 39 applied**, highest version 39 |
| `v0.5.0` binary starts against a `0039` schema | **Yes**, `/health/ready` **200 in 1 second**. `set_ignore_missing` (the [#76](https://github.com/sujanto-gaws/kelir/issues/76) fix) tolerates the nine versions it has never heard of. This is the bookkeeping half |
| `/health`, `/health/ready`, `/health/live` | **200, 200, 200** |
| `/version` on the rolled-back binary | `{"version":"0.5.0","commit":"c91433a",…}` — the tag's own commit |
| `_sqlx_migrations` after the N−1 run | Still **39 rows, all `success`**. No checksum complaint: v0.6.0 edited nothing `v0.5.0` had already applied |
| It writes, not just starts | **Yes.** Given `KELIR_BOOTSTRAP_ADMIN_*`, it created the bootstrap administrator on the `0039` schema |
| `GET /api/v1/documents` | **401** — `v0.5.0`'s own surface is present and asking for a token |
| `GET /api/v1/audit` | **404** — audit search is `0035`'s surface and correctly absent from `v0.5.0` |
| `GET /api/v1/documents/{id}/attachments` | **404** — as is the attachment surface |
| `GET /api/v1/documents/{id}/comments` | **404** — and the comment surface. That is what a rollback means |
| Startup log | Clean. One WARN, that no bootstrap administrator exists on an empty database, before the variables above were supplied |

**Why it passed, and the one migration this step existed for.** Eight of the nine are invisible to `v0.5.0`: `0032`, `0033`, `0034`, `0037` and `0038` create tables it has never heard of; `0036` alters `comments`, which `0032` created; and `0035` and `0039` carry **no DDL at all** — permission rows, notification channels and templates.

**`0031_attachment` is the only one touching a table `v0.5.0` knows**, and it adds `fk_document_type_attachment_rules_category_id` to `document_type_attachment_rules`, which `0015_document` created in Sprint 9. Close plan §5 argued the exposure was smaller than it looks. **That argument is now checked rather than repeated:**

| The claim | What was measured |
|---|---|
| `v0.5.0`'s source references `document_type_attachment_rules` nowhere | `git grep document_type_attachment_rules v0.5.0 -- kelir-backend/src` returns **nothing** |
| So no release has ever written a row into it | The migrated table holds **0 rows** |
| So the constraint can reject nothing | It exists on the schema (`contype = 'f'`) and the `v0.5.0` binary starts, migrates and **writes** against that schema |

**A constraint can only reject rows that exist.** There are none, and no released binary contains the code that would make one.

### What was not rehearsed

**§6's optional third step — the compiled check — was not run.** The step reads *"if it is a compiled check you want as well: the previous release's source compiling against the migrated database proves every query it holds still type-checks."* [Record 05](05.%20Release%20v0.5.0.md) ran it for `v0.4.0` and called it the schema half proven by the compiler.

It is **not run here**, and this row says so rather than being left blank — §6's own warning, and the reason `v0.2.0` shipped unrollable. What stands in its place is weaker and worth being exact about:

- **Proven:** the `v0.5.0` binary starts, migrates, writes and serves its own surface against a `0039` schema; the Phase 6 surfaces are absent from it; and the one migration touching a table it knows constrains a table its source never names and no release has written to.
- **Not proven by a compiler:** that every SQL statement `v0.5.0` holds still type-checks against the `0039` schema. The evidence above is behavioural rather than exhaustive.

**It does not block the tag**, on the same reasoning the table above sets out: the only schema change reaching `v0.5.0`'s world is a foreign key on an empty table its source does not mention. **The cheapest way to close it** is `cargo build --release` with `SQLX_OFFLINE=false` at tag `v0.5.0` against a `0039` database — the procedure [record 05](05.%20Release%20v0.5.0.md) used, which needs a full Rust build this session did not run.

---

## Release build

> The environment is the Docker Compose stack brought up from the release images (`deploy/staging/deploy-local.sh`), not a staging host: there is none, and none is scheduled. **D-9** replaced the staging steps rather than waiving them.

| Step | Check | Evidence |
|---|---|---|
| Stack up from release images | **Not done.** The images do not exist yet; §6 builds them from the tag | — |
| Smoke pass | **Not done**, and it cannot be run before the images exist — see the note below | — |
| Rollback rehearsed | **Passed**, 2026-09-03 | *The rollback rehearsal* above |

> **`kelir-backend:0.6.0-rc` is mislabelled, and it is not the image this row wants.** It reports `{"version":"0.5.0","commit":"6392e43"}` — it was built from `main` on 2026-09-03 at 11:48Z, before the bump, so the tag on it is aspirational.
>
> **It was sound for the rehearsal and is not sound for the smoke pass.** Sound for the rehearsal because `db.rs:89` embeds `migrations/` at compile time, the image was built after the last migration-touching commit (`a67836a`, 2026-09-02), and the database it produced proves the point directly: 39 of 39. Not sound for the smoke pass because that step's whole content is `/version` **reporting the expected version and SHA**, and this image reports neither.
>
> §6 calls the `KELIR_BUILD_SHA` build argument the row most easily forgotten. This is how it gets forgotten.

---

## Ship

| Step | Check | Evidence |
|---|---|---|
| Tag pushed | **Not done.** Annotated `v0.6.0` on `0c4ad18`, message = the `[0.6.0]` changelog section | — |
| Images from the tag | **Not done.** `kelir-backend:0.6.0` and `kelir-frontend:0.6.0`, also tagged with the SHA, `KELIR_BUILD_SHA` supplied | — |
| Changelog | **Done.** `[0.6.0] — 2026-09-03`, curated per §4 step 4 | `CHANGELOG.md` |
| Production deploy | **Not applicable.** There is no production environment; **D-9** retired the clause rather than leaving it looking passed | — |
| Post-deploy verification | **Not applicable**, for the same reason. The compose smoke pass above stands in for it | — |

---

## What the tag rests on

> **Sprint plan §2 was settled on 2026-09-04, before the tag, and this is the record naming what the tag rests on.**

**The first branch was taken, not the default.** The [Sprint 13 retrospective](../retrospectives/11.%20Sprint%2013%20Retrospective.md)'s first action offered two: an owner and a date in §2, or the `author-verified` label. §2 now carries the first — **the project manager dispatches the independent pass at mid-sprint refinement, and the reader is any session appearing in no construction commit trailer in the sprint's range.**

**The criterion is the part that matters.** It is the one [record 13](../verifications/13.%20Sprint%2013%20Independent%20Pass.md) used, and it is checkable with `git log` rather than asserted — which is what the same retrospective's second action asks of every rule: *an action names the artefact outside itself that will fail.* Five sprints of this row failed because they named a future document's own compliance.

**So the tag rests on:**

| What | Evidence |
|---|---|
| An independent pass over the whole Sprint 13 surface | [Record 13](../verifications/13.%20Sprint%2013%20Independent%20Pass.md) — all ten items, the reader in no construction commit, three findings, all fixed |
| SRS §9 met in full | [Record 14](../verifications/14.%20MVP%20Verification.md) — fourteen on executed evidence, criterion 4 accepted on a stated basis (**D-64**) |
| The N−1 rollback | *The rollback rehearsal* above — passed, with the one gap it did not cover stated |
| §2 firing without an instruction | **From Sprint 14 onward.** Record 13 was directed rather than triggered; the rule that fixes that is dated 2026-09-04 and has not yet had a sprint to fire in |

**The last row is the honest one.** §2 now says the right thing and has never once been exercised as written. Sprint 14's close is the first test of whether naming an owner was enough, and the retrospective for it is where that gets read — not here.

---

## Aftermath

- **Issues found post-release:** not yet released.
- **Follow-ups filed:**
  - **`Pull-request title` is not among `main`'s required checks.** The check moved out of the `Commit messages` job in [#330](https://github.com/sujanto-gaws/kelir/pull/330) (**D-65**), and branch protection required it *through* that job. Until the new context is added, a title violation no longer blocks a merge — **D-38**'s original problem, by another route.
  - [#322](https://github.com/sujanto-gaws/kelir/issues/322), [#323](https://github.com/sujanto-gaws/kelir/issues/323), [#324](https://github.com/sujanto-gaws/kelir/issues/324) — Sprint 14's allowance; [record 13](../verifications/13.%20Sprint%2013%20Independent%20Pass.md) §7 says why none blocks the tag.
  - [#301](https://github.com/sujanto-gaws/kelir/issues/301) — dropping `activity:read`, which the §6 N−1 rule puts **after** this tag.
  - [#274](https://github.com/sujanto-gaws/kelir/issues/274) — unscheduled and still unreproduced; its first criterion is now satisfiable because the harness captures the log.
  - **Logout and the roles screen have no browser coverage** — [record 14](../verifications/14.%20MVP%20Verification.md) §6 finding 2, carried to Sprint 14.

Mark this document's header **`Status: Final`** when the tag, the images and the smoke pass are done.
