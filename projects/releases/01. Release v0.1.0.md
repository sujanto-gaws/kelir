# Release v0.1.0 — 2026-08-12

**Status:** In progress · **Last updated:** 2026-08-12

**Phase:** 1 Foundation (SDD §14) · **Release manager:** project owner

> **Cut, not verified.** Checklist steps 1–6 are done; **step 7, the staging deploy and smoke tests, is blocking and outstanding** because `kelir-staging-01` is not provisioned. This release exists as a tag and a pair of images; it has not run anywhere but a developer machine. Phase 1 stays open until that step passes.

## Pre-flight

| Step | Check | Evidence |
|---|---|---|
| Scope verified | Phase 1 issues #2–#11 closed and merged; #12 open | [Phase 1 milestone](https://github.com/sujanto-gaws/kelir/milestone/1) — 10 of 11 closed |
| Tests green | CI green on `main` at `67e4b7e`: backend fmt, clippy `-D warnings`, 37 tests, release build; frontend lint, format, type-check, 23 tests, build | [Actions](https://github.com/sujanto-gaws/kelir/actions) |
| Migration review | One migration, no gaps. Applied to an empty PostgreSQL 16 database on 2026-08-12: `_sqlx_migrations` holds one row, `tenants` and `system_settings` created, system tenant seeded. **(b) is not applicable** — there is no previous release to migrate from, and the N−1 rule first bites at `v0.2.0` | Verified against a throwaway container |
| Docs current | Link check clean across changed documents; all 37 documents carry a status from the vocabulary; indexes current | `f785a26`, `67e4b7e` |
| Version determined | First release, so `0.1.0` by §1. `Cargo.toml` and `package.json` already carried `0.1.0`, so no bump was needed — this release commit adds the changelog and this record instead | this commit |
| OpenAPI current | Generated at `/api/docs/openapi.json`; 4 paths, 7 component schemas, matching the endpoints that exist | verified on a running instance |

## Staging

| Step | Check | Evidence |
|---|---|---|
| Deployed to staging | **Outstanding — blocking.** No host exists | issue #12 |
| Smoke pass | **Outstanding.** Ran against a local IP deployment of the same images instead: `/health`, `/health/live`, `/health/ready` green, `/version` reporting `0.1.0`, SPA served with deep-route fallback, `/api/v1` reaching the backend | local run, 2026-08-12 |
| Rollback rehearsed | **Not applicable** — there is no previous release to roll back to. First rehearsal is due at `v0.2.0` |  |

## Ship

| Step | Check | Evidence |
|---|---|---|
| Tag pushed | `v0.1.0`, annotated, on the release commit | see below |
| Changelog | [CHANGELOG.md](../../CHANGELOG.md), curated by hand from the commit log | this commit |
| Production deploy | **Not applicable** — no production environment exists (SDD §13.2 is a sketch) |  |
| Post-deploy verification | **Outstanding**, follows the staging deploy |  |

## Aftermath

- **Issues found post-release:** none yet — the release has not run in a deployed environment.
- **Follow-ups filed:**
  - **#12 staging** — provision `kelir-staging-01`, point `staging.kelir.gawshub.com` at it, run `provision-ubuntu-24.sh` then `deploy.sh 0.1.0`, and complete steps 7 and 9 above.
  - **Approve the SRS and SDD** — SDD §16 step 1. Until then those two documents and the database schema remain `Draft`, which is the last inaccuracy in the documentation set.
  - **Publish images to a registry** so deploys pull rather than build on the host (release process §4.1).

Mark this document `**Status:** Final` when the release is verified — for this release, when the staging deploy and smoke tests pass.
