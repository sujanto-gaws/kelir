# Release v0.1.0 — 2026-08-12

**Status:** Final · **Last updated:** 2026-08-21

**Phase:** 1 Foundation (SDD §14) · **Release manager:** project owner

> **Settled 2026-08-21. Phase 1 closes.** Step 7 read "deploy to staging" and was blocking and outstanding, because `kelir-staging-01` was never provisioned and no infrastructure was ever available to provision it.
> Decision **D-9** retires that environment and replaces the step with the Docker Compose stack brought up from the release images — and **this release already passed that check on 2026-08-12**, recorded below as a substitute for the deploy it could not do: the same images, brought up locally, health endpoints green, `/version` reporting the tag's commit, the SPA served with deep-route fallback and `/api/v1` reaching the backend.
> Nothing is retro-verified here. The evidence was gathered at the time and the step it satisfies has since been rewritten to name what was actually done.

## Pre-flight

| Step | Check | Evidence |
|---|---|---|
| Scope verified | Phase 1 issues #2–#11 closed and merged; #12 open | [Phase 1 milestone](https://github.com/sujanto-gaws/kelir/milestone/1) — 10 of 11 closed |
| Tests green | CI green on `main` at `67e4b7e`: backend fmt, clippy `-D warnings`, 37 tests, release build; frontend lint, format, type-check, 23 tests, build | [Actions](https://github.com/sujanto-gaws/kelir/actions) |
| Migration review | One migration, no gaps. Applied to an empty PostgreSQL 16 database on 2026-08-12: `_sqlx_migrations` holds one row, `tenants` and `system_settings` created, system tenant seeded. **(b) is not applicable** — there is no previous release to migrate from, and the N−1 rule first bites at `v0.2.0` | Verified against a throwaway container |
| Docs current | Link check clean across changed documents; all 37 documents carry a status from the vocabulary; indexes current | `f785a26`, `67e4b7e` |
| Version determined | First release, so `0.1.0` by §1. `Cargo.toml` and `package.json` already carried `0.1.0`, so no bump was needed — this release commit adds the changelog and this record instead | this commit |
| OpenAPI current | Generated at `/api/docs/openapi.json`; 4 paths, 7 component schemas, matching the endpoints that exist | verified on a running instance |

## Release build

| Step | Check | Evidence |
|---|---|---|
| Stack up from release images | **Passed.** A local deployment of `kelir-backend:0.1.0` and `kelir-frontend:0.1.0` — the tagged images, not a working-tree build. Recorded at the time as a substitute for the staging deploy; **decision D-9 (2026-08-21) makes it the step itself**, because no staging host exists or is scheduled | local run, 2026-08-12 |
| Smoke pass | **Passed.** `/health`, `/health/live`, `/health/ready` green, `/version` reporting `0.1.0`, SPA served with deep-route fallback, `/api/v1` reaching the backend. Sign-in was not exercised: Phase 1 had no authentication to exercise | local run, 2026-08-12 |
| Rollback rehearsed | **Not applicable** — there is no previous release to roll back to. First rehearsal is due at `v0.2.0` |  |

## Ship

| Step | Check | Evidence |
|---|---|---|
| Tag pushed | `v0.1.0`, annotated, on `8a85b10` | `git show v0.1.0` |
| Changelog | [CHANGELOG.md](../../CHANGELOG.md), curated by hand from the commit log | this record's commit |
| Images built from the tag | `kelir-backend:0.1.0` and `:8a85b10` (146 MB); `kelir-frontend:0.1.0` and `:8a85b10` (89 MB). Both tagged with version and SHA per release process §4 step 6 | built 2026-08-12 |
| Artifact reports its build | The backend image answers `/version` with `{"version":"0.1.0","commit":"8a85b10",…}` — the commit matches the tag, which is what the smoke test compares | verified against a throwaway PostgreSQL |
| Images pushed to a registry | **Not applicable** — no registry is configured; deploys build on the host (release process §4.1) |  |
| Release announced | **Outstanding.** The working token cannot create GitHub Releases (403); publish from the tag in the UI. Non-blocking (checklist item 9) |  |
| Production deploy | **Not applicable** — no production environment exists (SDD §13.2 is a sketch) |  |
| Post-deploy verification | **Not applicable** — nothing is deployed to observe. The release-build check above is what stands in its place (**D-9**) |  |

## Aftermath

- **Issues found post-release:** none at the time. Two surfaced later, at the `v0.2.0` release-build check, and both predate this record: the backend image became unbuildable in Sprint 3, and the release compose file never passed the bootstrap credentials. Neither was visible here — Phase 1 had no query macros to break the build, and no authentication whose absence would be noticed.
- **Follow-ups filed:**
  - ~~**#12 staging**~~ — **closed 2026-08-21 as not planned.** No infrastructure was ever available for `kelir-staging-01`; decision **D-9** retires the environment and replaces the step, which this release had already satisfied in substance.
  - **Approve the SRS and SDD** — SDD §16 step 1. Until then those two documents and the database schema remain `Draft`, which is the last inaccuracy in the documentation set.
  - **Publish images to a registry** so deploys pull rather than build on the host (release process §4.1).

Marked `Final` on 2026-08-21. The release-build check it waited on had in fact passed on 2026-08-12; what changed is that the checklist now names the environment that was used, rather than one that has never existed.
