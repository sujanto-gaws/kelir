# Release v0.7.0 — 2026-09-10

**Status:** Draft · **Last updated:** 2026-09-10

**Phase:** Phase 7 — RAD Engine ([SDD](../../docs/design/01.%20System%20Design%20Document.md) §14) · **Release manager:** Sujanto

> **This record is `Draft` and cannot go `Final` yet.** The independent-pass row below is outstanding, and `releases_are_independently_verified.rs` refuses a `Final` record that cites no verification record no earlier release has cited. That is [#392](https://github.com/sujanto-gaws/kelir/issues/392) working as designed on the first release it governs — **the run stops here rather than tagging around it.**

## Pre-flight

| Step | Check | Evidence |
|---|---|---|
| **Independent pass** | **A verification record for work in this release exists in [`projects/verifications/`](../verifications/) and is cited in this record — one no earlier release cited.** Held by `releases_are_independently_verified.rs`, so a record cannot go `Final` without it (§2, [#392](https://github.com/sujanto-gaws/kelir/issues/392)) | **Outstanding.** The directory ends at [record 14](../verifications/14.%20MVP%20Verification.md), and every record from `v0.3.0` on is spent — see *The blocking row* below |
| Scope verified | All committed FR IDs traceable and verified (`requirements-analyst`) | **Not started** — belongs with the pass |
| Tests green | Full suite passes in CI, including integration and E2E (`test-engineer` sign-off) | `main` at [`aa3af07`](https://github.com/sujanto-gaws/kelir/commit/aa3af07), five required contexts green on [#403](https://github.com/sujanto-gaws/kelir/pull/403) — 1,477 backend tests across 74 targets at the last full-suite count. **The post-merge `main` run for this tree failed and the failure is not the code**: [run 34384164582](https://github.com/sujanto-gaws/kelir/actions/runs/34384164582) died in `End-to-end (browser)` at *Install the browser* — `E: Failed to fetch https://dl.google.com/linux/chrome-stable/deb/…/Packages.gz  Hash Sum mismatch`, an apt index fetch, exit 100 — six minutes after the same tree passed E2E on #403 in 6m45s. **Step 1 wants a green `main` run and does not have one**, so this row is not satisfied until the next merge to `main` produces one. `test-engineer` sign-off belongs with the pass |
| Migration review | Migrations sequential, no gaps; apply cleanly to (a) empty DB and (b) previous release's schema; N−1 compatible (release process §6) | **Reviewed, not yet rehearsed** — see *Migration review* |
| Docs current | Consistency and schema gates pass; `Last updated` dates and `docs/README.md` index current | **Not started** |
| Version determined | From commit log per release process §1; `Cargo.toml` + `package.json` bumped in one `chore(release): vX.Y.Z` commit | **`v0.7.0`** — 46 commits since `v0.6.0`, no `!` or `BREAKING CHANGE` marker, nine `feat`, so §1's rule gives a minor bump. Bumped in `chore(release): v0.7.0` |
| OpenAPI current | Spec regenerated and matches the API surface | **Not started** |

## The blocking row

**The gate is not a formality on this release; it is unmet, and it is the first release it could be unmet on.** `releases_are_independently_verified.rs` asserts that a `Final` record cites a verification record **that no earlier release cited**. The four governed releases have spent every record in the directory:

| Release | Cites |
|---|---|
| `v0.3.0` | [03. Sprint 6 Surface Verification](../verifications/03.%20Sprint%206%20Surface%20Verification.md) |
| `v0.4.0` | [06. Sprint 9 Surface Verification](../verifications/06.%20Sprint%209%20Surface%20Verification.md) |
| `v0.5.0` | [09. Sprint 11 Independent Pass](../verifications/09.%20Sprint%2011%20Independent%20Pass.md) · [10. Phase 5 Exit Demo](../verifications/10.%20Phase%205%20Exit%20Demo.md) |
| `v0.6.0` | [13. Sprint 13 Independent Pass](../verifications/13.%20Sprint%2013%20Independent%20Pass.md) · [14. MVP Verification](../verifications/14.%20MVP%20Verification.md) |

**So record 15 has to be written, and by somebody who did not write the work.** [Sprint plan](../planning/01.%20Sprint%20Plan.md) §2's criterion is *any session appearing in no construction commit trailer in the sprint's range* — verifiable with `git log`, which is what makes it a control rather than a statement about diligence.

**This is the outcome [#392](https://github.com/sujanto-gaws/kelir/issues/392) was filed to produce.** Eight sprints asked for the pass at mid-sprint refinement and got it none of those times; the ninth attaches it to the tag instead. The row being outstanding here — visibly, in the document that cannot close — is the control working, not the release failing.

## Migration review

**Three migrations since `v0.6.0`, sequential from `0039` with no gaps.**

| Migration | What it does | N−1 |
|---|---|---|
| `0040_rad_menu_permissions.sql` | Inserts the four `rad:menu:*` permissions and grants them to `ROLE-ADMIN` | **Additive.** Rows `v0.6.0` reads as catalogue and checks in no route |
| `0041_activity_read_dropped.sql` | Deletes the `activity:read` grants, then the permission row | **Compatible, and this is the release it waited for.** `v0.5.0`'s binary called `caller.require("activity:read")`; `v0.6.0`'s does not, so the N−1 for this migration reads the row in no statement. Deprecate in N, remove in N+1 — release process §6, followed to the letter |
| `0042_rad_action_scoped_to_a_list.sql` | Adds nullable `rad_actions.list_id`, a composite FK, a `CHECK`, a partial index, and a unique constraint on `rad_lists (id, tenant_id)` | **Additive and nullable.** `v0.6.0` never selects the column; the null preserves the old behaviour, so no data migration had to guess a list for an existing tenant-wide action |

**Reviewed by reading, not yet rehearsed.** Release process §6 is explicit that the rehearsal is the evidence and the reasoning is not — *rehearse the rollback, don't reason about it* — and the two halves (empty DB migrated with the new binary; `v0.6.0`'s binary reaching `/health/ready` against it) belong to *Release build* below, which has not run.

## Release build

> The environment is the Docker Compose stack brought up from the release images
> (`deploy/staging/deploy-local.sh`), not a staging host: there is none, and none
> is scheduled. Decision **D-9** replaced the staging steps rather than waiving
> them — the property being checked is that what ships is what was verified.

| Step | Check | Evidence |
|---|---|---|
| Stack up from release images | Immutable images, migrations run by the backend at startup, nothing built from the working tree | **Not started** |
| Smoke pass | Canonical E2E scenario green against that stack; `/version` reports the released version and SHA | **Not started** |
| Rollback rehearsed | Previous image boots against migrated schema — both halves of the N−1 rule, by the procedure in [release process](../../docs/standards/04.%20Release%20Process.md) §6. Record a failure here rather than leaving it blank | **Not started** |

## Ship

| Step | Check | Evidence |
|---|---|---|
| Tag pushed | `v0.7.0` on the release commit | **Not pushed.** Held behind the independent pass — see *The blocking row* |
| Changelog | Generated per commit convention | [`CHANGELOG.md`](../../CHANGELOG.md) `## [0.7.0]`, curated from the `[Unreleased]` section the phase filled as it went, with upgrade notes for the two permission changes and the pattern refusal |
| Production deploy | Per release process §4 | **No production environment** (**D-9**) |
| Post-deploy verification | Health endpoints, version endpoint, smoke check | **Not started** |

## Aftermath

- **Issues found post-release:** <none yet — the release has not shipped>
- **Follow-ups filed:** none from the run so far. One issue was filed from the sprint's last row and is not a release finding:
  - [`#404` — §2.9 never gained the stated-versus-run mutation rule it was ordered to](https://github.com/sujanto-gaws/kelir/issues/404) · **reproduced** · found while building [#395](https://github.com/sujanto-gaws/kelir/issues/395); the missing sentence is verifiable by `grep -nE '\bdated\b|\bSeen red\b'` over the coding standard, which returns nothing, and [#376](https://github.com/sujanto-gaws/kelir/issues/376) asserts the rule exists

> **A follow-up filed from a release run says whether its mechanism was *reproduced* or *only observed*.** One of the two words, on every row — the rule [record 06](06.%20Release%20v0.6.0.md)'s Aftermath earned, carried by the template since [#382](https://github.com/sujanto-gaws/kelir/pull/382).

Set this document's header status to `Final` when the release is verified in production.

> **The header line is written out rather than quoted, deliberately.** `releases_are_independently_verified.rs` decides whether a record is governed by asking whether its body contains the header string — so a `Draft` record quoting that string verbatim — which is what copying the checklist template's closing line produced, until [#406](https://github.com/sujanto-gaws/kelir/pull/406) took it out there — would enrol itself in the gate and fail rule 2 against citations it was only listing. The template is not governed because its file name carries no version; this record is, which is why the trap was invisible in the file that carried it. **A control that reads a document is a control a document can trip**, which is worth knowing at the one release where the gate is load-bearing.
>
> **Seen red, 2026-09-10**, rather than reasoned about: restoring the quoted header string to that line reddens `each_release_brings_a_pass_no_earlier_release_cited`, naming this record and all six citations it would then be making — *every record it names was written for work that shipped before it, so nothing read this release*. Reverted, and the suite is green.
