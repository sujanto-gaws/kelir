# Sprint 19 and Sprint 17 Independent Pass — 2026-09-17

**Status:** Final · **Last updated:** 2026-09-17

**Subject:** Phase 8's two unread sprints, as **D-85** asked for them. **Sprint 19**'s construction is `9f3d38c..9cfadb3`, [construction plan 15](../planning/15.%20Sprint%2019%20Phase%208%20Close%20Construction%20Plan.md). **Sprint 17**'s is `00a27b4..ae3baa0`, [construction plan 13](../planning/13.%20Sprint%2017%20Reporting%20Construction%20Plan.md) and [status report 18](../status/18.%20Sprint%2017%20Status.md). Sprint 19 is named first in the file name because `sprint_reports_are_verified.rs` reads only the first `Sprint N` (§4).

| Sprint | PR | Issue | What |
|---|---|---|---|
| 19 | [#476](https://github.com/sujanto-gaws/kelir/pull/476) | [#475](https://github.com/sujanto-gaws/kelir/issues/475) | `check-commit-messages.sh` refuses a Claude commit with no session line (**D-84**) |
| 19 | [#477](https://github.com/sujanto-gaws/kelir/pull/477) | [#465](https://github.com/sujanto-gaws/kelir/issues/465) | Validation Rule Registry 1.5.1, ADR-0038, the upgrade note |
| 19 | [#478](https://github.com/sujanto-gaws/kelir/pull/478) | [#449](https://github.com/sujanto-gaws/kelir/issues/449) | The outbox is Phase 9, in five places |
| 19 | [#479](https://github.com/sujanto-gaws/kelir/pull/479) | [#467](https://github.com/sujanto-gaws/kelir/issues/467) | Release gate rule 10 hardened |
| 19 | [#481](https://github.com/sujanto-gaws/kelir/pull/481) | [#461](https://github.com/sujanto-gaws/kelir/issues/461) | FR-RPT-006, the approval time card (**D-83**) |
| 19 | [#473](https://github.com/sujanto-gaws/kelir/pull/473), [#474](https://github.com/sujanto-gaws/kelir/pull/474), [#480](https://github.com/sujanto-gaws/kelir/pull/480) | — | Plan 15, D-83/84/85, bookkeeping |
| 17 | [#436](https://github.com/sujanto-gaws/kelir/pull/436) | [#431](https://github.com/sujanto-gaws/kelir/issues/431) | The dashboard summary endpoint, `reporting:dashboard:read`, migration `0043`, the shell, ADR-0039 |
| 17 | [#437](https://github.com/sujanto-gaws/kelir/pull/437) | [#432](https://github.com/sujanto-gaws/kelir/issues/432) | The pending-tasks widget (FR-RPT-002) |
| 17 | [#438](https://github.com/sujanto-gaws/kelir/pull/438) | [#433](https://github.com/sujanto-gaws/kelir/issues/433) | The recent-documents widget (FR-RPT-003), migration `0044` |
| 17 | [#435](https://github.com/sujanto-gaws/kelir/pull/435) | [#412](https://github.com/sujanto-gaws/kelir/issues/412) | Release gate rules 5–9 |
| 17 | [#430](https://github.com/sujanto-gaws/kelir/pull/430), [#439](https://github.com/sujanto-gaws/kelir/pull/439), [#422](https://github.com/sujanto-gaws/kelir/pull/422)–[#425](https://github.com/sujanto-gaws/kelir/pull/425), [#427](https://github.com/sujanto-gaws/kelir/pull/427)–[#429](https://github.com/sujanto-gaws/kelir/pull/429), [#434](https://github.com/sujanto-gaws/kelir/pull/434) | — | e2e CI's apt sources, coding standard §2.9, MinIO from quay.io, planning, operations docs, hygiene |

> **The pass ran the full backend suite, the frontend's five CI steps and all 24 browser tests. It drove the dashboard as four viewers, and ran `0044`'s N−1 half against the real `kelir-backend:0.7.0` image.** It also probed the pattern dialect on both engines, and all three gates with synthetic inputs, each beside a control that failed.
>
> **Seven findings. One blocks `v0.8.0`, and it is a sentence in the release's upgrade notes.** Finding 1: Registry 1.5.1 and the `CHANGELOG`'s upgrade note give authors a pattern subset they call *safe*, and a negated class inside it splits the two engines. Findings 2 to 5 are four more cases of a control accepting inputs its text says it refuses. The release gate accepts an old record under another spelling, and it only checks a record once the tag already exists. The session-line check can be satisfied by a quoted line, and rule 10 misses a follow-up under a `*` bullet. Finding 6 is a task offered to a deleted role, which stays *waiting for you* and cannot be decided. Finding 7 is a browser flow that asserts less than its title.
>
> **What held up is most of the product code.** The approval time's arithmetic, window and exclusions reproduced, including an even count through the endpoint and the 90-day edge to the minute. The first three widgets' scoping held everywhere this pass looked except finding 6. `0044` is N−1 safe against the real `0.7.0` image. #478 left no live *Phase 8* for the outbox. The upgrade note's account of the publish path is accurate.

---

## 1. The reader, checked before reading

**This reader is `f26c8233-84da-442b-9e7c-a86e7ed0f195`.** The product owner started it as a top-level Claude Code session in its own worktree (`D:\Projects\kelir-record-17`), with a prompt naming the brief. It was not dispatched as a subagent and it dispatched none, so no reading here was done under another session's ID.

| Check | Output |
|---|---|
| `$env:CLAUDE_CODE_SESSION_ID` | `f26c8233-84da-442b-9e7c-a86e7ed0f195` |
| Where the transcript is | `~/.claude/projects/D--Projects-kelir-record-17/f26c8233-….jsonl`, a top-level session file. `1d834a2a-…`'s `subagents/` folder holds four agents, `a6bab…`, `a7578…`, `a8d11…` and `a903d…`, and none of them is this session |
| `git log --all --format='%h %b' \| grep -c f26c8233` | `0` before this change |
| `git log --format='%h %s%n%b' 00a27b4..ae3baa0 \| grep -oE '^[0-9a-f]{7} \|session_[A-Za-z0-9]+\|Claude-Session: [0-9a-f-]{36}'` | 14 commits and **seven sessions**: `session_01KNGfREqVDAoRS5pCwisFGw` (`ae3baa0`, `f0d9c76`, `e73eab4`), `session_01HaPdqdF8qiJGbCeoYDBvXn` (`c5e4a74`, `879d497`), `session_012HG5QWd2HRPfhneYfaUQfP` (`c7bd626`, `97207ab`), `session_0137WfomNyNhN9PdttKnXbBv` (`6eb2654`), `session_01B62ZYL28hnRAbE8RWgXsuv` (`6f7063e`, `3ed903e`), `session_016sr3GCtawqX4MRz4d8hW73` (`e41bd7b`, `f1d8a47`), `session_016hyi2f8k9fVUySrBv58Hyp` (`5c69ffa`, `9647833`). **This ID is absent** |
| The same over `9f3d38c..9cfadb3` | 8 commits, `f7ac45e` to `9cfadb3`, **one session**: `Claude-Session: 1d834a2a-7eed-483a-84d2-149758e94129` on every commit, three times on `7fdb3b0` and twice on `9ee780f` and `9cfadb3`. **This ID is absent** |
| Every commit in either range whose body names a Claude co-author and carries no `session_…` or `Claude-Session:` line | **None.** Every commit in both ranges is co-authored by Claude, and every one names its session |

**So §2's criterion holds for all 22 commits.** It holds as a fact and not just as an absence of matches: every commit names a session, and none of them is this one.

**One point of contact, stated as record 16 §7.1 stated its own.** The brief was written by `1d834a2a-…`, which wrote all of Sprint 19, and it sits in that session's scratchpad. It names the leads this pass followed. That is a task, not reasoning, but it frames the task. **Finding 3 is in none of the brief's leads.** Every other finding answers a lead (the pattern texts, the gates, the scoping, #481's e2e spec), but no lead names the construct, spelling, marker or role it turned on. Each came from a probe run here.

---

## 2. Method

### 2.1 The surface, measured first

| Surface | Sprint 17 (`00a27b4..ae3baa0`) | Sprint 19 (`9f3d38c..9cfadb3`) |
|---|---|---|
| Whole diff | 46 files, +7,088 / −330 | 40 files, +2,764 / −145 |
| Backend source | 11 files, +1,345: `reporting` (4), `workflow` inbox repository and service, `document` list, `activity::domain::TOUCH_EVENT_TYPES`, router | 13 files, +399: `workflow::domain::approval_time`, `repository::instance::decided_document_seconds`, `reporting`, `rad::domain::validation` (comments), three outbox comments |
| New `sqlx::query!` statements | 2 (the recent-documents join; the inbox gains `count(*) OVER ()`) | 1 (`decided_document_seconds`) |
| Migrations | **2**: `0043_reporting.sql` (a permission, a grant, an index) and `0044_recent_documents.sql` (an index) | 0 |
| `kelir-frontend/src` | 4 files, +1,304: the dashboard page and its spec, types, API client | 7 files, +372: the approval card, `duration.ts`, `validation.ts` (comments) |
| E2E | **0 specs**, README only | 1 spec, +53 lines of support |
| Controls | Release gate rules 5–9 (628 lines changed); `narrow-apt-to-ubuntu.sh` | Rule 10 (653 lines changed); the session-line rule (73 lines changed) |

**So the dimensions were:**
1. The dashboard's scoping, against a second user, a lost grant, a deleted role and the grant alone.
2. #481's arithmetic and window, through the endpoint.
3. `0044`'s N−1 half, against the real image.
4. The three gates, attacked with synthetic inputs.
5. Document claims checkable from the tree.
6. The browser.

### 2.2 Executed

**Databases.** The shared `kelir` database has a checksum mismatch on migration 42 and lacks 43–44. So this pass created **`kelir_r17`** (all 44 migrations) for the suite and the probes. It created **`kelir_n1`** for P5 and **`kelir_e2e`** for P6–P8. `kelir_n1` and `kelir_e2e` were dropped afterwards. The test harness makes its own `kelir_test_<uuid>` databases from `kelir_r17`'s connection.

**Backend, the full suite at `9cfadb3`:** `cargo test --no-fail-fast` gave **78 targets, 1,593 passed, 0 failed, 2 ignored, exit 0**. **This is identical to CI**: run [`35131500938`](https://github.com/sujanto-gaws/kelir/actions/runs/35131500938), a `push` to `main` at `9cfadb3`, `Backend (Rust)` job log summed to 78 results, 1,593 passed, 0 failed, 2 ignored. `End-to-end (browser)` and `Frontend (Vue)` also succeeded there. **The four earlier Sprint 19 pushes to `main` (`e3646e3`, `8630bfe`, `1dbae68`, `9ee780f`) were cancelled by concurrency**, so `9cfadb3`'s run is the only full CI result on `main` for #481.

**Frontend, all five CI steps locally:** `lint`, `format:check` and `type-check` green. `vitest run` **56 files, 690 passed**. `build` green, and `check:bundle` green with *"10 chunk(s) on first load"*.

| Probe | What it is | Result | Its control |
|---|---|---|---|
| **P1** | 11 patterns, node v24.15.0 `new RegExp(p, f).test(v)` against `regex` 1.13.1 built as `compile_pattern` builds it; four also posted as `regex` rules to `POST /api/v1/rad/forms` | **Six of the eleven decide one input two ways, and all three posted return `201`** (finding 1) | `^[0-9]{4}$` against `1234` and `١٢٣٤`, `^a$` against `a\n`, and flag `g`: **agree**. `^\d{4}$` at save: **422** |
| **P2** | Synthetic `08. Release v0.8.0.md` records written into `projects/releases/`, each run alone against the built `releases_are_independently_verified` binary, then deleted. Baseline 21 passed with nothing planted | **Three spellings of an old or foreign citation pass all 21** (finding 2) | Record 15 cited plainly: **2 failed** (rules 2 and 7). Record 16 cited plainly: 21 passed |
| **P3** | Scratch commits in a throwaway repository, `check-commit-messages.sh` run on each | **Five messages without a real session line accepted** (finding 4) | A Claude co-author and no line: **refused**. A UUID line: accepted |
| **P4** | Rule 10's `follow_up_block`, `row_opens_with_issue_link`, `says_none` and `read_aftermath`, extracted verbatim from the test file and compiled, fed `record_with_follow_ups` shapes with record 07's two rows | **Four list markers hide an unfiled row** (finding 5) | The unfiled row as `  - ` under the filed row: **refused** |
| **P5** | `kelir-backend:0.7.0` started on a fresh database (migrates to 42), a form, type, document and comment written through it, then `0043` and `0044` applied **under the running image**, reads and a write repeated, the image restarted and repeated again | **N−1 holds** (§4) | `GET /api/v1/dashboard/summary` on the image: **404**, so the calls reached `0.7.0` |
| **P6** | Two probe tests appended to `tests/reporting_dashboard.rs`: a role-offered task whose role is deleted; an even count, and the 90-day edge ± 60 s, through `GET /dashboard/summary` | **The deleted role's task stays waiting** (finding 6). The median and edge **held** (§4) | The same task shape with the grant expired: **0 waiting**. Mutation D2 (lower middle) applied to `approval_time.rs`: the even-count probe printed **86,400** instead of 129,600. Restored |
| **P7** | All 16 E2E spec files against a local stack: the backend binary at `9cfadb3` on `kelir_e2e`, the bundle built with `VITE_KELIR_API_BASE_URL=/api/v1` behind Caddy on the staging Caddyfile's routes | **24 passed** | The approval card mutated two ways and rebuilt: **both passed** (finding 7). Count `+ 1`: **failed**. Restored and rebuilt |
| **P8** | The dashboard driven as four seeded users by a Playwright script, full-page screenshots read: no grant; the grant and nothing else; a requester with an approved, a rejected, a returned-then-resubmitted-then-approved, a waiting and a draft document; and their approver | §4 | — |

**Why a script, not the browser extension, for P8.** The browser extension this session drives may not type passwords into a sign-in form. The seeded users' passwords are throwaway values on a local stack, but they are still passwords, so P8 used the Playwright harness the E2E suite already uses. Screenshots were read, as record 16's P7 did.

**An environment note, not a finding.** The first E2E run failed 24 of 24 at sign-in. Git Bash had rewritten `VITE_KELIR_API_BASE_URL=/api/v1` into `C:/Program Files/Git/api/v1` in the bundle. The bundle was rebuilt from PowerShell, and the second run is the one reported.

**Baseline and cleanup.** `main` at `9cfadb3` throughout. Both probe tests, the planted release records, the E2E probe spec, the mutated `approval_time.rs` and `DashboardPage.vue` and the throwaway repository were removed or restored (`git checkout --`). The probe container, the Caddy container and the backend binary were stopped, and `kelir_n1` and `kelir_e2e` dropped. The `kelir-staging` and `cosmetic-deploy` stacks already running on this machine were not touched. `git status` was clean before this record was written.

**Read in full:** the diffs of #435–#438, #476–#479 and #481, and #481's description; `reporting::{service, handlers, domain, mod}`; `workflow::repository::inbox::list_for_caller` and `service::inbox::{waiting_work, late_work}`; `document::repository::list::{count_own_by_status, list_recent_touched}` and their services; `activity::domain::TOUCH_EVENT_TYPES`; `workflow::domain::approval_time`, `repository::instance::decided_document_seconds` and `service::instance::approval_time`; `InstanceOutcome::from_document_status`; `DocumentStatus::may_move_to`; `rad::service::form::{publish_form, create_revision}`; `compile_pattern` and `refuse_pattern`'s callers; migrations `0043` and `0044`; `check-commit-messages.sh`; `narrow-apt-to-ubuntu.sh`; rules 1–10 of `releases_are_independently_verified.rs`; rule 1 of `sprint_reports_are_verified.rs`; `duration.ts` and the approval card; the new E2E spec; the Registry 1.5.1 note; ADR-0039's and ADR-0040's amendments; the `CHANGELOG` upgrade note; D-83, D-84 and D-85; #475; the release checklist template's Pre-flight and Ship tables.

---

## 3. Findings

### Finding 1: Registry 1.5.1 and the upgrade note call a pattern subset safe, and it is not. **Blocking for `v0.8.0`, as a claim, not as code. [#482](https://github.com/sujanto-gaws/kelir/issues/482), `sprint:19`.**

**`^[^,]{1,3}$` rejects two emoji in the browser and accepts them on the server, and it is built only from what the registry recommends.**

#477 corrected 1.5.0's claim of closure, which was record 16 finding 2's charge, and tabulated the divergent constructs. It then added: *"write a `both`-scoped pattern from ASCII literals, written-out classes, `^`, `$` without `m`, and the ordinary quantifiers"*. The `[Unreleased]` upgrade notes send deployments to the registry for *"the subset that is safe"*. P1:

| Pattern | Flags | Value | ECMA-262 | `regex` crate | Saved |
|---|---|---|---|---|---|
| `^[^,]{1,3}$` | — | two emoji | false | **true** | **201** |
| `^[^a]$` | — | one emoji | false | **true** | — |
| `abc` | `y` | `xabc` | false | **true** | **201** |
| `abc` | `q` | `abc` | **SyntaxError**, so every value fails | true | **201** |
| `^[a-z]+$` | `i` | U+017F | false | **true** | — |

- **A negated written-out class is inside the subset**, and it counts UTF-16 units in the browser and scalar values on the server. `[^,]` (*anything but a comma*) is about as ordinary as a written-out class gets.
- **The flags are nowhere in the table.** `compile_pattern` reads `i`, `m` and `s` and ignores every other letter. ECMA-262 anchors under `y`, counts differently under `u`, and throws on a letter it does not know.
- **`i` over an ASCII literal** is in the table (*case folding under `i`*) and not excluded from the subset, so the two sentences disagree.

**Why blocking.** These sentences are what `v0.8.0`'s upgrade notes will say to a deployment. [Record 15](15.%20Sprint%2016%20Independent%20Pass.md) finding 2 held `v0.7.0` for a release note that told a deployment something false. Record 16 finding 2 was graded not blocking because it corrected an overstatement no deployment had yet been handed. This one gives a deployment advice to follow. **The discharge is text**: #482's *Done when*. No refusal has to change for the tag.

### Finding 2: release gate rules 2 and 7 accept an old record under another spelling. **Not blocking. [#483](https://github.com/sujanto-gaws/kelir/issues/483).**

P2. Each synthetic record was `Final` and in house shape, with `- **Follow-ups filed:** none`, and was run alone:

| The one citation | Result |
|---|---|
| **Control** `../verifications/15.%20Sprint%2016%20Independent%20Pass.md`, which record 07 already cites | **2 failed**: `each_release_brings_a_pass_no_earlier_release_cited`, `each_release_cites_a_record_written_after_every_earlier_citation` |
| `../verifications/./15.%20Sprint%2016%20Independent%20Pass.md` | **21 passed** |
| `../verifications/%31%35.%20Sprint%2016%20Independent%20Pass.md` | **21 passed** |
| `../verifications/../releases/07.%20Release%20v0.7.0.md` | **21 passed** |

**Rule 2** compares raw strings, so `./15…` is new. **Rule 7** reads the number off the raw name. `record_number` returns `None` for all three, and a release with no numbered citation reaches `continue` and is not judged. **Rule 6** decodes and resolves, so every one exists. **This is [record 15](15.%20Sprint%2016%20Independent%20Pass.md)'s door A and finding 7 reopened by spelling**, the two #435 was built to shut. **Why not blocking:** the control shows the honest shape is caught, and record 08 is written by a person citing records 16 and 17 plainly. Its citations should still be read by hand.

### Finding 3: the release gate turns red after the tag, not before it. **Not blocking. [#484](https://github.com/sujanto-gaws/kelir/issues/484).**

The test file opens *"A release does not tag without a pass that read the work in it"*. The gate governs a record whose header says `Final`, and a record goes `Final` after the tag:

| Release | Tag created | Record went `Final` |
|---|---|---|
| `v0.6.0` | 2026-09-04 09:24:40 +07 | `5fb7f1c`, 10:03:16 |
| `v0.7.0` | 2026-09-12 00:15:10 +07 | `00a27b4` (#419, *"v0.7.0 is tagged, and record 07 is final"*), 09:25:56 |

That is the prescribed order. The checklist template puts *Tag pushed* first under *Ship*, and its Pre-flight table has no row for the verification record. Nothing reads `git tag`. **So what held `v0.7.0` for record 15 was a person reading a `Draft` record, not the test.** [Retrospective 14](../retrospectives/14.%20Sprint%2016%20Retrospective.md)'s *"the pass ran because the tag could not be cut without it"* is a dated record and is left as written. The live claim is the test's first line. Its *What a record can still do* names the reverse, *go `Final` without the tag ever being pushed*, and not this.

**Why not blocking.** D-85 holds `v0.8.0` by decision, not by the test, and this record exists before the tag. **What it means for the run**: nothing mechanical stops `v0.8.0` being tagged before finding 1 is discharged. The release manager is the control.

### Finding 4: the session-line check can be satisfied without a session. **Not blocking. [#485](https://github.com/sujanto-gaws/kelir/issues/485).**

P3, controls first: a Claude co-author with no line was **refused**, and a UUID line was **accepted**.

| Message | Result |
|---|---|
| `Co-Authored-By: Opus 5 (1M context) <noreply@anthropic.com>`, no line | **accepted**: the rule keys on the word `claude`, not on Anthropic's address |
| `Co-authored-by: Anthropic Assistant <assistant@anthropic.com>`, no line | **accepted** |
| `Co-Authored-By : Claude <…>`, which git's trailer parser still reads as a co-author | **accepted** |
| **Another session's line quoted inside a fenced code block**, no line of its own | **accepted** |
| `Claude-Session: 00000000-0000-0000-0000-000000000000` | **accepted** |

**The squash is not checked at all.** `Commit messages` runs on `pull_request` over the branch commits. The commit on `main` is the squash, composed at merge and editable, and §2's criterion reads `main`. D-84 rejected a check on `main` because *"a passing branch commit's text survives the squash"*. That is true only when nobody edits the squash message. The last UUID row is inherent to a string check, and #475 never claimed otherwise.

**Why not blocking.** §1 shows every commit in both ranges names a real session. The gaps are ways the next range could fail silently, and this range did not.

### Finding 5: rule 10 misses an unfiled follow-up under any other list marker. **Not blocking. [#486](https://github.com/sujanto-gaws/kelir/issues/486).**

P4, under the answer `two:` and record 07's filed row:

| The unfiled row written as | `read_aftermath` |
|---|---|
| **Control** `  - ` | **refused**, quoting it |
| `  * ` | **accepted** |
| `  + ` | **accepted** |
| `  2. ` | **accepted** |
| `  -` then a tab | **accepted** |

`follow_up_block` starts a row only at an indented line opening `- ` or `~~`, and joins every other indented line to the row above. #479 wrote that join for record 16's shape D, a wrapped sentence. **It also swallows a sibling list item**, and CommonMark renders all four as separate items. None of the six stated limits covers it. **Why not blocking:** the house writes `- `, and the control shows that shape is caught.

### Finding 6: a task offered to a deleted role stays *waiting for you*, and cannot be decided. **Not blocking. [#487](https://github.com/sujanto-gaws/kelir/issues/487).**

P6. A holder has the dashboard grant on one role and a queue on a second. A document is submitted to the queue, and the queue role is deleted with `DELETE /api/v1/identity/roles/{id}` (**204**):

| | Before | After the delete |
|---|---|---|
| `tasksWaiting` / `pendingTasks` rows / `GET /api/v1/tasks` | 1 / 1 / 1 | **1 / 1 / 1** |
| **Control**: a second holder whose queue grant expired yesterday instead | 1 | **0** |
| Deciding the task as the holder | — | **422** `ASSIGNMENT_UNRESOLVED`, *"`R17-QUEUE` is not a live role in this tenant"* |

`soft_delete_role` stamps `roles.deleted_at` and leaves `user_roles` live. `list_for_caller`'s `EXISTS` over `user_roles` never reads the role. **The inbox's defect predates Sprint 17.** #437 took *whose task is this* from the inbox statement by design, which is why the widget and the inbox agree here, and why the card that every session opens shows work nobody can do. Record 16's P5 tried an expired grant and not a deleted role.

**Why not blocking.** Deleting a role that still has open tasks strands those tasks whatever the dashboard says. The widget reports that state rather than causing it.

### Finding 7: #481's browser flow cannot tell a right duration from a wrong one. **Not blocking. [#488](https://github.com/sujanto-gaws/kelir/issues/488).**

P7's control on `the-dashboard-says-how-long-approval-takes.spec.ts`:

| Mutation to the card | Spec |
|---|---|
| The median cell renders `slowestSeconds` | **passed** |
| Every time renders `under a minute` | **passed** |
| **Control**: the count renders `documents + 1` | **failed** |

The median and slowest assertions match `/^(under a minute|\d+ min|\d+ h( \d+ min)?)$/`, and every seeded decision lands seconds after its submission, so both cells read *under a minute* whatever the card computes. **The rules are held by K1–K10 in the unit specs**, so nothing is untested. What is wrong is the flow's claim: its title says it checks *how long*. #481's AC5 asked that a flow **reach** the card, and it does.

---

## 4. What was read and cleared

- **The approval time is right where D-83 put it.**
  - **The arithmetic.** An even count through the endpoint, 1 day and 2 days, returned a median of **129,600 s**. Mutation D2 made the same probe return 86,400, so #481's note that D2 is caught only by a unit test is accurate, and the endpoint agrees with the unit.
  - **The window.** A decision 90 days minus 60 s ago was counted and one 90 days plus 60 s ago was not. `>=` includes the edge, and the harness session's `TimeZone` is `UTC`. `make_interval(days => 90)` is calendar days in the session's zone, so a server in a zone with daylight saving moves the edge by an hour twice a year, which does not matter here.
  - **Returned rounds and exclusions.** P8's returned document produced **one instance** (`COMPLETED`/`APPROVED`), which confirms plan 15 §1.2's corrected account that a return does not start an instance. The card counted it at *1 min*, the returned round included. The exclusions for a `CANCELLED` last instance, a document in flight, a second tenant, a soft delete and another author are each an integration test with a second subject. They were read, not repeated.
  - **Rounding.** `EXTRACT(EPOCH …)::bigint` rounds each document's time to the nearest second. The doc says only that the median is rounded down, which is accurate.
  - **Formatting.** `duration.ts` floors both units and drops a zero smaller unit, and its spec covers the boundaries.
- **The first three widgets' scoping held everywhere but finding 6.**
  - The committed tests plant a second tenant, a second user, soft-deleted documents and tasks, a department-scoped task and a delegated task, and a caller holding only `reporting:dashboard:read` (`the_summary_asks_for_no_permission_but_its_own`, `the_widget_asks_for_no_document_read`). They assert on the rows returned.
  - Record 16's P5 already moved late tasks out one exclusion at a time through the same `WHERE`.
  - `list_recent_touched` carries `a.tenant_id = d.tenant_id` on the join, excludes `Attachment.Downloaded` by the `TOUCH_EVENT_TYPES` list, and matches the document list's `WHERE` as #433 AC3 asked. Neither statement reads `document_types.deleted_at`, so they agree there too.
  - P8's approver saw the three documents they decided in *What you touched last*, and not the one still waiting for them.
- **`0044` and `0043` are N−1 safe (P5).** `kelir-backend:0.7.0` served sign-in, permissions (70 rows, then 71 with `reporting:dashboard:read`), the administrator role, documents, a comment write and a document timeline, **before and after `0043`/`0044` were applied under it and after a restart**. Startup logged no error, because `db.rs` sets `set_ignore_missing(true)`. The dashboard route answered 404, so the calls reached the old image.
- **P8, the dashboard by hand.**
  - The viewer without the grant gets *"Your account cannot see the dashboard summary. Ask an administrator for reporting:dashboard:read."*
  - The viewer with the grant and nothing else gets every card's sentence, including *"None of your documents has been decided in the last 90 days."* It gets no links, because it lacks `document:read` and `workflow:task:read`.
  - The requester's status counts (Draft 1, Pending approval 1, Rejected 1, Completed 2) match the bars, and the card reads *under a minute / 1 min / 3*.
- **The upgrade note's account of publishing is accurate.** `publish_form` re-validates the stored definition. `create_revision` copies a source definition without validating it, so a title-only revision is created and then refused at publish, as the note says. A published form is not re-validated anywhere else, so *"keeps working as it did"* holds.
- **#478.** Every remaining *Phase 8* beside *outbox* in `docs/`, `kelir-backend/src`, `kelir-frontend/src` and `CHANGELOG.md` is struck through. `workflow/mod.rs` and Database Schema §7.11 say *Phase 8* of the signature binding, which is true, as the commit says.
- **ADR-0039's and ADR-0040's amendments.** Both are dated, both cite D-83, both leave the original text standing, and both describe what was built: `approvalTime` on the one contract, and no chart.
- **#430.** `narrow-apt-to-ubuntu.sh` disables a source file carrying any non-Ubuntu URI and refuses to leave apt with no source. A `mirror+file:` source has no `http` URI and is kept. CI's `End-to-end (browser)` job succeeded on `9cfadb3`.
- **`sprint_reports_are_verified.rs` reading only the first `Sprint N` is not a finding.** A two-sprint record credits one sprint, and the test fails safe: the other sprint's report keeps `author-verified`, which [status report 18](../status/18.%20Sprint%2017%20Status.md) carries. Status report 20 can cite this record for Sprint 19, and status report 18's rows can point here without the label changing.
- **The frontend and bundle.** 690 unit tests. `check:bundle` confirms Unovis and the evaluator are off the first-load path, and the approval card adds nothing to it.

---

## 5. What this pass did not cover

- **Sprint 18.** It is record 16's, and was read here only where a Sprint 17 or 19 change touched it.
- **No mutation campaign** over the new predicates. #481 records 18 backend and 10 frontend mutations, and #437/#438 record their own. The mutations here were controls for P6 and P7.
- **No browser flow reaches the pending-tasks or recent-documents cards** (FR-RPT-002, FR-RPT-003). Status report 18 said so, and it is still true at `9cfadb3`. P8 drove both by hand once. This is recorded rather than filed: it is Sprint 17's declared `verified by inspection only`, not something this pass found.
- **The release stack was not built from images.** P7 and P8 ran the debug binary at `9cfadb3` and the built bundle behind Caddy. That is the release topology, not its artefacts. The `0.7.0` backend image was used only for P5.
- **ECMA-262 is V8's.** Only node was run. The negated-class and flag rows are grammar, not engine discretion.
- **The squash, and #435's rules beyond P2**, were read against their code and not attacked further. Record 15's probes A–E, which #435's file records, were not re-run.
- **Dark mode, a narrow viewport, and an empty tenant** were not driven.

---

## 6. What this means for `v0.8.0`

| Gate | State after this pass |
|---|---|
| Sprint plan §2's criterion for the reader | **Met for all 22 commits** in both ranges (§1) |
| **D-85**, record 17 exists and its blocking findings hold the tag | **One blocking finding, #482.** Its discharge is two texts: the registry's subset and the `CHANGELOG` upgrade note |
| `sprint_reports_are_verified.rs` | Sprint 19 is credited by this record's name. Sprint 17 keeps `author-verified`, and its report can now cite §3 and §4 |
| `releases_are_independently_verified.rs` | This record is numbered above every cited record. **Findings 2, 3 and 5 mean record 08's citations and Aftermath need a hand check, and the tag must wait for #482 by decision**, because the gate turns red only after the tag exists |
| Full suite green | **Yes**: 78 targets, 1,593 passed, 0 failed, locally and in CI on `9cfadb3`. Frontend 690/690. E2E 24/24 locally and green in CI |
| Upgrade notes the run must write | #477's note stands, amended by #482. **Nothing in Sprint 17 needs one**: `0043` and `0044` are additive and N−1 safe (P5) |

**One finding holds the tag, and it is a sentence.** Findings 2 to 7 are not blocking, and each has an issue. Status `Final`: every blocking finding has an issue.
