# Sprint 11 Status — 2026-08-29

**Status:** Final · **Last updated:** 2026-08-31

**Phase:** 5 Workflow Engine (SDD §14) · **Sprint goal:** the approval loop closes — a decision is taken from the inbox with its reason recorded, a document can be sent back instead of only refused, the history says how it got here, and someone away from their desk does not stop the queue. Phase 5 closes and `v0.5.0` is cut.

> **This report was `Draft` for two days, and why is the sprint's own finding rather than a footnote to it.** [Sprint plan](../planning/01.%20Sprint%20Plan.md) §2 makes an item whose author is also its only verifier `In progress` rather than Done, and **every construction commit in this sprint has one author**; the same rule says the report is not `Final` before the independent pass and the retrospective land in the same change. **That pass completed on 2026-08-31** — three sittings by two readers, [record 09](../verifications/09.%20Sprint%2011%20Independent%20Pass.md) — and this change is the one §2 names. The rows below now read `Done`, and the [retrospective](../retrospectives/09.%20Sprint%2011%20Retrospective.md) is beside it.
>
> **The pass arriving is not the same as the sprint having had one, and this was the third sprint running without.** Sprint 10 closed the same way and its pass was discharged late by [record 08](../verifications/08.%20Sprint%2010%20Independent%20Pass.md), which found [#225](https://github.com/sujanto-gaws/kelir/issues/225) — a defect 34 mutations had not reached, because there was no clause to mutate. Sprint 9 was the sprint before that. A rule that fires only when someone is already worried is the risk §7 has carried since Sprint 5, and three consecutive sprints is not a run of clean work; it is a run of unexamined work. What broke the run was a **phase boundary**, which is later than a sprint and is why the retrospective's second action asks the closing pass for a date and a party rather than for another reminder.

## Summary

**The sprint's scope is met, and ~~the phase is not closed~~ the phase closed two days after this report was written.** All six construction items are merged to `main` — #231 through #236 — and the goal's sentence is true of the running product: a decision is taken from the inbox with its reason, a return sends a document back and a resubmission keeps its number, the history says how the document got here and on whose authority, and a delegation window keeps somebody's queue moving while they are away.

~~**Three of the four phase-exit criteria are outstanding**: the demo has not been run, no independent pass has happened, and the `v0.5.0` release check has not been rehearsed. `v0.5.0` is therefore not tagged.~~ The distance between "the last pull request merged" and "the phase closed" is exactly those three things, and this report exists to keep them from being mistaken for each other.

> **All four are discharged as of 2026-08-31**, in the order the [Phase 5 Close Plan](../planning/06.%20Phase%205%20Close%20Plan.md) §2 sequenced them rather than the order the sprint plan listed them. The demo ran on 2026-08-30 against the release stack CI builds — 13 passed, [record 10](../verifications/10.%20Phase%205%20Exit%20Demo.md); the release check was rehearsed the same day with the N−1 rollback passing in both halves; **`v0.5.0` is tagged at `c91433a`** ([release record 05](../releases/05.%20Release%20v0.5.0.md), still `Draft` for one row — the images are not built from the tag, which needs Docker); and the independent pass finished on 2026-08-31. The sentence above is struck rather than rewritten because it was true when this report was written, and the two-day distance between it and this note is the thing the retrospective is about.

**One item came in cheaper than planned and one came in with a reversal.** Conditional routing (#186) was priced as *cheap* because **D-10**'s evaluator and #174's operator bound already existed — that held, and AC1 and AC2 were regression assertions rather than new work. What it cost instead was a **behaviour reversal**: an unevaluable condition now stops the transition rather than reading as `false` and taking the fallback. That reverses a considered decision of this project's own and is recorded as JWSS **R-4** §6.4.

## Scope Status

| Backlog item (FR IDs) | Status | Evidence |
|---|---|---|
| Workflow history per transition — FR-WF-012 | Built, **`Done`** | [#181](https://github.com/sujanto-gaws/kelir/issues/181), PR #231. `0027_workflow_history.sql`; 5 tests; AC6 asserted over the columns rather than the router. **Read independently** in [record 09](../verifications/09.%20Sprint%2011%20Independent%20Pass.md) §3, first sitting, and its screens in §6 |
| Decide from the task, with the reason — FR-TASK-004, 005, 006 | Built, **`Done`** | [#182](https://github.com/sujanto-gaws/kelir/issues/182), PR #232. JWSS gained `requiresComment` (**D-41**); the reason lands in three columns in one transaction. **Read independently** in record 09 §3; §6 found [#263](https://github.com/sujanto-gaws/kelir/issues/263) in the screen it shares, since closed |
| The return action — FR-WF-008 | Built, **`Done`** | [#183](https://github.com/sujanto-gaws/kelir/issues/183), PR #233. Resubmission through `POST /documents/{id}/submission` (**D-42**), keeping the number. **Read independently** in record 09 §3, and driven end to end by the exit demo |
| Delegation, end to end — FR-IDM-006, FR-WF-009, FR-TASK-008 | Built, **`Done`**, one finding open | [#184](https://github.com/sujanto-gaws/kelir/issues/184), PR #234. `0028_delegation.sql`; 20 integration tests; 9 mutations seen red. **Read independently** in record 09 §7, third sitting, which found that a task handed back to the person who handed it out is decided *on their own behalf* — F3 below |
| Due dates and the overdue indicator — FR-WF-011, FR-TASK-007 | Built, **`Done`** | [#185](https://github.com/sujanto-gaws/kelir/issues/185), PR #235. No migration — `due_at` existed since `0025`; 7 tests; 4 mutations seen red. **Read independently** in record 09 §7, which walked all five acceptance criteria and found the item sound; F2 below is in the statement beside it rather than in this item |
| Conditional routing — FR-WF-015 | Built, **`Done`** | [#186](https://github.com/sujanto-gaws/kelir/issues/186), PR #236. `0029_workflow_routing.sql`; 7 tests; 3 mutations seen red. **Read independently** in record 09 §7 and found sound on all six criteria; the exit demo then found [#271](https://github.com/sujanto-gaws/kelir/issues/271) in the screen that reads its edges, since closed |

**Suite at the close:** 1 097 backend tests across 53 binaries, 473 frontend tests across 41 files, `clippy -D warnings` clean, the offline query cache regenerated and verified.

**Mutation campaign (§2's per-sprint rule): 16 mutations over predicates this sprint added, 16 red.** The ratio is 100%, and it should be read with its denominator: these were chosen to attack named claims in doc comments rather than sampled across the surface, so the number says the claims are honest, not that the surface is covered. Records 03 and 04 measured 65% and 37% over broader samples. **Two tests deliberately pin an *absence*** and say so instead of claiming a mutation they cannot run.

## What the Close Found

**Six findings were filed and all six closed inside the close window**, which is the verification allowance being spent rather than carried — Sprint 7 filed seven and closed none.

| # | Finding | Closed by |
|---|---|---|
| C1 | A definition can save (201), publish (200) and then **500 at run time** — three instances, one root cause, in the seam between what the meta-schema validates and what the columns permit. **Release-blocking** | [#259](https://github.com/sujanto-gaws/kelir/issues/259), `ffe128a`. Found by [record 09](../verifications/09.%20Sprint%2011%20Independent%20Pass.md) §3 and confirmed end to end against a real database |
| C2 | A safety comment named one caller where there are five | [#260](https://github.com/sujanto-gaws/kelir/issues/260), `0113507`. Every caller traced: no leak, and a reader deciding whether a sixth is safe was being told the wrong thing about what keeps it safe |
| C3 | The Workflow tab says an approval has no steps when the reader may not read tasks — and the permission combination is **the requester's** | [#263](https://github.com/sujanto-gaws/kelir/issues/263), `82e16ff`. Record 09 §6, the frontend sitting |
| C4 | The task screen tells its holder that engine-only transitions arrive in a later release, where the file's own header says a `DELEGATE` edge is one nothing fires | [#264](https://github.com/sujanto-gaws/kelir/issues/264), `2b74f91`. The unusual case where **the comment was the correct half** |
| C5 | `formParity` went red under concurrent load and green on re-run — a spec waiting on a fixed number of microtask flushes for a WebAssembly load | [#266](https://github.com/sujanto-gaws/kelir/issues/266), `ca0cd1b`. Its backend half was **separated rather than closed with it** and is carried as [#274](https://github.com/sujanto-gaws/kelir/issues/274) |
| C6 | With two `APPROVE` edges out of one state, the task screen named the first edge's destination for every document — including the ones going the other way | [#271](https://github.com/sujanto-gaws/kelir/issues/271), `38534f1`. **Found by the exit demo on its first run**, which is the job an exit demo exists to do |

**Three findings are open**, all from the third sitting of the independent pass and none blocking. **None was executed**: that session had no Docker, no Rust toolchain and no PostgreSQL, so each is traced through the statements that produce it and states the precondition it needs — [record 09](../verifications/09.%20Sprint%2011%20Independent%20Pass.md) §7.1 says so before it says anything else.

| # | Finding | Standing |
|---|---|---|
| F1 | **A document with a live approval can be discarded, and the approval survives it.** A state's `mapsToDocumentStatus` may be `DRAFT`, and `delete_document` asks only whether the status is `DRAFT` — where the status route refuses a document being decided. `soft_delete`'s own comment excludes `RETURNED` and names this harm exactly; the guard is right and is written against a proxy the projection falsifies. Afterwards `decide` answers 404 and the process can never move again | Open, not blocking. **No definition in this repository maps a state to `DRAFT`** — it is a trap for the first author who reaches for the status that means *editable again* |
| F2 | **The inbox's count and its page apply different rules.** `list_for_caller` joins `documents … deleted_at IS NULL`; `count_for_caller` has no documents join — the divergence the comment directly above it forbids in those words. `is_visible_to` lacks it too, so it answers *visible* where the detail read answers 404 | Open, not blocking. Reachable today only through F1, and filed apart from it because closing F1 leaves this statement still wrong |
| F3 | **A task handed back to the person who handed it out is decided "on their own behalf".** `COALESCE(delegated_from_user_id, $4)` is right for A → B → C and tested for it; A → B → A leaves the column naming the actor, and the history row names one person twice — the state `0028`'s header says the nullability exists to prevent | Open, not blocking. No authorization defect: `permits` checks the same candidate twice and widens nothing. What is wrong is the record, on the screen #184 AC4 exists to serve |


## Blockers and Decisions Needed

> **All four blockers below are discharged, and the table is left standing with its outcomes rather than deleted.** What each of them cost is the retrospective's subject, and a blocker table emptied at the close reads as a sprint that never had any.

| Item | Owner | Needed by | Outcome |
|---|---|---|---|
| **No independent pass over this sprint's surface** — third consecutive sprint | Whoever did not write it | Before `v0.5.0` | **Discharged 2026-08-31** — and *after* the tag rather than before it, which is what the recommendation asked for and did not get. Three sittings, two readers, [record 09](../verifications/09.%20Sprint%2011%20Independent%20Pass.md). It found C1 blocking, four more that were not, and three that are still open |
| **The exit demo has not been run** | Sprint owner | Before `v0.5.0` | **Run 2026-08-30, 13 passed** ([record 10](../verifications/10.%20Phase%205%20Exit%20Demo.md)). The stack cost nothing: CI's browser job already builds the release images and stands the stack up on every pull request, so this was **provisioned rather than procured** — the one estimate at this boundary that was too high |
| **Four of six items added no browser coverage** | Sprint owner | Before `v0.5.0` | **Closed by the demo**, which took the three undriven items into the existing flow as **one** scenario rather than three, because they share a branch — and the first run found C6. The gap was real and the fix was on the exit path. Original recommendation: [#182](https://github.com/sujanto-gaws/kelir/issues/182) and [#183](https://github.com/sujanto-gaws/kelir/issues/183) extended `a-document-is-approved.spec.ts` and `e2e/support/workflow.ts`; [#181](https://github.com/sujanto-gaws/kelir/issues/181), [#184](https://github.com/sujanto-gaws/kelir/issues/184), [#185](https://github.com/sujanto-gaws/kelir/issues/185) and [#186](https://github.com/sujanto-gaws/kelir/issues/186) added none. **Delegation, due dates and conditional routing changed the task and workflow screens and no browser sees them.** The exit demo is driven through the harness, so the gap is on the exit path rather than beside it |
| **`v0.5.0` release check not rehearsed** | Sprint owner | Before the tag | **Rehearsed 2026-08-30, N−1 passed in both halves** over the six migrations new since `v0.4.0`, `0024`'s tightening index included. **`v0.5.0` tagged at `c91433a`.** [Release record 05](../releases/05.%20Release%20v0.5.0.md) stays `Draft` for one row: the images are not built from the tag, which needs Docker and which `ci.yml` has no tag trigger to do instead |

## Scope Changes This Sprint

- **Nothing was added and nothing was cut mid-sprint.** The six planned items are the six delivered.
- **The four items cut at planning were never re-targeted in the backlog**, which went on reading `Sprint 11` for work that shipped without them. Disposed of at this boundary by **D-43**: FR-MDM-010, FR-TASK-009 and FR-SRH-003 to Sprint 13, FR-WF-005 to Sprint 14. None is unscheduled; §8's totals do not move.
- **A claim in an earlier draft of this report was wrong and is corrected here.** It said no e2e flow was added for any of the six items. Two were: #182 and #183 each extended `a-document-is-approved.spec.ts`. The accurate statement is the blocker row above — four of six added none, and the three most recent are one author's.
- **A Phase 6 planning error was found while detailing Sprint 12 and is corrected before that sprint starts** (**D-44**). The backlog targeted FR-AUD-001 and FR-AUD-003 at Sprint 12; both shipped in Sprint 3. The one real gap is **FR-AUD-005** — `audit_events.ip_address` exists, `middleware::client_address` resolves the address, the rate limiter uses it, and all 53 audit call sites pass `None` while that middleware's own documentation says otherwise.
- **[#24](https://github.com/sujanto-gaws/kelir/issues/24) closed by hand.** It was absorbed into #184 by **D-17**, so no branch was named after it and no `Closes` line mentioned it; the work merged and the issue stayed open. The [git workflow](../../docs/standards/05.%20Git%20Workflow.md) §4 gained the rule that prevents the next one.

## Next Sprint Entry Criteria

**Sprint 12 does not start until Phase 5 closes.** Its entry criteria say so, and the reason is that a phase opening with the previous one untagged is how a release stops being a boundary and becomes a label. In order, with what each of them came to:

1. ~~The independent pass over Sprint 11's surface. It is the criterion this report is `Draft` for.~~ **Done 2026-08-31**, three sittings and two readers.
2. ~~The exit demo, run rather than described.~~ **Done 2026-08-30**, 13 passed.
3. ~~The `v0.5.0` release check rehearsed, then the tag.~~ **Done 2026-08-30**, tagged at `c91433a`; the release notes say `v0.5.0` is not the MVP, which **D-1** puts at `v0.6.0`. **One row of the release record is still open** — the images are not built from the tag.
4. **One 25 MB ClamAV scan measured by hand**, before Sprint 12's item 3 is estimated. **D-4** chose the scanner and sized nothing, and it is the only unmeasured number in Sprint 12's plan. **Outstanding, and it needs Docker** — the same dependency that leaves criterion 3's last row open, so the two are one errand.

**So Sprint 12's entry stands on one criterion and one row**, both of which need a session that can run containers. Nothing else is in the way: `activity_events` is confirmed unbuilt, and the [Phase 5 Close Plan](../planning/06.%20Phase%205%20Close%20Plan.md) is discharged in full. The three open findings above are Sprint 12 allowance work, not entry criteria — the [Phase 5 Close Plan](../planning/06.%20Phase%205%20Close%20Plan.md) §3's rule is that a finding blocks only if it is a security control, a data-integrity rule, or a defect in something the phase claims to have delivered, and F1's precondition is a definition nobody has written.
