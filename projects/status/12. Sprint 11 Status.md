# Sprint 11 Status — 2026-08-29

**Status:** Draft · **Last updated:** 2026-08-29

**Phase:** 5 Workflow Engine (SDD §14) · **Sprint goal:** the approval loop closes — a decision is taken from the inbox with its reason recorded, a document can be sent back instead of only refused, the history says how it got here, and someone away from their desk does not stop the queue. Phase 5 closes and `v0.5.0` is cut.

> **This report is `Draft` and not `Final`, and that is the first thing it says.** [Sprint plan](../planning/01.%20Sprint%20Plan.md) §2 makes an item whose author is also its only verifier `In progress` rather than Done, and **every construction commit in this sprint has one author**. The same §2 rule says the status report is not `Final` before the independent pass. That pass has not happened. Every row below therefore reads `author-verified`, not `Done`.
>
> **It is the third sprint running.** Sprint 10 closed the same way and its pass was discharged late by [record 08](../verifications/08.%20Sprint%2010%20Independent%20Pass.md), which found [#225](https://github.com/sujanto-gaws/kelir/issues/225) — a defect 34 mutations had not reached, because there was no clause to mutate. Sprint 9 was the sprint before that. A rule that fires only when someone is already worried is the risk §7 has carried since Sprint 5, and three consecutive sprints is not a run of clean work; it is a run of unexamined work.

## Summary

**The sprint's scope is met and the phase is not closed.** All six construction items are merged to `main` — #231 through #236 — and the goal's sentence is true of the running product: a decision is taken from the inbox with its reason, a return sends a document back and a resubmission keeps its number, the history says how the document got here and on whose authority, and a delegation window keeps somebody's queue moving while they are away.

**Three of the four phase-exit criteria are outstanding**: the demo has not been run, no independent pass has happened, and the `v0.5.0` release check has not been rehearsed. `v0.5.0` is therefore not tagged. The distance between "the last pull request merged" and "the phase closed" is exactly those three things, and this report exists to keep them from being mistaken for each other.

**One item came in cheaper than planned and one came in with a reversal.** Conditional routing (#186) was priced as *cheap* because **D-10**'s evaluator and #174's operator bound already existed — that held, and AC1 and AC2 were regression assertions rather than new work. What it cost instead was a **behaviour reversal**: an unevaluable condition now stops the transition rather than reading as `false` and taking the fallback. That reverses a considered decision of this project's own and is recorded as JWSS **R-4** §6.4.

## Scope Status

| Backlog item (FR IDs) | Status | Evidence |
|---|---|---|
| Workflow history per transition — FR-WF-012 | **author-verified** | [#181](https://github.com/sujanto-gaws/kelir/issues/181), PR #231. `0027_workflow_history.sql`; 5 tests; AC6 asserted over the columns rather than the router |
| Decide from the task, with the reason — FR-TASK-004, 005, 006 | **author-verified** | [#182](https://github.com/sujanto-gaws/kelir/issues/182), PR #232. JWSS gained `requiresComment` (**D-41**); the reason lands in three columns in one transaction |
| The return action — FR-WF-008 | **author-verified** | [#183](https://github.com/sujanto-gaws/kelir/issues/183), PR #233. Resubmission through `POST /documents/{id}/submission` (**D-42**), keeping the number |
| Delegation, end to end — FR-IDM-006, FR-WF-009, FR-TASK-008 | **author-verified** | [#184](https://github.com/sujanto-gaws/kelir/issues/184), PR #234. `0028_delegation.sql`; 20 integration tests; 9 mutations seen red |
| Due dates and the overdue indicator — FR-WF-011, FR-TASK-007 | **author-verified** | [#185](https://github.com/sujanto-gaws/kelir/issues/185), PR #235. No migration — `due_at` existed since `0025`; 7 tests; 4 mutations seen red |
| Conditional routing — FR-WF-015 | **author-verified** | [#186](https://github.com/sujanto-gaws/kelir/issues/186), PR #236. `0029_workflow_routing.sql`; 7 tests; 3 mutations seen red |

**Suite at the close:** 1 097 backend tests across 53 binaries, 473 frontend tests across 41 files, `clippy -D warnings` clean, the offline query cache regenerated and verified.

**Mutation campaign (§2's per-sprint rule): 16 mutations over predicates this sprint added, 16 red.** The ratio is 100%, and it should be read with its denominator: these were chosen to attack named claims in doc comments rather than sampled across the surface, so the number says the claims are honest, not that the surface is covered. Records 03 and 04 measured 65% and 37% over broader samples. **Two tests deliberately pin an *absence*** and say so instead of claiming a mutation they cannot run.

## Blockers and Decisions Needed

| Item | Owner | Needed by | Recommendation |
|---|---|---|---|
| **No independent pass over this sprint's surface** — third consecutive sprint | Whoever did not write it | Before `v0.5.0` | Run it before the tag, not after. Sprint 10's late pass found a real defect; the two sprints since have the same exposure and more surface |
| **The exit demo has not been run** | Sprint owner | Before `v0.5.0` | It needs a deployed stack. The harness exists (#153) and `a-document-is-approved.spec.ts` already covers part of the path |
| **No e2e flow was added for any of the six items** | Sprint owner | Before `v0.5.0` | Four items changed the task and workflow screens and none is driven in a browser. This is the sprint's largest single gap |
| **`v0.5.0` release check not rehearsed** | Sprint owner | Before the tag | §2 rule 3: rehearse at the close, not at the tag — compose stack from release images, smoke pass, N−1 rollback against `v0.4.0` |

## Scope Changes This Sprint

- **Nothing was added and nothing was cut mid-sprint.** The six planned items are the six delivered.
- **The four items cut at planning were never re-targeted in the backlog**, which went on reading `Sprint 11` for work that shipped without them. Disposed of at this boundary by **D-43**: FR-MDM-010, FR-TASK-009 and FR-SRH-003 to Sprint 13, FR-WF-005 to Sprint 14. None is unscheduled; §8's totals do not move.
- **A Phase 6 planning error was found while detailing Sprint 12 and is corrected before that sprint starts** (**D-44**). The backlog targeted FR-AUD-001 and FR-AUD-003 at Sprint 12; both shipped in Sprint 3. The one real gap is **FR-AUD-005** — `audit_events.ip_address` exists, `middleware::client_address` resolves the address, the rate limiter uses it, and all 53 audit call sites pass `None` while that middleware's own documentation says otherwise.
- **[#24](https://github.com/sujanto-gaws/kelir/issues/24) closed by hand.** It was absorbed into #184 by **D-17**, so no branch was named after it and no `Closes` line mentioned it; the work merged and the issue stayed open. The [git workflow](../../docs/standards/05.%20Git%20Workflow.md) §4 gained the rule that prevents the next one.

## Next Sprint Entry Criteria

**Sprint 12 does not start until Phase 5 closes.** Its entry criteria say so, and the reason is that a phase opening with the previous one untagged is how a release stops being a boundary and becomes a label. In order:

1. The independent pass over Sprint 11's surface. It is the criterion this report is `Draft` for.
2. The exit demo, run rather than described.
3. The `v0.5.0` release check rehearsed, then the tag. **The release notes must say `v0.5.0` is not the MVP** — **D-1** put that at `v0.6.0`.
4. One 25 MB ClamAV scan measured by hand, before Sprint 12's item 3 is estimated. **D-4** chose the scanner and sized nothing, and it is the only unmeasured number in Sprint 12's plan.
