# Sprint 13 Status — 2026-09-03

**Status:** Final · **Last updated:** 2026-09-03

**Phase:** 6 Attachments, Comments, Activity (SDD §14) · **Sprint goal:** the collaboration surfaces become usable rather than merely present — a timeline somebody reads, a notification somebody receives, and the epics' `Should` rows. **Phase 6 closes, the MVP is verified against SRS §9 in full, and `v0.6.0` is cut.**

> **This report went `Final` on 2026-09-03, and what changed is that the pass ran.** [Record 13](../verifications/13.%20Sprint%2013%20Independent%20Pass.md) is an independent pass over all ten items by a reader who authored none of them — the first in this project's history to be independent of a whole sprint, and the first run *before* a tag rather than after a boundary forced it. [Sprint plan](../planning/01.%20Sprint%20Plan.md) §2's condition is discharged.
>
> **`Final` means that condition is discharged. It does not mean the sprint's exit is met, and it does not mean every item is `Done`.** The exit is still three steps short (§*Summary*), one finding is **release-blocking for `v0.6.0`**, and no row in the scope table moves to `Done` on the pass alone — the Definition of Done also requires the exit, and two items keep a storage path the pass could not execute. The report is `Final` because the label this project defined is about the second reader, and there is now a second reader.
>
> **The five-sprint run is over, and the ledger should say how it ended.** It was not ended by the rule firing: [construction plan](../planning/08.%20Sprint%2013%20MVP%20Construction%20Plan.md) §8 named no party, §1.1 said it had fixed a date and fixed none, and §2 was not amended. It ended because the product owner directed a pass after the sprint was already closed. **A control that needs an instruction each time it fires is still not a control**, and the [retrospective](../retrospectives/11.%20Sprint%2013%20Retrospective.md)'s first action stands unchanged in what it asks for.
>
> **The pass corrected this report.** §*Mutation campaign* below is rewritten: the claim that the sprint's mutation evidence was thin and unrecoverable was made by counting commit bodies, and the evidence is in the test files. [Record 13](../verifications/13.%20Sprint%2013%20Independent%20Pass.md) §6 has the count.

## Summary

**Ten construction items built against seven planned, and the exit did not run.** Every item in the [construction plan](../planning/08.%20Sprint%2013%20MVP%20Construction%20Plan.md) §4 shipped, the three items §3.1 cut for capacity were re-included and shipped as well, seven of the eight carried findings closed, and all three of step 0b's process actions landed. Nineteen pull requests merged to `main` between 2026-09-01 00:26 and 2026-09-03. **This is the most work this project has closed in one sprint.**

**And Phase 6 is not closed.** Of the [plan](../planning/08.%20Sprint%2013%20MVP%20Construction%20Plan.md) §7's eight sequenced steps, steps 0–4 and 6 ran; **steps 5, 7 and 8 did not** — the independent pass, the MVP verification against release images, and the `v0.6.0` rehearsal and tag. `git tag` ends at `v0.5.0`; `projects/releases/` ends at [record 05](../releases/05.%20Release%20v0.5.0.md). **Every one of the sprint's four exit criteria is outstanding.**

**Two MVP criteria got the screens they were missing, and neither is demonstrated in a browser.** §1.2 of the plan was right that *a criterion that says **users can** is not met by an endpoint*, and it bought two screens with three `Should` rows to fix it. `features/attachments/` and `features/comments/` now exist. But the e2e flow asserts only their **empty states** — *"Nothing is attached to this document yet"*, *"Nobody has commented on this document yet"* — and **no browser flow uploads a file, downloads one, or posts a comment**. The evidence for criteria 6 and 11 is 545 jsdom component tests. The same argument the plan made one level up applies here: a criterion that says *users can* is not met by a component test either.

## Scope Status

| Backlog item (FR IDs) | Status | Evidence |
|---|---|---|
| **Step 0** — #292 the timeline discloses attachment names; #278 a live approval can be discarded | **Closed, author-verified** | PR [#299](https://github.com/sujanto-gaws/kelir/pull/299) (**breaking**: four fields leave the activity payload, four link ids replace them; `activity::domain::disclosable` holds it at the read because the table is append-only) and PR [#300](https://github.com/sujanto-gaws/kelir/pull/300), **reproduced before fixing** as its criteria required — the delete answered 204, the instance stayed RUNNING and the task stayed claimable, which the issue did not predict. **D-45, D-46** |
| **Step 0b** — the postcondition, the header rule, `v0.5.0`'s images | **Done** | PR [#305](https://github.com/sujanto-gaws/kelir/pull/305). `minio-init` ends `mc ls kelir/kelir` in both compose files; `check-pull-request-title.sh` lints the title as the header it becomes — **four of the eight headers then on `main` were over 72**; `v0.5.0`'s images built from a worktree at the tag and [release record 05](../releases/05.%20Release%20v0.5.0.md) is `Final`. **D-38** |
| **Item 1** — activity timeline per document, FR-ACT-005 | **author-verified** | [#250](https://github.com/sujanto-gaws/kelir/issues/250), PR #302. `features/activity/`; the read drops `activity:read` and asks the document's own permission — **D-47**, which three documents had said since Sprint 12 and the code did not do. The permission row stays until `v0.6.0` under the N−1 rule ([#301](https://github.com/sujanto-gaws/kelir/issues/301)) |
| **Item A** — attachments on a document, FR-ATT-001…005 | **author-verified** | [#295](https://github.com/sujanto-gaws/kelir/issues/295), PR #297. **MVP criterion 6.** The three scan refusals read differently, and `FAILED` is refused exactly as `INFECTED` is — a scan that could not run has cleared nothing. The download button follows the server's status and adds no client-side rule |
| **Item B** — comments on a document, FR-CMT-001 | **author-verified** | [#296](https://github.com/sujanto-gaws/kelir/issues/296), PR #298. **MVP criterion 11.** Not the decision comment, and the note under the composer is the first place a *person* can see the difference. The last `EmptyTab` loses its consumer; the tests it served invert rather than disappear |
| **Item 2** — notification migration, in-app centre, assignment and decision triggers, FR-NTF-001…003 | **author-verified** | [#251](https://github.com/sujanto-gaws/kelir/issues/251), PR #303. `0034_notification.sql`; written in the transaction of the thing it announces, which is `activity::record`'s shape and deliberately not `audit`'s. **D-48** (ADR-0024): a role task tells every current holder, and `holders_of_role` is written as the inverse of `task::holds_role` |
| **Item 3** — audit search for authorized users, FR-AUD-004 | **author-verified, and API-only** | [#252](https://github.com/sujanto-gaws/kelir/issues/252), PR #304. `0035_audit_search.sql`, `GET /api/v1/audit` behind a new `audit:read`. **D-49** generalizes D-12 to nineteen object types: the trail is served to anybody who may search, the recorded *values* need the named object's own read, and a caller without it gets `valuesWithheld` rather than a shorter list. **No screen** — nothing under `kelir-frontend/src/` references the endpoint |
| **Item 4** — threaded replies, edit and delete, FR-CMT-002/003/004 | **author-verified** | [#253](https://github.com/sujanto-gaws/kelir/issues/253), PR #309. `0036_comment_thread.sql`. **D-50/ADR-0029** one level of threading; **D-51/ADR-0030** a soft delete leaves a tombstone where a reply hangs from it. `count_for_document` repeats the page's predicate verbatim — **the version that did not was seen red**, the page at two rows reporting a total of one |
| **Item 5** — attachment categories, soft-delete, external references, FR-ATT-006/009/010 | **author-verified** | [#254](https://github.com/sujanto-gaws/kelir/issues/254), PR #310, **with #293 and #294 as §5 placed them**. `0037_attachment_tail.sql`. **D-52/ADR-0032** the object stays; **D-53/ADR-0031** an external reference is its own table, so a link can never read `CLEAN` — held by the shape rather than by a convention the second screen forgets |
| **Item 6** — master-data changes through workflow, FR-MDM-010 | **author-verified** · *cut 2026-08-31, re-included 2026-09-02* | [#255](https://github.com/sujanto-gaws/kelir/issues/255), PR #313. `0038_master_data_governance.sql` **alters nothing**: `document_types.target_entity_type` was created by `0015` in Sprint 4 and read by nothing for nine sprints, and `record_status` already reserved `PENDING_APPROVAL`. **D-55/ADR-0033**. AC6 holds by direction — `grep -rn "modules::master_data" src/modules/workflow/` returns nothing |
| **Item 7** — completed task view and task search, FR-TASK-009, FR-SRH-003 | **author-verified** · *cut, re-included* | [#256](https://github.com/sujanto-gaws/kelir/issues/256), PR #314, **with #279 as §5 placed it**. A fourth point on one axis rather than a second endpoint; search runs inside the visibility rule rather than over the rows it returned. `%` and `_` typed by a person are characters |
| **Item 8** — email channel and templates, FR-NTF-004/005 | **author-verified** · *cut, re-included last* | [#257](https://github.com/sujanto-gaws/kelir/issues/257), PR #315. `0039_notification_email.sql` seeds and **adds no DDL** — all four tables have been in `0034` since item 2, which said so. **D-56/ADR-0034**: one attempt, recorded either way, and what that costs is stated rather than discovered. **Nine tests, four mutations seen red; one first went green** and the fixture now arrives like a second worker |

"Done" requires the [sprint plan's](../planning/01.%20Sprint%20Plan.md) Definition of Done, whose independent-verification row is unmet for every item above. **No row in this table is `Done`.** That is the fifth sprint for which that sentence is true, and the second in which it is true of a whole MVP.

**Also merged, and unplanned:** PR [#306](https://github.com/sujanto-gaws/kelir/pull/306) — the ADR standard ([standards/06](../../docs/standards/06.%20Architecture%20Decision%20Records.md)), the template, the `write-adr` skill, and a one-time retrofit of **ADR-0001…0028** from `architectures/01` §29 and the `D-n` table; PR [#318](https://github.com/sujanto-gaws/kelir/pull/318) — eleven environment variables the configuration reference did not list, held from now on by `configuration_reference.rs` rather than by a diff somebody happens to run ([#316](https://github.com/sujanto-gaws/kelir/issues/316), **D-57**); PR [#319](https://github.com/sujanto-gaws/kelir/pull/319) — `KELIR_STORAGE_ACCESS_KEY` and `KELIR_STORAGE_SECRET_KEY` defaulted to MinIO's own and staging and production now refuse to start without them ([#317](https://github.com/sujanto-gaws/kelir/issues/317), **D-58**).

**Six migrations:** `0034_notification.sql` through `0039_notification_email.sql`, consecutive and correctly numbered — **the first sprint in three whose migration numbers did not have to move**.

**Suite: 1,278 backend tests over 67 targets and 545 frontend tests over 45 files.** The backend count is verified against the tree (`#[test]`, `#[tokio::test]` and `#[sqlx::test]` attributes: 1,278) and against three clean full-suite runs recorded on [#274](https://github.com/sujanto-gaws/kelir/issues/274); the frontend suite was run for this report on 2026-09-03 — 45 files, 545 passed, 0 failed, 44.7 s. **OpenAPI is current by a test rather than by a checklist**: `every_annotated_route_reaches_the_document` reads the source in both directions.

**Mutation campaign (§2.9): thirty mutations tabulated with their reddened tests, plus two the compiler refuses — larger and better recorded than Sprint 12's twenty.** ~~The evidence is thinner than Sprint 12's, and where it went is not recoverable from the repository.~~ **Struck 2026-09-03 by [record 13](../verifications/13.%20Sprint%2013%20Independent%20Pass.md) §6, which is this report's own claim corrected by the pass it asked for.** That sentence was reached by counting *commit bodies*; the evidence is in the **test files**, dated and naming the reddened test for each mutation, which is what §2.9 asks for and a more durable place than a commit message: `audit_search.rs` 6, `notifications.rs` 6, `master_data_governance.rs` 4, `notification_email.rs` 4 (one first green), `task_inbox.rs` 4 (plus one that no longer compiles), `attachment_tail.rs` 3, `configuration_reference.rs` 3, and `document_activity.rs` inline including one that does not compile.

**What survives is one gap rather than a sprint-wide regression:** `document_comments.rs` carries no mutation record, so **items B and 4 — the comment screen and the comment tail — have no campaign evidence in the tree**, and #309's single informal mention in its commit body is all there is for the comment tail's `count_for_document` predicate.

## What writing this report found

> **The independent pass has since run** — [record 13](../verifications/13.%20Sprint%2013%20Independent%20Pass.md), 2026-09-03. It confirmed observations 1, 3, 5 and 6 below, could not reach observation 7 for want of object storage, and added three findings of its own. The section is kept as written because it was the honest description of this report before the pass, and because a reader should be able to see which of these an author's read could produce and which it could not.

**This is not the independent pass**, and nothing below substitutes for one: it is a read of the repository performed to write this report, it executed no part of the product beyond the frontend suite, and it is stated before its findings for the reason [record 12](../verifications/12.%20Sprint%2012%20Surface%20Read.md) stated its own limits first.

| # | Observation | Evidence |
|---|---|---|
| 1 | **The plan fixed no date**, having said twice that it did | §1.1 and §7 step 5 cite each other; no calendar date in the file |
| 2 | **Step 0's ordering argument was inverted in execution.** §5 put #278 *before item A* because *"item A puts a delete button within reach of the same documents"* — item A merged at 00:26 on 2026-09-01 and #278's fix at 13:12, after item B as well. #292 before item 1 **was** honoured (11:36 against 15:42), which is the half where a screen would have been built twice | `git log --pretty='%h %ci %s'` |
| 3 | **No browser flow exercises the two MVP criteria this sprint existed to give surfaces.** `e2e/tests/` asserts the attachments and comments panels' empty states and the activity timeline; nothing uploads, downloads or comments. PR #297's own body says so: *"what is still missing is a flow that uploads a file and watches it become downloadable"* | `e2e/tests/a-document-is-created-and-submitted.spec.ts` |
| 4 | **`minio-init`'s postcondition fails the container, and nothing observes the container.** `backend` has no `depends_on: minio-init: condition: service_completed_successfully`, and `deploy.sh`'s smoke test covers `/health/ready` and `/version`. A stack whose bucket step fails still deploys green and still fails at the first upload — which, with observation 3, means **a Sprint 12-shaped deployment defect would still reach a tag** | `deploy/staging/docker-compose.staging.yml`, `deploy/staging/deploy.sh` |
| 5 | **Item 3 shipped API-only.** FR-AUD-004 is `Must` — *"provide audit search for authorized users"* — and no frontend file references `/api/v1/audit` or `audit:read`. Not an SRS §9 criterion, so not MVP-blocking; the same shape as the defect plan §1.2 was written to fix | `grep -rln "api/v1/audit\|audit:read" kelir-frontend/src` returns nothing |
| 6 | **PR #308 is an empty commit** duplicating #305's three commits verbatim. `git diff cfbcefb^ cfbcefb` is empty. No harm done; it is a merge that reports work it did not do, which is the shape of the defect #305 itself fixed | `git show --stat cfbcefb` |
| 7 | **The master-data list does not render `PENDING_APPROVAL`.** Item 6 parks a record there for the life of its change document; `PENDING_APPROVAL` appears in the frontend only as a *document* status. Whether #255's criteria asked for it is not recoverable here — flagged for the pass rather than filed | `kelir-frontend/src/features/master-data/MasterDataListPage.vue` |

## Blockers and Decisions Needed

| Item | Owner | Needed by | Recommendation |
|---|---|---|---|
| **The MVP exit is two steps short**, the independent pass having run on 2026-09-03 — the SRS §9 verification against release images, and the `v0.6.0` rehearsal and tag | Project owner | Before Sprint 14 opens | **Run them before starting new construction.** The plan ordered these deliberately: step 5 sits before the tail so a finding has somewhere to be fixed. The pass found one blocking defect in the surface those two steps would otherwise have tagged over |
| ~~**Record 13 finding 1 — a parked master-data record can be deleted, stranding its approval for ever.**~~ **Fixed 2026-09-03, D-60.** `refuse_if_awaiting_approval` now guards `delete_party` and `delete_facility` under `lock_record_status`'s `FOR UPDATE`, so a delete and a submit serialise rather than race. Three regression tests, two mutations seen red, [ADR-0033](../../docs/architectures/adr/0033.%20A%20Governed%20Record%20Parks%20at%20Pending%20Approval.md) §4 corrected | Backend lead | ~~Before the `v0.6.0` tag~~ **Done** | **What is still owed:** [#322](https://github.com/sujanto-gaws/kelir/issues/322) — `settle`'s `AppError::Internal` remains a 500 for a state the product can reach by other routes |
| ~~**Record 13 finding 2 — `EXTERNAL_REFERENCE` audit rows withhold their values from everybody.**~~ **Fixed 2026-09-03, D-61.** The arm maps to `attachment:read`; **and the cause is fixed with it** — the enumerating guard is replaced by `tests/audit_object_types.rs`, which walks the crate's source, asserts both directions and carries a count guard, which is verification rule 6 met at last. Designing the second direction found `PARTY_ROLE`, an arm nothing writes, now retired with its reasoning kept | Backend lead | ~~Sprint 14~~ **Done** | **Still owed:** [#323](https://github.com/sujanto-gaws/kelir/issues/323) — typing `AuditEntry.object_type`, so an enum and an exhaustive `match` make an unplaceable type fail to compile, which is the close the source walk stands in for |
| ~~**Record 13 finding 3 — the release stack's infrastructure images are unpinned.**~~ **Fixed 2026-09-03, D-62.** All four pinned in both compose files and in CI, to what `latest` resolved to that day by digest — so the change froze the stack rather than moving it — and held by `tests/deployment_images_are_pinned.rs` rather than by a convention. The stack was stood up from the pinned files and each service answered its own protocol | Release owner | ~~Before the `v0.6.0` tag is deployed from~~ **Done** | **Still owed:** [#324](https://github.com/sujanto-gaws/kelir/issues/324) — setting ClamAV's `StreamMaxLength` explicitly; the pin freezes the default it rests on, which is as far as a pin can close that |
| **MVP criteria 6 and 11 have screens and no browser demonstration** | Project manager | Before `v0.6.0` | One e2e flow: attach a file, wait for `CLEAN`, download it, post a comment, see both on the timeline. It closes observation 3 and observation 4 together, because it is the only assertion in the suite that would fail on an unwired object store |
| **[#274](https://github.com/sujanto-gaws/kelir/issues/274) is not merged.** The harness fix is on `test/party-roles-log-capture`, one commit ahead of `origin/main`, and the CHANGELOG entry for it is already written | Sprint owner | Before the tag | Merge it. Its own criterion — *capture a failing run with the log* — is now satisfiable for the first time; the defect itself is still unreproduced after three clean full-suite runs, and the issue should say that rather than stay open silently |
| **`v0.5.0` shipped with `KELIR_STORAGE_SECRET_KEY` defaulting to `minioadmin`**, fixed here by #319 | Release owner | `v0.6.0` release notes | State it in the release notes. The shipped staging compose refused to start without it, so the exposure was to a bare binary or another orchestrator — narrow, and not nothing |

## Scope Changes This Sprint

- **The §3.1 trade was made and then fully reversed, and both halves are recorded rather than absorbed.** Items 6, 7 and 8 were cut at planning for capacity — seven items against a measured rate of six to seven — and two screens ([#295](https://github.com/sujanto-gaws/kelir/issues/295), [#296](https://github.com/sujanto-gaws/kelir/issues/296)) took their place. All three came back on 2026-09-02 once the seven and the allowance had landed. **None of the three `Should` rows §3.1 said would slip did slip.** The plan's own note calls this a planning finding rather than a win, and the retrospective takes it.
- **Ten construction items against seven planned and a measured rate of six to seven.** The estimate was wrong by three items in a sprint that had already re-baselined its rate twice.
- **Two new issues were filed and closed inside the sprint** — [#316](https://github.com/sujanto-gaws/kelir/issues/316) the configuration-reference gap, found by hand while adding a twelfth variable for #257, and [#317](https://github.com/sujanto-gaws/kelir/issues/317) the object-store credentials, found by writing #316's row.
- **One deferral was taken deliberately:** [#301](https://github.com/sujanto-gaws/kelir/issues/301) drops the now-unchecked `activity:read` permission after `v0.6.0`, because the [release process](../../docs/standards/04.%20Release%20Process.md) §6 N−1 rule deprecates in release N and removes in N+1.
- **The ADR retrofit was not in any plan.** Thirty-four records, a new standard, and a skill, taken at the product owner's direction on 2026-09-01–02. It is real work and it is not in §4's list; the capacity note above does not count it.

## Next Sprint Entry Criteria

**Sprint 14 must not open on ten items of unexamined MVP surface.** In order:

1. **Record 13's finding 1 fixed**, which is the only row that blocks the tag, and findings 2 and 3 filed into Sprint 14's allowance. The independent pass itself is **met** ([record 13](../verifications/13.%20Sprint%2013%20Independent%20Pass.md)) — but it ran because the product owner directed it, not because §2 fired, so the [retrospective](../retrospectives/11.%20Sprint%2013%20Retrospective.md)'s first action stands: the rule still has no owner and no date.
2. **SRS §9 verified criterion by criterion against release images**, not against [Product Backlog](../planning/02.%20Product%20Backlog.md) §5's coverage table — which still reads *"Covered at the tag"* for criteria 6, 11 and 12, and says of itself that it is this project's belief about §9 and is not evidence.
3. **A browser flow over attachments and comments**, per the blocker above. It is the entry criterion that discharges two others.
4. **`v0.6.0` rehearsed, tagged, and its release record written**, with the release notes saying plainly that this one is the MVP — as plainly as `v0.5.0`'s said it is not.
5. **[#274](https://github.com/sujanto-gaws/kelir/issues/274) merged or closed with its attempts stated**, and the allowance carried into Sprint 14 named. It opens holding **one** — the first time in three sprints it has opened at fewer than five, and the reason is the plan §5 shape this sprint should be judged for keeping.
