# Sprint 5 Status — 2026-08-21

**Status:** Final · **Last updated:** 2026-08-21

**Phase:** 3 Master Data (SDD §14) · **Sprint goal:** Phase 2's exit debt is discharged, and the party model exists with parties and their roles created, read and audited through the API.

## Summary

**The sprint goal is met.** All seven planned items are merged with CI green, and the exit demo runs end to end: a party created with a SUPPLIER role and a supplier profile, read back through the API, both writes in the hash-chained audit log (evidence below). Phase 2's exit debt is closed, so nothing stands between the `v0.2.0` tag and a Phase 2 that can be called done.

**The measurement the plan asked for: seven planned, seven closed, and the sprint filed zero issues against itself.** Sprint 4 built five and filed twelve. That is not, on its own, evidence that this sprint's work is better — the same agent authored and verified items 6 and 7, which is precisely the independence the [Sprints 2–4 retrospective](../retrospectives/02.%20Sprints%202-4%20Retrospective.md) made an action. Read the zero as *unverified by a second party*, not as *clean*. The [retrospective](../retrospectives/03.%20Sprint%205%20Retrospective.md) treats it as this sprint's most important finding.

What did hold: every security-control and contract claim below was verified by reintroducing the defect and watching the named test go red (coding standard §2.9, **written down this sprint** — the second half of retrospective action 1, which asked for a status report citing the rule against a named test). Twenty mutations across items 6 and 7, one of which started green and produced a real gap.

## Scope Status

| Backlog item (FR IDs) | Status | Evidence |
|---|---|---|
| 1. Request structs reject misspelled fields (#62, FR-API-005) | Done | `06cff05` (PR #83). `JsonBody` + `deny_unknown_fields`; every integration case run against the unfixed code and seen to fail — reverting `deny_unknown_fields` reproduces the report verbatim. **Known residual, recorded rather than left:** the generated OpenAPI does not say `additionalProperties: false`, because utoipa 5 accepts it only on map-like schemas; a generated client still *sends* the field and now gets a 422 naming it |
| 2. Automate the eight Sprint 3 hand-verified flows (#59, FR-AUTH-001..004) | Done | `30e6065` (PR #84). `tests/auth_session.rs` and `tests/identity_roles.rs`; **seven claims checked by reintroducing the defect and watching the test go red**. Covers what had none at all: `/api/v1/auth/refresh`, rotation, replay killing the whole family, `alg: none` as a case distinct from a wrong signature |
| 3. Change password closes on tests and its session contract is made true (#16, #60, FR-AUTH-005) | Done | `f3802d2` (PR #85). Fourteen tests. The 204 said "every session for the account ends" and only refresh tokens were revoked; the wording was narrowed rather than the behaviour changed (architecture 01 §18.1 keeps authorization off the database), and `an_access_token_issued_before_a_password_change_still_works` pins the residual fifteen-minute window so it stays recorded. A failed attempt is now audited — `Security.PasswordChangeFailed` — which is the shape a hijacked session takes and appeared nowhere in the record before |
| 4. Multi-tenant mode fails at boot, not at the login form (#67, FR-IDM-009, **D-7**) | Done | `aab2eda` (PR #86). Four mutations, each seen to go red. **A fifth was written and thrown away** because it edited the guard's message on a path a typo never reaches — an invalid mutation rather than a weak test, and recorded as such |
| 5. JFSS operator-parity spike (#31, Phase 4 entry criterion) | Done | `bc7c077` (PR #88) and `4764ec4` (PR #89). Output is a written finding, as a spike's should be: [01. JFSS Operator Parity](../spikes/01.%20JFSS%20Operator%20Parity.md). The registry's Rust column named a crate that does not exist; the crate it meant returns unknown operators rather than rejecting them, so the Tamper-Proof Pattern persists 0 in place of a real total. Candidate found whose WebAssembly build agrees on 51/51 cases. **Raised D-10, still open** |
| 6. Master-data migration and party core (#80, FR-MDM-001, FR-MDM-003) | Done | `5667dc6` (PR #91) and `efe95d9` (PR #92). Migration verified against a clean database — 31 tables, 19 of them master-data, 47 indexes, 19 permissions all granted to `ROLE-ADMIN`, both deferred foreign keys from `0002`. Nine mutations seen to go red, covering the permission binding, tenant scope on two paths, the audit write, `STATUS_CHANGE` fidelity, the no-token refusal, the `ROLE-ADMIN` grant, the OpenAPI annotation and `deny_unknown_fields` |
| 7. Party roles and role-specific profiles (#81, FR-MDM-002) | Done | `7c0141e` (PR #93) and `19652e0` (PR #94). Eleven mutations seen to go red. **One started green**: the tenant-scope mutation passed against `a_party_in_another_tenant_has_no_roles_to_reach`, because all three routes refuse at the party lookup and never reach the role type. `a_role_type_from_another_tenant_does_not_resolve` was added and the mutation now goes red against it |

**Unplanned work that landed in the same window**, each closing a standing item rather than adding scope:

- `9d67e37` (PR #87) — **D-9** retires the staging environment for want of infrastructure and replaces the Definition-of-Done clause that named it. #12 closes as not planned after four sprints of waiver.
- `45f5225` (PR #90) — coding standard §2.9 states the mutation rule the sprint had been operating under since day one, with a matching §6 review-checklist item.
- `4764ec4` (PR #89) — **D-11**: the JFSS family described a two-language backend; every Go reference is converted or removed across three documents.

### Exit demo

Run 2026-08-21 against a freshly migrated database, the binary built from `2c95eb4`:

```text
POST   /api/v1/master-data/parties                      201  PARTY-ACME
PUT    /api/v1/master-data/parties/{id}/roles/SUPPLIER  201  SUP-0001
GET    /api/v1/master-data/parties/{id}                 200  roles ['SUPPLIER'], approvalStatus APPROVED

audit_events
  User.Created      CREATE         USER    sha256:0000000 -> sha256:252be47
  Security.SignedIn LOGIN          USER    sha256:252be47 -> sha256:263eddd
  Party.Created     CREATE         PARTY   sha256:263eddd -> sha256:bee164a
  Security.SignedIn LOGIN          USER    sha256:bee164a -> sha256:a3b98fe
  Supplier.Created  ROLE_ASSIGNED  PARTY   sha256:a3b98fe -> sha256:9121ff7
```

The demo is the local one the sprint's waiver names, not the compose stack built from release images that **D-9** made the release check. That check belongs to the `v0.3.0` tag and has not been performed.

## Blockers and Decisions Needed

| Item | Owner | Needed by | Recommendation |
|---|---|---|---|
| **Items 6 and 7 were verified by their own author.** The retrospective's action asks for evidence checked by someone other than the item's author; twenty mutation runs are strong evidence of what the tests catch and no evidence of what nobody thought to test | Backend lead | Before Sprint 6 closes | Run an independent verification pass over the party surface — the same shape that found PR #61's FR-IDM-004 claim and PR #63's harness break. The one green mutation is what this would have looked for |
| **D-10 — which JSON Logic evaluator does Kelir take?** Three registry rules are known-wrong pending it | Product owner | Sprint 7 planning | Take `datalogic-rs` and its WebAssembly build on both sides: parity by construction rather than by CI gate. The cost is a 588 KB gzipped payload against 4.1 KB, lazy-loadable |
| **`v0.3.0` has never been rehearsed against the release check D-9 wrote.** `v0.2.0`'s rehearsal is where the N−1 defect (#76) surfaced, and that was the first time anyone tried | Backend lead | Before the `v0.3.0` tag | Rehearse at the Sprint 6 close, not at the tag. The one rehearsal this project has run found a defect that blocked a release |
| **Merges are still not self-service.** Third sprint carrying this; every merge in Sprint 5 was manual | Project owner | Continuing | Grant the working token pull-request write, or name the manual step and its owner in sprint plan §2. Carried from Sprints 0–1 and 2–4 |

## Scope Changes This Sprint

- **Nothing was added, dropped, or moved between sprints.** All seven planned items closed as planned; the three unplanned changes above each discharge a standing decision rather than displacing scope.
- **#25 closed into #28** per **D-8** at the sprint's entry, leaving one department surface.
- **#12 closed as not planned** per **D-9** — not deferred into a later sprint. It needed a host and DNS, which no amount of sprint capacity produces.
- **Migration numbers shifted a third time**: `0009_party_role_permissions.sql` took the next free number, so RAD is now `0010_rad.sql` and the plugin migration `0018_plugin.sql`. Nothing merged was renumbered. Four inline forward references in the Database Schema were already pointing at the wrong migration before this shift and were corrected rather than mechanically bumped.
- **Database Schema §4 gained three deviation rows** (#15 string lengths, #16 `notes`/`tenantPartyId`) and its §1.3.1 lengths were applied to the master-data DDL at `CREATE TABLE` time. No table was rewritten.

## Next Sprint Entry Criteria

1. **Close #80 and #81.** Both are merged with green CI and the exit demo above; they are open only because the tracker was not updated at merge.
2. **Sprint 6 is planned** in [sprint plan](../planning/01.%20Sprint%20Plan.md) §5, and it is a cut rather than a wish: the outline held ten items against a measured rate of six to seven. What was cut, and in what order, is written there.
3. **Run the independent verification pass on the party surface** before Sprint 6's own work starts. It is the first blocker above and the only one that is about this sprint rather than a later one.
4. **D-10 is not a Sprint 6 dependency** and must not be pulled forward as one. Nothing in Phase 3 touches the evaluator; it gates Sprint 7 planning.
