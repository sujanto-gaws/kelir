# Sprint 7 Status — 2026-08-26

**Status:** Final · **Last updated:** 2026-08-26

**Phase:** 4 Document Core (SDD §14) · **Sprint goal:** a form definition is stored and read back through the API; a document type binds one and carries its numbering rule; a document created from that type submits and takes its number inside the submit transaction; and one flow is driven through a real browser.

## Summary

**The sprint goal is met, with one clause of it deferred by design and one defect against another.** All six planned items are built and merged, both tail items landed, and an unplanned seventh arrived complete. The goal's third clause — "a document created from that type submits and takes its number" — is met at the *numbering* layer only: **D-16** moved the document items to Sprint 9, so there is no document to submit yet, and `allocate_in` is exercised through the service rather than through a route. That was the plan's intent and the wording outran it.

**Verification found seven defects, and the sprint does not close clean.** [Record 04](../verifications/04.%20Sprint%207%20Surface%20Verification.md) is the independent pass §2 requires; it is the first pass in the project over two modules opened from nothing plus a library adoption. One finding — [#200](https://github.com/sujanto-gaws/kelir/issues/200), a `DEPARTMENT_YEAR` numbering rule issuing `000001` to every document forever — is **release-blocking for `v0.4.0`** and needs a migration. None of the seven is fixed; all seven are filed, and sprint plan §5's Sprint 8 section now carries them.

**The measurement, stated beside who took it.** Sprint 7 planned six construction items and a **verification allowance of ten closing issues**. It built six, took a seventh unplanned, and its verification produced **seven issues, none closed in-sprint** — against Sprint 6's twenty filed and fourteen closed. That is not a better sprint; it is the same sprint with the pass at the end instead of throughout, and §"Next Sprint Entry Criteria" says what that costs Sprint 8.

**Tests at branch HEAD: 696 passed, 0 failed, 1 ignored** across 35 integration binaries and both unit targets; frontend 279 passed across 27 files. CI green on `main` at `b49f76e`. The one ignored test is finding F5 ([#204](https://github.com/sujanto-gaws/kelir/issues/204)) — quarantined on **D-7**, which **D-18** superseded inside this sprint.

## Scope Status

| Backlog item (FR IDs) | Status | Evidence |
|---|---|---|
| 1. Browser-driving harness, one flow end to end (#153, **D-14**) | Done | PR #189, 2026-08-24. `e2e/` with Playwright, `find-a-supplier.spec.ts` driving a real deployment. Ran **before** the sprint's own construction, as the plan required. **Residual:** record 04 read the harness and did not run it — no deployment was brought up during the pass — so the harness's own correctness is its authors' evidence |
| 2. JSON Logic evaluator on both sides (#154, FR-RAD-006, **D-10**) | **In progress** | PR #190, 2026-08-24; independently verified 2026-08-26. `datalogic-rs` and `@goplasmatic/datalogic-wasm` both pinned `=5.2.0`, `sum` registered on both, `parity/` replayed by a test on each side. Record 04 measured the two as identical on all five shapes it tried, including the four the corpus does not carry — and found [#201](https://github.com/sujanto-gaws/kelir/issues/201): `{"sum":[a,b]}` is `0` on both sides, silently, which is the symptom D-10 was adopted to remove reached by arity instead of by a typo |
| 3. RAD metadata migration (#155, FR-RAD-001, 002, 003, 005) | Done | PR #192, 2026-08-24. `0014_rad.sql`: form and list definitions, revisions, sections, components, columns, filters, lookups; nine `rad:*` permissions seeded for the storage APIs and for nothing else. `rad_metadata_schema.rs` asserts every table is tenant-scoped and carries the base columns. Applies before the document migration, which is the ordering constraint **D-2** rests on |
| 4. Form and list definition storage APIs (#156, FR-RAD-002, 003) | **In progress** | PR #193, 2026-08-24; independently verified 2026-08-26. A published revision is immutable in the *statement* as well as the service; a JFSS document is validated against the vendored v2.0.1 meta-schema and against the registry's operator allow-list on the way in. Record 04 confirmed the operator walk reaches all five container shapes the meta-schema defines. **Held open by [#206](https://github.com/sujanto-gaws/kelir/issues/206)**: the two statement-level `status = 'DRAFT'` predicates that exist for the publish race are exercised by nothing |
| 5. Document migration, document types, form and workflow bindings (#157, FR-DTYPE-001, 002, 003) | Done | PR #194, 2026-08-24; independently verified 2026-08-26. `0015_document.sql` creates the document table group and the deferred foreign keys `0002` and `0008` were waiting for; `/api/v1/document-types` binds a form, and `lock_bindable_form` refuses one that is not published. Four permissions seeded, for the type surface only |
| 6. Numbering rules with scoped sequences (#158, FR-DTYPE-004) | **In progress** | PR #195, 2026-08-24; independently verified 2026-08-26. The lock is right where it was designed to be right — `FOR UPDATE` on the row the decision reads, in the caller's transaction, and removing it turns `no_two_concurrent_allocations_take_the_same_number` red. **Held open by [#200](https://github.com/sujanto-gaws/kelir/issues/200)**, release-blocking: `DEPARTMENT_YEAR` needs a bucket per department and the table holds one per rule, so every document of every department is numbered `000001` |

**The tail landed rather than moving**, which is the first time a Sprint 7 tail has:

| Item | Issue | Status | Evidence |
|---|---|---|---|
| Department management (FR-ORG-002, FR-IDM-008 per **D-8**) | #28 | Done | PR #196, 2026-08-25; independently verified 2026-08-26. **The first hierarchy in this project to be right on the first attempt** — a tenant-wide advisory lock in the caller's transaction (#133), an ancestor walk that reports truncation instead of answering from a prefix (#134), and both references re-read inside the transaction (#118/#137). `master_data/repository/role.rs` had been validating against a table no surface could populate; it can now |
| Forgot and reset password (FR-AUTH-006) | #17 | **In progress** | PR #197, 2026-08-25; independently verified 2026-08-26. `password_reset_tokens` has existed since `0006` with nothing reading it; it has a reader. Single-use under concurrency is held by the consuming `UPDATE`'s predicate, which is red under mutation. **Held open by [#202](https://github.com/sujanto-gaws/kelir/issues/202)**: the SMTP send is awaited before the `202`, so the answer is timeable, and the module states the opposite in as many words. **Its two pages are verified by inspection only** — no Playwright flow covers them |

**One item arrived that the plan did not schedule**, recorded here as sprint plan §5's Sprint 8 item 6 requires:

| Item | Issue | Status | Evidence |
|---|---|---|---|
| Tenant management and the tenancy model (FR-ORG-001, FR-IDM-009, **D-18**) | #27, #65 | Done | PR #198, 2026-08-25; independently verified 2026-08-26. Multi-tenant mode boots, `/deployment` tells the login form whether to ask for a tenant code, creating a tenant creates the administrator who can sign in to it in one transaction, and `0017_tenant_administration.sql` makes a cross-tenant role grant unwritable. **It ships with a Playwright flow** (`e2e/tests/create-a-tenant.spec.ts`), so it is the first frontend work in the project whose status row does not say "by inspection only". A change-control deviation, taken deliberately and recorded in **D-18** |

**The defect sweep, in scope for the pass and recorded here for the same reason:**

| Item | Status | Evidence |
|---|---|---|
| Every open verification finding closed — #68, #107, #108, #109, #119, #120, #122, #145 | **In progress** | PR #199, 2026-08-26. Eight findings records 01–03 had left standing, four of them contract defects, two needing a migration (`0018`, `0019`) because a merged migration is never edited. Decisions **D-19** and **D-20**. **Held open by [#203](https://github.com/sujanto-gaws/kelir/issues/203)**: the comment introduced by the #145 fix claims the chain hash distinguishes an absent optional field from an empty one, and it does not |

**The closing pass** — [record 04](../verifications/04.%20Sprint%207%20Surface%20Verification.md), 2026-08-26: 33 mutations over the predicates Sprint 7 added — one invalid and excluded, 12 of the remaining 32 red — and 4 probes. Seven findings, one release-blocking.

## Blockers and Decisions Needed

| Item | Owner | Needed by | Recommendation |
|---|---|---|---|
| **[#200](https://github.com/sujanto-gaws/kelir/issues/200) — `DEPARTMENT_YEAR` numbering issues `000001` forever.** Release-blocking for `v0.4.0`, and the fix is a migration either way: a bucket row per department, or the scope leaves the `CHECK` | Backend lead | Sprint 8, **before** Sprint 9 opens the submit path | Take it into Sprint 8 as construction, not into the verification allowance. A numbering change is cheaper before the path that consumes it exists than after |
| **[#201](https://github.com/sujanto-gaws/kelir/issues/201) — what a malformed `sum` should do.** A decision, not a patch: the registry specifies one shape and is silent on the rest, and both engines answer `0` | Product owner | Sprint 8 planning | Refuse it at save (`validate_definition`), which is where this module already prefers to refuse, and add all four shapes to `parity/cases.json` whichever way it goes. Sprint 8 item 4 is the server-side re-evaluation that would otherwise confirm the zero |
| **[#206](https://github.com/sujanto-gaws/kelir/issues/206) — twenty of thirty-two predicates this sprint added are exercised by nothing.** The Sprint 6 retrospective's fourth action was written to prevent exactly this and did not | Backend lead | Sprint 8 | Four of the twenty (`highest_revision`'s tenant filter, `lock_active_rule`'s `is_active` and `tenant_id`, `highest_issued`'s `sequence_key`) are the only guard on their behaviour and need tests now; the other sixteen are unexercised defence in depth and need the rule the retrospective asked for, not sixteen tests |
| **The `v0.4.0` release check has not been rehearsed**, and Sprint 6's precedent puts the rehearsal at the *entry* to the sprint that follows rather than the exit of the one that built it | Backend lead | Sprint 9 entry | Not a Sprint 7 blocker — `v0.4.0` is Phase 4's exit and **D-16** gives it to Sprint 9. Recorded so it is not discovered there |
| **Grant the working token pull-request write, or name manual merge as a step in sprint plan §2.** Sixth occurrence | Project owner | — | Consolidated in the retrospective, per the Sprints 2–4 action. Reported there only |

## Scope Changes This Sprint

- **FR-ORG-001 and #65 returned from Product Backlog §7 and were delivered inside the sprint** (**D-18**, 2026-08-25), at the product owner's direction rather than at a sprint boundary. A change-control deviation against §8's "scope enters through backlog refinement", taken deliberately and recorded. No requirement was added, dropped or re-prioritised — FR-ORG-001 and FR-IDM-009 were always in the SRS; what changed is that they became reachable. Impact: `0017_tenant_administration.sql` shifted the workflow migration to `0018`, and the two defect-sweep migrations then shifted it to `0020`.
- **The two carry-overs #28 and #17 landed rather than moving**, which retires **D-13**'s scheduled half. #24 and #27 were D-13's unscheduled half; #24 returned to Sprint 11 under **D-17** and #27 is the item above.
- **Two decisions were taken by the defect sweep**, both recorded in Product Backlog §6.2: **D-19** (a `partyId` is never released) and **D-20** (the audit hash format changes now, and once).
- **Nothing was dropped, and nothing moved out.** The seven findings of record 04 are new work, not re-prioritised work; they are scope, per sprint plan §5's Sprint 5 capacity note.

## Next Sprint Entry Criteria

Sprint 8's three entry criteria, measured:

1. **"Sprint 7 closed, with its status report and an independent verification pass over its surface."** Met by this report, [record 04](../verifications/04.%20Sprint%207%20Surface%20Verification.md) and the [Sprint 7 retrospective](../retrospectives/05.%20Sprint%207%20Retrospective.md), landing together as §2 and the Sprint 5 retrospective's third rule require.
2. **"#154 merged and its CI parity gate green."** Met — PR #190 at `fe709c4`, and `jsonlogic_parity.rs` and `jsonlogic.parity.spec.ts` are both green in the run behind this report. Qualified by [#201](https://github.com/sujanto-gaws/kelir/issues/201): the gate is green over a corpus that carries one of the three `sum` edge cases the two implementations promise to agree on.
3. **"Sprint 8's capacity re-checked against Sprint 7's measured verification load."** Done, and it adds an item rather than moving one. Sprint 7's measured total is **six built, one unplanned taken, seven filed and zero closed** — the seven are Sprint 8's, on top of Sprint 8's own five.

   The re-check is written into sprint plan §5's Sprint 8 section. **[#200](https://github.com/sujanto-gaws/kelir/issues/200) enters as a sixth construction item, ahead of the renderer**, because it is release-blocking for `v0.4.0`, needs a migration, and Sprint 9's submit path is built on the storage it changes. **Nothing moves out.** Sprint 8's stated exit — *"an administrator configures a document type against a published form; a user opens that type, fills the form … and submits it"* — needs all five of its items, so cutting item 5 to make room would cost the exit its configuration half. That trade is named here rather than discovered: **item 5 (#165) is the cut candidate if the sprint runs late**, and cutting it costs the exit demo, not a requirement, because FR-RAD-008's storage half already exists in #157.

   Six construction items sits at the top of the measured rate of six to seven, and two of the six are the phase's centrepiece. That is deliberate and it is the product owner's to overrule: the alternative is Sprint 9 opening its submit path on a numbering table that issues one number.

**What Sprint 8 inherits, stated plainly.** Its five items depended on Sprint 7's foundations and all three arrived: the evaluator (#154), the definition storage (#156) and the harness (#153). Nothing in Sprint 8's dependency table is blocked. What it also inherits is seven open findings and a coverage rule that has failed once already — and item 4 is the security-critical one, where coding standard §2.9 applies in full.
