# Sprint 0 Status — Project Baseline — 2026-08-11

**Status:** Final · **Last updated:** 2026-08-11

**Phase:** Sprint 0, Project Setup (precedes SDD §14 Phase 1) · **Sprint goal:** a contributor can clone, run, and merge a PR the standard way.

## Summary

**The sprint goal is not met, and no part of it can be met yet: this working tree is not a git repository.** Documentation and planning, by contrast, are finished — the SRS reached v0.5, the design and schema set is complete, and the planning folder now carries a sprint plan and a full 164-requirement backlog with all five scope decisions resolved. What exists of the codebase is scaffolding: 19 backend module stubs and 12 one-line placeholder migrations that compile to a binary printing one string. The project is ready to start Sprint 0, not to finish it.

## Scope Status

| Backlog item | Status | Evidence |
|---|---|---|
| **Documentation set** — SRS, SDD, database schema, 8 JSON standards, 5 architecture docs, 5 standards, 4 concept docs | Done | 26 documents under `docs/`; indexed in `docs/README.md` with authority rules |
| SRS baselined | Done | `docs/requirements/srs.md` v0.5 — 164 FRs, 40 NFRs, MVP criteria §9 |
| Database schema specified | Done | `docs/design/02. Database Schema.md` — 94 tables across 12 migrations |
| **Sprint plan** | Done | `projects/planning/01. Sprint Plan.md` — re-baselined 2026-08-11 (MVP → `v0.6.0`, RAD split) |
| **Product backlog** | Done | `projects/planning/02. Product Backlog.md` — all 164 FRs assigned; 149 scheduled, 15 unscheduled |
| Scope decisions D-1…D-5 | Done | Backlog §6.1 — all five resolved 2026-08-11, applied to SRS, SDD and sprint plan |
| Project-management templates (status, retrospective, release) | Done | `projects/status/`, `projects/retrospectives/`, `projects/releases/` |
| Initialize git repository, first commit, branch protection | **Not started** | No `.git` directory in the working tree |
| CI pipeline (fmt, clippy, backend tests, frontend lint/type-check/tests, builds, commit-header lint) | **Not started** | No `.github/` directory |
| Docker Compose stack runs | **In progress** | `deploy/docker/docker-compose.yml` defines all five services correctly, but the `backend` container runs `cargo run`, and `kelir-backend/src/main.rs` prints one line and exits — the stack starts, the API does not |
| `.env.example` complete | Done | `deploy/env/.env.example` — 8 `KELIR_*` and 2 `VITE_*` variables, matching the compose environment |
| Issue tracker seeded with Phase 1–2 backlog | **Not started** | No tracker in use; the backlog document is the source to seed from |
| Backend skeleton | **Scaffold only** | 19 module stubs (one comment line each), 12 placeholder migrations (38–49 bytes each); `Cargo.toml` has axum/serde/tokio/tracing and **no `sqlx`**, so no pool or migration runner exists |
| Frontend skeleton | **Scaffold only** | Vue 3 + Vite + TS + Pinia + vue-router + axios wired; `src/api/client.ts` is an axios instance without envelope unwrapping; feature folders are `.gitkeep` |

Nothing above claims FR coverage — per the backlog, Sprint 0 carries no functional requirements.

## Blockers and Decisions Needed

| Item | Owner | Needed by | Recommendation |
|---|---|---|---|
| Not a git repository — blocks the entire sprint goal, and every Definition-of-Done clause that mentions merging | Project owner | Before any Sprint 0 work | `git init`, commit the current tree as the documentation baseline, push to the remote, then apply branch protection per the [git workflow](../../docs/standards/05.%20Git%20Workflow.md) |
| No CI, and the toolchain it must run does not exist yet | Backend + frontend leads | Sprint 0 exit | Frontend `package.json` has no `lint`, `type-check` or `test` scripts and no vue-tsc/vitest/eslint; the backend has no test dependencies. Add both toolchains *before* writing the workflow, or the pipeline will be green over nothing |
| Compose stack cannot serve the API | Backend lead | Sprint 0 exit | The stack is only demonstrably "up" once `main.rs` serves the router. This is Sprint 1 work (health endpoints), so either accept compose as verified-at-Sprint-1 or pull a minimal listener into Sprint 0 |
| Issue tracker not chosen | Project owner | Sprint 1 planning | Seed from backlog §4; the epics map one-to-one onto tracker epics and the item rows onto issues |

## Scope Changes This Sprint

Five scope decisions were raised and resolved on 2026-08-11 (backlog §6.1). Two changed the plan materially:

- **D-1** — the MVP milestone moved from `v0.5.0` to `v0.6.0`. SRS §9 requires attachments, comments, activity log and audit of approvals, all Phase 6; tagging MVP at Phase 5 would have declared it against five unmet acceptance criteria.
- **D-2** — RAD split across two phases. `document_types.form_id` references `rad_forms`, so `0004_rad.sql` must apply before `0005_document.sql`; Phase 4 now also carries the RAD storage APIs and a read-only JFSS renderer.

D-3 raised FR-API-004 to `Must` and separated priority from MVP scope in SRS v0.5; D-4 chose ClamAV; D-5 baselined six previously proposed targets. No FR was added, removed or re-scoped.

## Next Sprint Entry Criteria

Sprint 1 cannot start until Sprint 0's own items close. In order:

1. **Git repository initialized**, documentation baseline committed, remote set, branch protection applied. Everything else depends on this.
2. **Toolchains complete before CI:** backend test dependencies and `sqlx` added to `Cargo.toml`; frontend `lint` / `type-check` / `test` scripts and their packages (vue-tsc, vitest, eslint) added, plus Tailwind v4 and shadcn-vue, which Sprint 1 needs on day one.
3. **CI green on a trivial PR** merged through the workflow — the sprint's stated exit criterion.
4. **Backlog seeded** into the chosen tracker from backlog §4, Phase 1–2 items first.
5. **JFSS operator-parity spike scheduled** for Sprints 5–6, not Sprint 13 — decision D-2 moved the renderer into Phase 4, and the sprint plan §5 now says so.

Carry-over is the whole of Sprint 0. No re-baselining of the sprint plan is warranted yet — velocity is still unmeasured, and the plan is scope-sequenced rather than date-committed, so the sequence stands as written.
