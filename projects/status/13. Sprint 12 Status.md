# Sprint 12 Status — 2026-08-31

**Status:** Draft · **Last updated:** 2026-08-31

**Phase:** 6 Attachments, Comments, Activity (SDD §14) · **Sprint goal:** a document stops being only a form and a decision — it carries the files it is about, the conversation around it, and a timeline that says what happened to it.

> **This report is `Draft` and not `Final`, and it is the second sprint running to open with that sentence.** [Sprint plan](../planning/01.%20Sprint%20Plan.md) §2 makes an item whose author is also its only verifier `In progress` rather than Done, and **every construction commit in this sprint has one author**. The same §2 rule keeps the report out of `Final` until the independent pass and the retrospective land in one change. **That pass has not happened.**
>
> **[Record 12](../verifications/12.%20Sprint%2012%20Surface%20Read.md) is not it**, and says so before it says anything else: the party that wrote all six items read them. It found three defects and is worth its findings; it is worth nothing as evidence of independence.
>
> **This is the fourth consecutive sprint without a second reader.** Sprints 9, 10 and 11 closed the same way; Sprint 11's was rescued late by a phase boundary, and there is no phase boundary here. The [construction plan](../planning/07.%20Sprint%2012%20Collaboration%20Construction%20Plan.md) §15 named the one decision that would break the run — **a party and a date, set at planning** — and recorded that it could not take it. Nobody took it.

## Summary

**Six items planned, six built, all six merged to `main`** — #283, #285, #286, #288, #289 and #290 — and the goal's sentence is true of the running product: a document carries its files, its conversation, and a timeline of both.

**The exit demo ran and the phase's wiring did not work.** [Record 11](../verifications/11.%20Sprint%2012%20Exit%20Demo.md): twelve steps against release images, twelve passed — after three defects, every one of them in deployment configuration that no test reads. **Two of them would have made attachments non-functional in any real deployment while the whole suite stayed green.**

**One decision was taken outside its item and is recorded rather than absorbed.** [#246](https://github.com/sujanto-gaws/kelir/issues/246)'s download-side gate shipped with [#245](https://github.com/sujanto-gaws/kelir/issues/245), one item early, because `modules::attachment` claimed an attachment could not be retrieved until the gate existed and nothing set `CLEAN`. Shipping download without it would have served every unscanned byte in the product **and made that sentence false**.

## Scope Status

| Backlog item (FR IDs) | Status | Evidence |
|---|---|---|
| The attachment migration, upload to object storage, metadata — FR-ATT-001, 003 | **author-verified** | [#244](https://github.com/sujanto-gaws/kelir/issues/244), PR #283. `0031_attachment.sql`; 10 tests; 3 mutations red. Object written before the row, so an orphaned object is the failure that can happen and a row without one is not |
| Download behind a permission check, size and type validation — FR-ATT-002, 004, 005 | **author-verified** | [#245](https://github.com/sujanto-gaws/kelir/issues/245), PR #286. 8 tests; 5 mutations red. Size refused on the body before it is read; type decided by content, never by name; **and #246's download gate, taken early** |
| Async virus-scan gating — FR-ATT-001, 002 | **author-verified** | [#246](https://github.com/sujanto-gaws/kelir/issues/246), PR #288. 9 tests; 3 mutations red, one of which found an untested predicate — see the campaign below. Sized 2026-08-31 rather than guessed: 25 MiB in ~169 ms, ~1 GB resident, no gain from concurrency |
| The activity migration, lifecycle and workflow events — FR-ACT-001, 004 | **author-verified** | [#247](https://github.com/sujanto-gaws/kelir/issues/247), PR #289. `0033_activity.sql`; 7 tests; 2 mutations red and **one that would not compile** — the audit-shaped `record` cannot be written without changing every call site |
| Activity events for attachments and comments, and the audit address — FR-ACT-002, 003, FR-ATT-008, FR-CMT-007, FR-AUD-005 | **author-verified** | [#248](https://github.com/sujanto-gaws/kelir/issues/248), PR #290. 4 tests; 3 mutations red. **`audit_events.ip_address` stops being always null** — 55 call sites, one line each |
| The comment migration, and a comment on a document — FR-CMT-001 | **author-verified** | [#249](https://github.com/sujanto-gaws/kelir/issues/249), PR #285. `0032_comment.sql`; 9 tests; 4 mutations red. Not the decision comment, and `commenting_writes_no_decision_record` makes that checkable over rows |

**Also merged:** [#287](https://github.com/sujanto-gaws/kelir/pull/287), the ClamAV measurement that discharged the sprint's last entry criterion, and PR #291, the exit demo's fixes and two verification records.

**Suite:** 47 new integration tests and 11 new unit tests across six binaries; CI green on every item's branch, including the browser flow against the release stack.

**Mutation campaign (§2's per-sprint rule): 20 mutations over predicates this sprint added — 18 red on the first attempt, one green, and one the compiler refused.**

**The green one is the campaign earning its keep.** Dropping `AND virus_scan_status = 'PENDING'` from the scan write left **every test in the file passing**, because `pending_scans` already filters to `PENDING` and a decided row was therefore never handed to the write. A guard behind another guard — [record 07](../verifications/07.%20Sprint%2010%20Surface%20Verification.md)'s shape — and it now has a test that reaches the statement rather than the layer in front of it.

**The refused one is stronger than a red.** Changing `activity::record` to take a `&PgPool`, which is the audit trail's shape, fails at every call site: each has a transaction and nothing else. The rule #247 AC2 states in prose is held by the type.

## What the close found

**The exit demo ([record 11](../verifications/11.%20Sprint%2012%20Exit%20Demo.md)) — three defects, all in deployment wiring.**

| # | Finding | Closed by |
|---|---|---|
| D1 | The deployed stack had **no object storage configuration and no scanner** — uploads would have addressed `localhost:9000` inside the backend's own container, and any that landed would have stayed `PENDING` for ever | PR #291. The `clamav` service went into the development compose with #246 and not into the one CI deploys |
| D2 | **`NoSuchBucket` on every upload.** A YAML folded scalar keeps more-indented lines literal, so `minio-init`'s command reached the container as three shell commands and exited 0 | PR #291, both stacks. The same defect sat unexercised in the development compose because that bucket had been made by hand |
| D3 | A false failure in the demo's own script — a case-sensitive header lookup | Fixed and recorded, because a demo that cries wolf is disbelieved when it matters |

**The author's read ([record 12](../verifications/12.%20Sprint%2012%20Surface%20Read.md)) — three findings, all in the seam between items**, filed as [#292](https://github.com/sujanto-gaws/kelir/issues/292), [#293](https://github.com/sujanto-gaws/kelir/issues/293) and [#294](https://github.com/sujanto-gaws/kelir/issues/294). The timeline discloses attachment file names to a caller holding no `attachment:read`; a download that fails after its event is committed is recorded as one that happened; and the scan write is the sprint's only statement with no tenant in its predicate.

## Blockers and Decisions Needed

| Item | Owner | Needed by | Recommendation |
|---|---|---|---|
| **No independent pass** — fourth consecutive sprint | Whoever did not write it | Before this report is `Final` | It is a party and a date, not a reminder. §15 owed that decision at planning and it is still owed; a fifth sprint of author-verified rows is the plan admitting the rule does not fire |
| **Exit criterion 1 cannot be met as written** | Project manager | Sprint 13 planning | The exit asks for a browser demonstration of a feature set with no screens, by this sprint's own design. Record 11 §1; the sentence needs rewriting, not the work |
| **The verification allowance was not spent** | Sprint owner | Sprint 13 | Five findings came in, three were added, **none closed** — see the retrospective |
| **`v0.5.0`'s images are still not built from the tag** | Release owner | Before `v0.6.0` | [Release record 05](../releases/05.%20Release%20v0.5.0.md) has been `Draft` for one row since 2026-08-30, and the same session that could not do it then could do it now |

## Scope Changes This Sprint

- **Nothing was added and nothing was cut.** Six planned, six delivered — including item 5's FR-AUD-005 half, which the construction plan named as the cut candidate and which was not cut.
- **The item order changed at planning and the reason is a foreign key.** `activity_events` references `attachments` and `comments`, and `comments` references `attachments`, so the three Phase 6 migrations have exactly one legal order. The sprint plan's order would have produced a migration that does not apply.
- **All three migration numbers had to move.** `0028`–`0030` were reserved for this sprint and taken by Sprints 10 and 11; the schema's mapping table had been left listing two of each. Corrected as step 0.
- **#246's download gate shipped with #245**, one item early. Recorded in the module, the changelog and the item's own PR rather than absorbed.

## Next Sprint Entry Criteria

**Sprint 13 carries the MVP and closes Phase 6**, which makes this boundary the one where an unexamined surface costs most. In order:

1. **The independent pass over Sprint 12's surface.** It is the criterion this report is `Draft` for, and the only one that has failed four sprints running.
2. **The eight open findings triaged** — [#273](https://github.com/sujanto-gaws/kelir/issues/273), [#274](https://github.com/sujanto-gaws/kelir/issues/274), [#278](https://github.com/sujanto-gaws/kelir/issues/278), [#279](https://github.com/sujanto-gaws/kelir/issues/279), [#280](https://github.com/sujanto-gaws/kelir/issues/280), [#292](https://github.com/sujanto-gaws/kelir/issues/292), [#293](https://github.com/sujanto-gaws/kelir/issues/293), [#294](https://github.com/sujanto-gaws/kelir/issues/294) — and #278's *reproduce before fixing* honoured.
3. **Sprint 13's exit sentence names artefacts rather than a user's day**, so that it can be met by the work it scopes.
