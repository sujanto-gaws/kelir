# Sprint 15 Status — 2026-09-09

**Status:** Final · **Last updated:** 2026-09-13 (the suite discrepancy is closed; see *Suite*)

**Phase:** 7 RAD (SDD §14) · **Sprint goal:** the authoring surface RAD has never had, and the process controls [retrospective 12](../retrospectives/12.%20Sprint%2014%20Retrospective.md) converted from sentences into artefacts.

> **This report is `Final` and every row in it reads `author-verified`.** [Sprint plan](../planning/01.%20Sprint%20Plan.md) §2 says what happens when the independent pass does not run, and this is the second sprint to apply it — **the first in which a test enforces the label rather than a person remembering it**. `sprint_reports_are_verified.rs`, merged as this sprint's item 1, asserts that every report either cites a verification record or contains `author-verified`. It governs this file, which is why it ran first.
>
> **`Final` does not mean every exit criterion is met.** [Sprint plan](../planning/01.%20Sprint%20Plan.md) §5 sets four, and **three are met**:
>
> | # | Exit criterion | Verdict |
> |---|---|---|
> | 1 | **Items 1–6 merged and `Done` per §2**, or cut at the line with the reason recorded | **Partly.** All six merged or closed, and the cut line *was* walked — row 7 was **built**, not dropped. **No row is `Done`**, because §2's independent-verification clause is unmet, exactly as in Sprint 14 |
> | 2 | **A verification record exists, or the report says `author-verified` and item 3's test enforces the choice** | **Met, on the branch the criterion deliberately made available.** No record 15 exists; this report carries the label; the test enforces it. **The underlying rule failed for the seventh time** — §*The pass* below |
> | 3 | **D-66 answered** rather than carried a second time | **Met 2026-09-08, in week one.** The first time either twice-carried rule has fired on schedule |
> | 4 | **No release**; the sprint ends demoable and does not tag | **Met.** `git tag` still ends at `v0.6.0`; Phase 7 exits at Sprint 16 |

## Summary

**Six planned above the line, six built, and the seventh built too.** [Construction plan 11](../planning/11.%20Sprint%2015%20RAD%20Authoring%20Construction%20Plan.md) §4 ordered seven rows with the line at six. All six above it merged between 2026-09-08 12:15Z and 2026-09-09 — **not in the order §4 set**, which §*Scope Changes* records; **the row below the line was built rather than dropped**, and `gh issue list --label sprint:15` now returns seven closed issues and none open. **This is the first sprint in three in which the cut line was walked inside the sprint** rather than after the boundary on request.

**The two things Phase 7's published sentence needed both shipped.** `v0.6.0`'s release notes said document types are configured through the API *with the builders scheduled for Phase 7*, plural. The document type and menu builders landed in Sprint 14; **the form builder ([#373](https://github.com/sujanto-gaws/kelir/issues/373)) and the list builder ([#374](https://github.com/sujanto-gaws/kelir/issues/374)) landed here**, which makes that sentence true with a sprint to spare.

**And the gate is shut.** **D-66** was answered on 2026-09-08 in the week [construction plan 11](../planning/11.%20Sprint%2015%20RAD%20Authoring%20Construction%20Plan.md) §7 step 2 asked for — *yes, the administrator is inside the gate* — and [#335](https://github.com/sujanto-gaws/kelir/issues/335) closed on 2026-09-09 with `main` requiring five contexts and bypassing off. **It had stood open since [#330](https://github.com/sujanto-gaws/kelir/pull/330) merged on 2026-09-03**, and every merge in that window went through it, thirteen in Sprint 14 alone.

**The capacity argument that set the line at six was wrong, and the diff says so.** §3 cut the line from seven to six on the reasoning that the form builder was *larger than anything Sprint 14 built*. It came in at **2,117 lines** against Sprint 14's largest two at 5,275 and 5,108. The sprint then built the row it had cut, so nothing was lost — but the estimate that bought the reduction was wrong in its stated direction, and it is the retrospective's.

## Scope Status

| Backlog item (FR IDs) | Status | Evidence |
|---|---|---|
| **Item 3 (plan order 1)** — §2's independent-pass label becomes a test | **author-verified** | [#375](https://github.com/sujanto-gaws/kelir/issues/375), PR [#381](https://github.com/sujanto-gaws/kelir/pull/381). `sprint_reports_are_verified.rs`, in the shape `adr_records_are_current.rs` has. **The constraint it carried was met and its position was not.** §7 step 3 required it to merge before the report it governs: it merged 2026-09-08 and this report was written 2026-09-09. **It did not run first, though** — PR [#383](https://github.com/sujanto-gaws/kelir/pull/383) merged 27 minutes ahead of it, at 12:15:13Z against 12:42:38Z. The plan's argument was against a control landing in the *last* week, which merging second on day one leaves untouched. It governs this file. Four mutations, four red |
| **Item 5 (plan order 2)** — a release follow-up says whether its mechanism was reproduced or only observed | **author-verified** | [#377](https://github.com/sujanto-gaws/kelir/issues/377), PR [#382](https://github.com/sujanto-gaws/kelir/pull/382). One template section and one sentence, in [the checklist](../releases/00.%20Release%20Checklist%20Template.md) **Sprint 16's tag will use** — the next release run finds the rule already there rather than being the run that motivates it |
| **Item 1 (plan order 3)** — the form builder, FR-RAD-004 | **author-verified** | [#373](https://github.com/sujanto-gaws/kelir/issues/373), PR [#383](https://github.com/sujanto-gaws/kelir/pull/383). 2,117 lines. A JFSS definition authored, revised and published from the browser; `build-a-form.spec.ts` is the browser evidence. **The first cut authors structure and rule *selection*, not a visual expression builder** (§8) — `calculate` and `conditional.logic` are JSON Logic in a text field, validated by the server's own `rad::domain::engine` and reported through the S10.3 envelope. Six mutations, six red |
| **Item 4 (plan order 4)** — ten stated mutations are run and dated, or rewritten | **author-verified** | [#376](https://github.com/sujanto-gaws/kelir/issues/376), PR [#384](https://github.com/sujanto-gaws/kelir/pull/384). [Retrospective 12](../retrospectives/12.%20Sprint%2014%20Retrospective.md)'s fourth action. **Merged before mid-sprint refinement, which was the whole point of the position.** Nine of ten reddened; **the tenth came back green and was the reason for the row** — §*Mutation campaign* |
| **Item 2 (plan order 5)** — the list builder, FR-RAD-003, FR-RAD-004 | **author-verified** | [#374](https://github.com/sujanto-gaws/kelir/issues/374), PR [#385](https://github.com/sujanto-gaws/kelir/pull/385). 1,375 lines, and cheap after item 1 exactly as §8 predicted. **This is [SDD](../../docs/design/01.%20System%20Design%20Document.md) §8.2.4's stated condition being made true**: a broken list definition stores happily and fails at render, *on the condition that the builder calls the same resolution to show an author the problem first*. Four mutations, three red first time |
| **Item 6 (plan order 6)** — the required checks consume the jobs that run, and **D-66** is answered | **author-verified** | [#335](https://github.com/sujanto-gaws/kelir/issues/335), closed 2026-09-09. **No code.** `main` requires `Backend (Rust)`, `Frontend (Vue)`, `Commit messages`, `End-to-end (browser)` and `Pull-request title`, and bypassing is off. **AC3 was satisfied by observation, not by an API call** — see §*The gate* |
| — | **CUT LINE — walked, and the row below it was built** | |
| **Item 7** — a configured action can be scoped to one list | **author-verified** | [#348](https://github.com/sujanto-gaws/kelir/issues/348), PR [#388](https://github.com/sujanto-gaws/kelir/pull/388), merged `5c57d69`. `0042_rad_action_scoped_to_a_list.sql`. **Two defects found in in-flight code and fixed before merge** — §*Scope Changes*. Two mutations, two red |

**"Done" requires the [sprint plan's](../planning/01.%20Sprint%20Plan.md) Definition of Done, whose independent-verification row is unmet for every item above, so no row here is `Done`.** Every other clause is met: merged to `main`, CI green on all five required checks for each pull request — **which for the first time means the checks actually gated the merge** — OpenAPI current by `every_annotated_route_reaches_the_document`, and the permission and audit rows in place. What is missing is a second reader, for the second sprint running.

**Also merged, and not in §4's list:** PR [#379](https://github.com/sujanto-gaws/kelir/pull/379) (the ERP document's links resolve), [#380](https://github.com/sujanto-gaws/kelir/pull/380) (**D-66** recorded), and [#386](https://github.com/sujanto-gaws/kelir/pull/386) — how to run the backend tests and the two traps, which was written because this sprint hit both.

**Still open at the close:** PR [#390](https://github.com/sujanto-gaws/kelir/pull/390), the documentary record of #335 — the settings change itself is applied and the issue is closed; what is in review is the three documents that say so.

**One migration:** `0042_rad_action_scoped_to_a_list.sql`. It took a number the [Database Schema](../../docs/design/02.%20Database%20Schema.md) mapping table had **reserved** for the integration migration, so that table shifted the integration and plugin rows to `0043`/`0044` — the third sprint running that the table has absorbed a shift rather than held a collision.

**Suite: 1,477 backend tests across 74 targets · 634 frontend tests across 52 files · 13 browser specs.** The backend figure is a full `cargo test --no-fail-fast` against this sprint's merged content, run 2026-09-09 with PostgreSQL, MinIO and ClamAV up: **1,477 passed, 0 failed, exit 0**. The frontend suite was run the same day — 52 files, 634 passed, 0 failed, 104 s. Two browser specs were added this sprint, `build-a-form.spec.ts` and `build-a-list.spec.ts`, and both are AC5 evidence for their items rather than decoration.

**That total is 18 lower than the 1,495 [status report 15](15.%20Sprint%2014%20Status.md) carried, and the suite did not shrink.** `#[test]` and `#[tokio::test]` attributes across `kelir-backend/tests` and `kelir-backend/src` went from **1,458 at Sprint 14's close to 1,466 today**, so this sprint added eight. **One of the two runtime measurements counts something the other does not, and which is not established here.**

> **Amended 2026-09-13: it is established now, and this paragraph was right about the part that mattered.** [Status report 18](18.%20Sprint%2017%20Status.md) ran [retrospective 14](../retrospectives/14.%20Sprint%2016%20Retrospective.md)'s fourth action and the answer is that **this report's 1,477 was correct and [status report 15](15.%20Sprint%2014%20Status.md)'s 1,495 was an overcount of 26** — now struck there. *The suite did not shrink* was the right conclusion from the right evidence.
>
> **What kept this open was one pattern, and the constant is eleven.** The static counts above — 1,458 → 1,466 — omit `#[sqlx::test]`, of which the tree holds **11** at each of the three commits. They therefore run exactly 11 below the runtime figure and could show the **+8** delta without ever meeting a runtime number. **Both figures above reproduce exactly when re-measured on 2026-09-13**, so the method was sound and the scope was one attribute short. **Widening it to include `#[sqlx::test]` gives 1,466 + 11 = 1,477 — this report's own runtime figure, to the test** — and does the same for [status report 17](17.%20Sprint%2016%20Status.md) at 1,486 + 11 = 1,497. It fails only on report 15. That is what turns *the suite did not shrink* into *one report's number was wrong*.
>
> **The hypothesis this gap acquired later — that a target went unexecuted — is refuted**, and it was never this report's. It is named here because two sprints of documents carried it. It is recorded rather than reconciled, because the alternative — quietly carrying the larger number forward — is exactly the failure the [Database Schema](../../docs/design/02.%20Database%20Schema.md)'s own table-count line describes: *a number maintained by remembering will always eventually be wrong*. **The figure above comes from a complete capture**, which is why this line states the command and the exit code rather than only the number: two earlier runs were discarded, one having filtered its own failure out of its output and one having truncated its total and the name of a failing target together.

### Mutation campaign (§2)

**The campaign as §2 defines it did not run**, because §2 runs it *with* the mid-sprint independent pass and that pass did not happen. What exists is per-item evidence in the test files, which is where [record 13](../verifications/13.%20Sprint%2013%20Independent%20Pass.md) §6 established it should live. Measured from the sprint's own diff:

| Item | Mutations | Red first time |
|---|---|---|
| Item 3 ([#375](https://github.com/sujanto-gaws/kelir/issues/375)) | 4 | 4 |
| Item 1 ([#373](https://github.com/sujanto-gaws/kelir/issues/373)) — the form builder | 6 | 6 |
| Item 4 ([#376](https://github.com/sujanto-gaws/kelir/issues/376)) — the stated ten, run | 10 | 9 |
| Item 2 ([#374](https://github.com/sujanto-gaws/kelir/issues/374)) — the list builder | 4 | 3 |
| Item 7 ([#348](https://github.com/sujanto-gaws/kelir/issues/348)) | 2 | 2 |
| **Run and recorded** | **26** | **24** |

**26 run and 24 red — the most mutations a sprint has run here, and not the best ratio.** 92% against Sprint 14's 96% (24 of 25), Sprint 6's 65% and Sprint 7's 37%. **The two figures move in opposite directions and that is the honest reading**: a sprint that runs more mutations reaches further into code the tests were not written against, so a ratio that dips while the count rises is the campaign working rather than slipping. It would be worth watching if both fell.

**Both greens were findings, which is what §2.9 says a green mutation is.** Item 4's tenth — `DocumentSort::default()` in place of `plan.sort` — came back green because the fixture's two rows were already in the asserted order, so **the mutation was invisible rather than absent**; the fixture now creates them in the other order and it reddens. Item 2's fourth had the same shape, a fixture in which two conditions coincided, copied from `require_bound`. **Neither was a wrong anchor; both were fixtures that did not reach the code under test**, which is precisely the failure mode [coding standard](../../docs/standards/01.%20Coding%20Standard.md) §2.9 names — *the gate absorbs every mutation beneath it*.

**Item 4 is also the sprint's own answer to the last retrospective.** Ten doc comments across five files said *the mutation that must make this red is …* — an intention written in the voice of evidence. All ten were run. **Two named symbols that do not exist**, in `evaluation.rs` and one other, which is the sharper half of the finding: a mutation pointing at a symbol nobody can find was never evidence of anything.

### The gate

[#335](https://github.com/sujanto-gaws/kelir/issues/335) closed on the strength of a probe rather than an assertion, because **the setting cannot be read from this repository at all**: `repos/…/branches/main/protection` answers `403` to its own token. The context list *is* readable, and returns the five.

For `enforce_admins`, PR [#389](https://github.com/sujanto-gaws/kelir/pull/389) carried a deliberately failing browser assertion. `End-to-end (browser)` went red on it; the other four required checks passed; **the merge box was greyed with no bypass offered.** Closed unmerged, branch deleted.

**The absent override is the evidence, and the blocked state is not.** GitHub reports `BLOCKED` to an administrator who *can* bypass exactly as it does to one who cannot — so the state string proves the checks are required, and only the missing *merge without waiting for requirements to be met* button proves who they bind. **AC3 was worded as an observation for this reason**, before anyone knew the endpoint would 403, and it turned out to be the only available route.

### The pass

**It did not run, for the seventh time, and nobody asked either.** There is no record 15 in [`projects/verifications/`](../verifications/). [Construction plan 11](../planning/11.%20Sprint%2015%20RAD%20Authoring%20Construction%20Plan.md) §7 step 6 put it on the sequence as a named event with a dispatcher, having already said that item 1 *does not make it fire, and nothing in this plan can*. That turned out to be exactly right.

**What is different this time is that the absence is now asserted by a test rather than by whoever writes the report.** That is what item 1 bought, and it is less than it sounds: `sprint_reports_are_verified.rs` makes the label unavoidable, not the pass. **Eight sprints have now closed without an independent pass firing on its own rule**, and [retrospective 12](../retrospectives/12.%20Sprint%2014%20Retrospective.md) already withdrew permission to restate the action. The retrospective has to do something other than write it an eighth time.

## Blockers and Decisions Needed

| Item | Owner | Needed by | Recommendation |
|---|---|---|---|
| **The independent pass has never fired on its own rule, and this is the eighth sprint.** §2 names the project manager, mid-sprint refinement, and a criterion; the mechanism is a sentence in a document | Project manager | **Sprint 16 planning** | **Amend §2 to describe what actually happens** — the pass is dispatched by the product owner on request — or give it a mechanism that is not a person remembering: a scheduled agent, or a release-checklist row that blocks the tag. Item 1 proved the shape works; a rule with a test behind it is a rule. **A rule describing a practice nobody performs is worse than a practice honestly described**, and that sentence is now two retrospectives old |
| **[#274](https://github.com/sujanto-gaws/kelir/issues/274) stopped being harmless the moment the gate closed** — an unexplained intermittent 500 in `master_data_party_roles`, in a job that is now a required check nobody can bypass | Sprint owner | **Sprint 16** | **Schedule it.** **D-75** accepted this cost in writing: an intermittent red was a walk-past and is now a 25-minute re-run, and a second recurrence spends the break-glass on a defect nobody has reproduced. Four sprints unscheduled |
| **D-15 is answered and applied nowhere.** The `conditional.logic` registry tier, the `regex` params constraint at save, and the ADR for the pattern dialect are all unfiled | Backend lead | **Sprint 16** | File the three. **One is a live defect rather than a design gap**: an uncompilable pattern is stored happily today and then rejects every value, because `matches_pattern` maps a compile error to `false` — right at evaluation, wrong at save |
| **D-73 — whether Kelir grows an ERP layer** — is the only row left in [Product Backlog](../planning/02.%20Product%20Backlog.md) §6.2, and [ADR-0037](../../docs/architectures/adr/0037.%20ERP%20Posting%20Runs%20Inside%20the%20Document%20Transaction.md) stays `Draft` behind it | Product owner | Before any ERP work is estimated | **Split the answerable part off.** Three of the seven platform changes §6 names — the unimplemented `POST` lifecycle stage, the general outbox, and refusing writes to a posted document — are gaps in Kelir's own specification with or without an ERP, and can be scheduled on their own merits |

## Scope Changes This Sprint

- **Nothing was cut. Seven planned, seven built** — six above the line and the one below it. The cut line was **walked at the boundary**, which is [retrospective 11](../retrospectives/11.%20Sprint%2013%20Retrospective.md)'s seventh action working for the first time inside the sprint that owns it: the last two sprints put rows below the line and then built them afterwards, on request.
- **Five of [retrospective 12](../retrospectives/12.%20Sprint%2014%20Retrospective.md)'s seven actions were rows here and all five closed.** The other two were discharged by the act of planning. **This is the first sprint in which no retrospective action sat below the cut line**, and the first in which all of them closed.
- **Two defects were found in in-flight code during item 7 and fixed before merge**, neither in the issue. The reader's **documented contract was wrong in two places** — `actions_for` and the handler both claimed an absent `listId` returns *every action of this context*, when `list_id = $3` is `NULL` rather than `true` for a scoped row, so the caller gets the tenant-wide rows alone. The behaviour was right and the sentences were not. And the **foreign key did not carry the tenant**, letting one tenant's action name another tenant's list — unreadable, because the catalogue filters `rad_actions.tenant_id`, but writable, where this project has settled on unwritable three times ([Database Schema](../../docs/design/02.%20Database%20Schema.md) deviations #19 and #20, and `0020`). Both were fixable only because the migration was still unmerged.
- **Three decisions taken, and three closed that were already answered.** **D-75** (`strict` stays off, the break-glass is the toggle recorded) and **D-15** (both spike halves, four sprints after they were raised) are new; **D-66** was answered. **D-39 and D-40 were moved to §6.1 after eleven days in a table headed *Open* with their resolutions written into the question column** — found by reading the rows, not by a check, and the table was malformed in three of its four rows besides.
- **The sprint did not run in the order [construction plan 11](../planning/11.%20Sprint%2015%20RAD%20Authoring%20Construction%20Plan.md) §4 set, and nothing noticed until this report.** §4 ordered the two small process rows first and the form builder third; the merge log says the form builder went first, at 12:15:13Z, with item 3's test 27 minutes behind it and item 5's row an hour after that. **The binding constraint still held** — §7 step 3 required item 3 to merge before the report it governs, and it did, a day ahead. **What it cost was the margin rather than the control**: §4 put the small rows first partly because item 1 was expected to *run the length of the sprint*, and a control ordered behind a row that long would have landed in the last week §4 was warning about. It did not, because that row came in at two fifths of its estimate. **The sequence in a construction plan is prose, and no check compares it to the merge log** — which is why this is recorded here rather than having been caught on the day.
- **[#324](https://github.com/sujanto-gaws/kelir/issues/324) was already closed, and this line said otherwise until 2026-09-09.** It closed on 2026-09-07 with PR [#364](https://github.com/sujanto-gaws/kelir/pull/364) — ClamAV's stream limit is chosen rather than inherited — in the two days between Sprint 14's report and Sprint 15's planning. **The claim was carried out of [status report 15](15.%20Sprint%2014%20Status.md), where it was true when written, rather than checked against `gh issue list`**, which is the failure this project keeps finding in its own documents and had not yet found in one of its status reports. Sprint 15 therefore carried **one** finding out of Sprint 14, not two.

## Next Sprint Entry Criteria

1. **§2's independent-pass rule is amended or given a mechanism.** Eight sprints, eight failures, and permission to restate it was withdrawn two retrospectives ago. This is first because it is the only entry criterion that has been carried this many times.
2. **[#274](https://github.com/sujanto-gaws/kelir/issues/274) is scheduled**, on **D-75**'s reasoning rather than on its own age.
3. **D-15's three artefacts are filed** — the registry tier, the save-path refusal, and the pattern-dialect ADR — so that Sprint 16 starts with them as rows rather than as a decision nobody has costed.
4. **PR [#390](https://github.com/sujanto-gaws/kelir/pull/390) is merged**, so the documents describing the gate match the gate.
5. **Sprint 16's allowance is named by position**, as this sprint's was. It opens holding **one** carried finding, [#274](https://github.com/sujanto-gaws/kelir/issues/274) — see the correction above.

**Phase 7 has one sprint left and exits at `v0.7.0`.** Its five named deliverables were met in Sprint 14; **the sentence `v0.6.0` published about the builders is met now**. What Sprint 16 owes is the release itself and whatever the phase's exit demands — which, for the first time in this project, is a sprint whose scope is genuinely open rather than owed.
